# PLACEHOLDER — DO NOT ENABLE until staging resources are provisioned
# and the checklist in infra/azure/README.md is complete.
#
# To activate:
#   1. Complete all items in infra/azure/README.md pre-provisioning checklist
#   2. Set GitHub Secrets: AZURE_CLIENT_ID, AZURE_TENANT_ID, AZURE_SUBSCRIPTION_ID
#   3. Provision ib-stg-api App Service
#   4. Uncomment this workflow and remove these header comments
#
# name: Deploy API — Staging
#
# on:
#   push:
#     branches: [main]
#     paths:
#       - "apps/api/**"
#       - ".github/workflows/deploy-api-staging.yml"
#
# permissions:
#   id-token: write   # required for OIDC
#   contents: read
#
# jobs:
#   deploy:
#     name: Deploy FastAPI to Staging
#     runs-on: ubuntu-latest
#     environment: staging
#     needs: []   # add api-ci job dependency once wired
#
#     steps:
#       - uses: actions/checkout@v4
#
#       - uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.AZURE_CLIENT_ID }}
#           tenant-id: ${{ secrets.AZURE_TENANT_ID }}
#           subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
#
#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.12"
#
#       - name: Install and build
#         working-directory: apps/api
#         run: pip install -e ".[dev]"
#
#       - name: Deploy to Azure App Service
#         uses: azure/webapps-deploy@v3
#         with:
#           app-name: ib-stg-api
#           package: apps/api
