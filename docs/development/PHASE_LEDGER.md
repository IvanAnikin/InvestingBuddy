# InvestingBuddy — Phase Ledger

Durable status list for phased development. The orchestrator reads and updates
this file. `docs/ROADMAP.md` is authoritative for the product roadmap; this
ledger tracks execution state (branch → PR → merge → deploy → validated → closed).

Legend: ✅ closed+validated · 🔜 next · 🟡 in progress · ⛔ blocked

| Phase | Title | Status | PR / SHA | Migration | Notes |
|---|---|---|---|---|---|
| 28A | Single-company LLM council | ✅ | #44 `00a3b2f` | no | council off-by-default, 8 agents |
| 28B | Discovery-run LLM council | ✅ | #45/#46/#47 `350f062` | no | gated by 2 flags |
| 28B.2 | Async discovery council | ✅ | #48 `e3f1cab` | no | pollable run-level council |
| 28A.1 / 28B.3 | LLM report routing | ✅ | #51 `432a0eb` + #52 `fef6ffb` | no | routes to Phase 28A final report |
| 28A.2 | Readable final report renderer | ✅ | #53 `00717c9` | no | frontend-only |
| 29A | Source registry + connector framework | ✅ | #49 `3ff96f6` | no | taxonomy T1–T6, /sources/registry |
| 29B | Filing/regulator connectors batch 1 | ✅ | #50 `a94e34b` | no | SEC + company_ir live, 6 scaffolds |
| 29B.1 | Non-US company IR evidence | ✅ | #54 `6046011` | no | verified issuer allowlist, safe fetcher |
| 29B.2 | Primary document text extraction | ✅ | #56 `793e0a7` | no | bounded annual-report PDF/HTML text extraction (no-OCR) + primary-fact parser + evidence budgeter, all off-by-default; staging-validated. Env note: every live issuer report reached is scanned/index-only → degrades to honest source-gaps, `primary_documents` present-but-empty (0 fabricated) |
| 29B.3 | Primary-fact integration | ✅ | #57 `29f4a84` | no | high-confidence T1 facts → `PrimaryFactRef` on `EvidenceItem` → council metadata (`source_summary_json.llm_council.primary_facts`, `to_report_dict` unchanged) → report `T1_primary_filing` datapoints + `extracted_primary_facts` + recomputed T1/T2 checklist + strict-schema completer (refuses non-USD revenue, no conversion; currency-guard hardened). Backend 2033 pass / 12 skip / 0 fail, security PASS. Staging VALIDATED-WITH-ENVIRONMENTAL-NOTE (API `29f4a84`, web unchanged `793e0a7`, no web change; CFR.SW final/8-agents/schema+safety valid, primary_facts=0 honest-empty, discovery regression no score inflation, AAPL/AMAT partial=Azure TPM environmental). Carry-forward: (1) facts-present happy path proven by unit fixtures only — staging reaches scanned/index-only PDFs (no-OCR) → 0 facts; (2) scoring/completeness credit capability-only, not wired to a production caller |
| 29B.4 | EU/UK regulated-disclosure connectors (umbrella) | ✅ COMPLETE | 4A #58 `5138725` · 4B #59 `1d97612` · 4C #60 `de126ee` | no | **COMPLETE** — all three subphases closed + staging-validated: 4A UK FCA NSM ✅ (#58 `5138725`), 4B Euronext ✅ (#59 `1d97612`), 4C Swiss/Nordic/Germany ✅ (#60 `de126ee`). Shipped pattern across all three: promote-scaffold→dedicated regulator connector emitting ONE bounded **T2 venue *reference*** + honest `primary_filing_unavailable` content gap, network-free at report time, verified-issuer-gated, with per-jurisdiction `requires_translation` semantics. Source registry now **11 enabled / 2 scaffolded** (only SEDAR+ + ASX regulator connectors remain scaffolded — deferred to a later batch). **Live regulator-venue CONTENT fetch deferred (reference-only) across all 29B.4 connectors** — future follow-up. Next: **Phase 29C** (macro/commodity/policy evidence connectors). |
| 29B.4A | UK FCA NSM/RNS regulated-disclosure connector | ✅ | #58 `5138725` | no | promotes `uk_fca_nsm` scaffold → dedicated `UkFcaNsmConnector`: a verified UK-regulated LSE issuer (BRBY.LSE / BA.LSE, never Boeing/SEC) → ONE bounded **T2 UK FCA NSM/RNS venue *reference*** (fixed public NSM URL, no fabricated filing/headline/date/RNS number) + honest `primary_filing_unavailable` content gap; **network-free at report time**. New `regulator_connector_for()` exchange→regulator map (LSE/GB → uk_fca_nsm) + tightened `_relevant_scaffold_ids` (UK → uk_fca_nsm only; DE/FR unchanged); `registry.py` moves uk_fca_nsm scaffold → enabled `regulator`/T2 (scaffold set now 5). **Live-content-fetch deferred** (NSM is a JS SPA) — future 29B.4 follow-up. Backend 2046 pass / 12 skip / 0 fail (+12), ruff clean, no new mypy, security PASS. Staging VALIDATED (clean): API `5138725` (3 stable polls), web unchanged `793e0a7` (backend-only), head `011`, registry shows uk_fca_nsm=enabled/T2 + scaffolded=5, BA.LSE→BAE T2 reference + honest gap (boeing/sec.gov/rns#=0, 8/8 agents, schema+safety valid, publication_ready false), non-UK unchanged, flags KEPT ON. Closure: `docs/development/closures/phase-29b4a.md` |
| 29B.4B | Euronext regulated-disclosure connector | ✅ | #59 `1d97612` | no | promotes `euronext_regulated_info` scaffold → dedicated `EuronextRegulatedConnector`: a verified Euronext issuer — Euronext Paris FR (MC.PA / RMS.PA / KER.PA) or Amsterdam NL (ASML.AS), never a fabricated filing/headline/date — → ONE bounded **T2 Euronext regulated-information venue *reference*** (+ AMF/AFM regulator; fixed public URL `https://www.euronext.com/en/regulated-information`, no fabricated filing/headline/date/notice number) + honest `primary_filing_unavailable` content gap; **network-free at report time**. French-jurisdiction issuers (MC/RMS/KER) carry a `requires_translation` (`GapType.translation_required`) marker — honest "not translated this phase", not an official-translation claim. `company_evidence.py` extends the exchange→regulator map (Euronext Paris PA/FR + Amsterdam AS/NL → euronext_regulated_info) + tightens `_relevant_scaffold_ids` (UK still uk_fca_nsm; DE unchanged); `registry.py` moves euronext_regulated_info scaffold → enabled `regulator`/T2 (registry now **8 enabled / 4 scaffolded**: SEDAR+/ASX/Deutsche Börse/Nordic); non-Euronext → honest `source_not_eligible` gap. **Live-content-fetch deferred** (carried from 29B.4A) — future 29B.4 follow-up. One adjacent 29B.1 honesty test (`test_26_27`) now allows the promoted regulator-reference source_ids. Backend 2058 pass / 12 skip / 0 fail (+12 `test_phase29b4b`), ruff clean, no new mypy (71 baseline), security PASS (pre-PR review APPROVED 10/10). **Staging VALIDATED (2026-07-27):** API `1d97612` (3 stable polls), web unchanged `793e0a7` (backend-only), head `011`; registry shows euronext_regulated_info=enabled/T2 + 8 enabled/4 scaffolded; MC.PA (LVMH FR) T2 reference + honest gap + `requires_translation`=true/French; ASML.AS (NL) T2 reference without translation; BA.LSE still uk_fca_nsm (4A unaffected, no Boeing); AAPL/AMAT + CFR.SW unchanged (honest not-eligible gap); schema/safety valid, publication_ready false, flags KEPT ON. Connector delta observed via authed evidence-preview on all 6 issuers; report-body/safety fields validated by untouched-code invariant (4A already proved a regulator reference flows into a full council FINAL report); fresh full-council runs skipped to avoid Azure OpenAI TPM burn. Closure: `docs/development/closures/phase-29b4b.md` |
| 29B.4C | Swiss / Nordic / Germany regulated-disclosure connectors | ✅ | #60 `de126ee` | no | Three new dedicated regulator connectors following the 29B.4A/4B pattern (ONE bounded **T2 venue *reference*** + honest `primary_filing_unavailable` content gap, network-free at report time, verified-issuer-gated): `DeutscheBoerseConnector` (`deutsche_boerse.py`, promotes the `deutsche_boerse` scaffold) — `SAP.DE` (Germany/Xetra) → T2 reference to the German regulated-info venue (Bundesanzeiger / Deutsche Börse / BaFin), fixed URL `https://www.bundesanzeiger.de`, + German `requires_translation` (`GapType.translation_required`); `NordicDisclosuresConnector` (`nordic_disclosures.py`, promotes the `nordic_disclosures` scaffold) — `PNDORA.CO` (Denmark/Nasdaq Copenhagen) → T2 reference (Nasdaq Nordic company news `https://www.nasdaqomxnordic.com/news/companynews` / Finanstilsynet) + Danish `requires_translation` (generalizes to ST/HE/OL); `SixSwissConnector` (`six_swiss.py`, **NEW `six_swiss` source — no Swiss scaffold existed**, resolves the honest gap the 4B plan flagged) — `CFR.SW` Richemont + `UHR.SW` Swatch on SIX (SW/VX) → T2 reference (SIX Swiss Exchange / SIX Exchange Regulation official notices) + honest content gap, **NO `requires_translation` claim** (Switzerland is multilingual and major issuers publish English — only a neutral DE/FR/IT multilingual note in warnings). `company_evidence.py` extends the exchange/country→regulator map (DE/Xetra/Frankfurt+Germany→`deutsche_boerse`; CO/Nasdaq-Copenhagen+Denmark→`nordic_disclosures`; SW/VX/SIX+Switzerland→`six_swiss`) + `REGULATOR_REFERENCE_IDS` + tightened `_relevant_scaffold_ids`; `registry.py` promotes `deutsche_boerse` + `nordic_disclosures` out of the scaffold table and adds `six_swiss` as a **new enabled `regulator`/T2 source** → registry now **11 enabled / 2 scaffolded** (remaining scaffolds: SEDAR+, ASX only). **Live-content-fetch deferred** (reference-only, carried from 29B.4A/4B). Backend **2071 pass / 12 skip / 0 fail** (+16 `test_phase29b4c`; adjacent scaffold-count tests updated, no ripple), ruff clean, mypy `71` baseline (no new), security PASS (pre-PR review APPROVED 10/10). **Staging VALIDATED (full, 2026-07-27):** merged `de126ee`, API `commit_sha=de126ee` (3 stable polls), web unchanged `793e0a7` (backend-only), head `011`, AUTH_TEST_MODE absent, logs current-build clean (sole `api_token=` is known 2026-07-22 historical). C: registry/health show all three enabled `regulator`/T2 + **11 enabled / 2 scaffolded**, honest content-not-fetched notes, `six_swiss` asserts NO translation, secret-free. D: `SAP.DE`→`deutsche_boerse` ref (`bundesanzeiger.de`) + German `requires_translation` + honest gap; `company_ir` present. E: `PNDORA.CO`→`nordic_disclosures` ref (`nasdaqomxnordic.com`) + Danish `requires_translation` + honest gap; `company_ir` present. F: `CFR.SW`+`UHR.SW`→`six_swiss` ref `requires_translation`=false (the `translation_required` gap present is the pre-existing `company_ir`'s, honest); `company_ir` present. G: `BA.LSE` still `uk_fca_nsm` (no DE/Nordic/Swiss item, BA→BAE never Boeing), `MC.PA` still `euronext` (no leakage), AAPL guardrail → three new connectors `source_not_eligible`/0 items. schema/safety valid, publication_ready false, human_review_required true, flags KEPT ON, publication admin-gated. **Completes the Phase 29B.4 umbrella.** Closure: `docs/development/closures/phase-29b4c.md` |
| 29C | Macro/commodity/policy evidence connectors (umbrella) | 🟡 | 29C.1 #61 `a8ac580` · 29C.2 @ `8c62297` (PR pending) | no | **IN PROGRESS — 29C.1 (macro baseline) CLOSED + deployed + staging-validated; 29C.2 (commodity + energy) 🟡 in progress / PR open @ `8c62297` (see rows below); 29C.3 (policy + government) remains.** Splits: **29C.1 macro baseline** (FRED/IMF/Eurostat/World Bank/national stats) ✅ #61 `a8ac580`, **29C.2 commodity+energy** (USGS/IEA/EIA/IRENA/ENTSO-E; World Bank Pink Sheet already enabled from 29C.1) 🟡 PR open @ `8c62297`, **29C.3 policy+government** (defense/NATO/tariffs/subsidies/industrial-policy/grid-investment/energy-transition — official/government sources USTR-TARIC/USAspending/EU TED/UN Comtrade — after). Evidence-first, **no recommendations/valuations**, prefer official/government sources, wire via source registry + `EvidenceItem` tiers + explicit `SourceGap`s (honest gaps, never fabricated), **no broad web search**, network-safe/allowlisted, OFF-by-default flags. |
| 29C.1 | Macro reference evidence connectors | ✅ | #61 `a8ac580` | no | **Reference-only + OFF-by-default** macro layer (first Phase 29C subphase). Generic `MacroReferenceConnector` (`connectors/macro_reference.py`) over 5 official sources (FRED/IMF/Eurostat/World Bank Pink Sheet/national stats+central banks): `fetch_macro_context` (was a dead hook) emits ONE bounded **T2 `macro_report` SOURCE REFERENCE** (fixed public token-free URL + which indicators covered) + honest `data_not_sourced` gap — **no figures/index levels/release dates/forecasts, network-free, no API key**. `collect_theme_macro_evidence` (`sources/macro_evidence.py`) dark when `source_macro_enabled` False, bounded by `source_macro_max_items` (3). Registry promotes the 5 macro sources PLANNED→enabled → **16 enabled / 2 scaffolded** (only SEDAR+/ASX remain; total 32). Discovery council cites macro refs as `R#` run facts (+gaps); company report gains OPTIONAL `industry_macro_context` block (beside `industry_context_events`; macro CONTEXT label, NOT company evidence, never a catalyst, no figures); `CouncilResult.macro_context` via `to_metadata_dict` (empty [] off). schema/safety valid, publication_ready false, human_review_required true; **dark-by-default byte-identical when off**. New flags `SOURCE_MACRO_ENABLED`(false)/`SOURCE_MACRO_MAX_ITEMS`(3). **Deferral:** live macro-FIGURE fetch deferred (reference-only) — keyless official-data-API fetch is a documented 29C follow-up. Backend **2092 pass / 12 skip / 0 fail** (+21 net `test_phase29c1`; adjacent count tests updated, no ripple), ruff clean, mypy `71` baseline no-new, security PASS (pre-PR review APPROVED 10/10). **Staging VALIDATED (2026-07-27):** merged `a8ac580`, API `commit_sha=a8ac580` (3 stable polls), web unchanged (backend-only), head `011`, AUTH_TEST_MODE absent, logs current-build clean. **OFF-state VALIDATED (clean):** registry 16 enabled/2 scaffolded, 5 macro sources enabled/T2/reference-only, fully dark at runtime, no regression. **ON-state (human-approved `az` flip `SOURCE_MACRO_ENABLED=true`) VALIDATED-WITH-ENVIRONMENTAL-NOTE:** SCCO (Southern Copper) report gains `industry_macro_context` block (`world_bank_pink_sheet`, T2, token-free URL, NO figures/dates, "not company-specific/not a catalyst" note), `llm_council.macro_context` non-empty, schema/safety valid, publication_ready false, human_review_required true; discovery "copper mining" cites macro refs (World Bank ×3, Pink Sheet ×2) + honest gaps, candidate returned, macro is CONTEXT not a candidate; safety clean (no fabricated figures, forbidden terms only in negated disclaimers); council discovery 8/8, company report 7/8 (1 failed = Azure gpt-4.1-mini TPM ENVIRONMENTAL not a code defect); publication admin-gated. Architecture: macro is **theme-scoped** (reports + discovery), NOT issuer-scoped (correctly absent from company evidence-preview). **`SOURCE_MACRO_ENABLED` KEPT ON** (validation clean, reference-only low-risk, matches keeping SOURCE_CONNECTOR/SOURCE_DOCUMENT_EXTRACTION on). Closure: `docs/development/closures/phase-29c1.md`. |
| 29C.2 | Commodity + energy reference connectors | 🟡 | branch `feature/phase-29c2-commodity-energy-connectors` @ `8c62297` (PR pending) | no | **IN PROGRESS / PR OPEN — NOT merged/deployed/validated.** Extends the 29C.1 reference-only macro layer to **commodity + energy** with **zero new wiring** (same generic `MacroReferenceConnector`, `collect_theme_macro_evidence` collector, discovery-council `R#` + report `industry_macro_context` paths, and the existing `SOURCE_MACRO_ENABLED` flag — **no new flag**). New `COMMODITY_ENERGY_SOURCES` table (+ combined `ALL_MACRO_SOURCES`): **USGS** (`usgs.gov`, T3; copper/lithium/rare-earths/critical minerals/cobalt/nickel/mining/uranium), **US EIA** (`eia.gov`, T2, no API key; uranium/nuclear/oil/gas/energy/electricity), **IEA** (`iea.org`, T3; energy/power-grid/nuclear/renewables/energy-transition), **IRENA** (`irena.org`, T3; renewables/solar/wind/hydrogen), **ENTSO-E** (`transparency.entsoe.eu`, T3; power-grid/electricity/grid/transmission). Each emits ONE bounded **T2/T3 `macro_report` SOURCE REFERENCE** (fixed official URL + which datasets covered) + honest `data_not_sourced` gap — **no tonnage/price/capacity/production/reserve figures or dates, network-free, no API key**. Registry promotes the 5 commodity/energy rows PLANNED→enabled → **21 enabled / 2 scaffolded / 9 planned** (only SEDAR+/ASX scaffolds; openbb + trade/procurement/patent rows stay planned; total 32). A copper company report surfaces up to 2 macro refs (World Bank Pink Sheet + USGS) when the flag is on; **dark-by-default byte-identical when off**. **Deferral (carried from 29C.1):** live commodity/energy FIGURE fetch deferred (reference-only) — keyless official-data-API fetch is a documented 29C follow-up. Backend **2119 pass / 12 skip / 0 fail** (+`test_phase29c2_commodity_energy_connectors.py`), ruff clean, mypy `71` baseline no-new, security PASS. **Not yet PR-reviewed/merged/staging-validated — do NOT mark ✅ until merge SHA + deployed SHA + validation on file.** Next: ib-pr-review-agent → open PR → merge → staging-validate; then 29C.3 policy + government. |
| 29D | Event-trigger / patents / local press | 🔜 | — | tbd | timely sourced event signals |
| 30 | Translation / local-language + PDF extraction | 🔜 | — | tbd | non-English primary sources usable |
| 31 | Source-aware research memo | 🔜 | — | tbd | memo cites source tier per claim |
| 32 | Durable queues / cost / observability | 🔜 | — | tbd | reliable async, cost ceilings, telemetry |
| tooling | Claude Code agents + phase workflow skills | ✅ | #55 `c98adca` | no | this dev-workflow system; no app runtime change; merged |

DB head baseline: **011** (unchanged since discovery/scoring migrations). Confirm
`alembic current` when validating any phase that claims a schema change.

## How to update
- Move a phase's status as it advances; fill PR/SHA/migration when known.
- Never set ✅ until a closure report with staging validation exists.
- Keep one row per phase; link the closure report if archived elsewhere.
