# InvestingBuddy — Phase Ledger

Durable status list for phased development. The orchestrator reads and updates
this file. `docs/ROADMAP.md` is authoritative for the product roadmap; this
ledger tracks execution state (branch → PR → merge → deploy → validated → closed).

Legend: ✅ closed+validated · 🔜 next · 🟡 in progress · ⛔ blocked

| Phase | Title | Status | PR / SHA | Migration | Notes |
|---|---|---|---|---|---|
| 28A | Single-company LLM council | ✅ | #44 `00a3b2f` | no | council off-by-default, 8 agents |
| 28B | Discovery-run LLM council | ✅ | #45/#46/#47 `350f062` | no | gated by 2 flags |
| 28B.2 | Async discovery council | ✅ | #48 `e3f1cab` | no | pollable run-level council |
| 28A.1 / 28B.3 | LLM report routing | ✅ | #51 `432a0eb` + #52 `fef6ffb` | no | routes to Phase 28A final report |
| 28A.2 | Readable final report renderer | ✅ | #53 `00717c9` | no | frontend-only |
| 29A | Source registry + connector framework | ✅ | #49 `3ff96f6` | no | taxonomy T1–T6, /sources/registry |
| 29B | Filing/regulator connectors batch 1 | ✅ | #50 `a94e34b` | no | SEC + company_ir live, 6 scaffolds |
| 29B.1 | Non-US company IR evidence | ✅ | #54 `6046011` | no | verified issuer allowlist, safe fetcher |
| 29B.2 | Primary document text extraction | ✅ | #56 `793e0a7` | no | bounded annual-report PDF/HTML text extraction (no-OCR) + primary-fact parser + evidence budgeter, all off-by-default; staging-validated. Env note: every live issuer report reached is scanned/index-only → degrades to honest source-gaps, `primary_documents` present-but-empty (0 fabricated) |
| 29B.3 | Primary-fact integration | ✅ | #57 `29f4a84` | no | high-confidence T1 facts → `PrimaryFactRef` on `EvidenceItem` → council metadata (`source_summary_json.llm_council.primary_facts`, `to_report_dict` unchanged) → report `T1_primary_filing` datapoints + `extracted_primary_facts` + recomputed T1/T2 checklist + strict-schema completer (refuses non-USD revenue, no conversion; currency-guard hardened). Backend 2033 pass / 12 skip / 0 fail, security PASS. Staging VALIDATED-WITH-ENVIRONMENTAL-NOTE (API `29f4a84`, web unchanged `793e0a7`, no web change; CFR.SW final/8-agents/schema+safety valid, primary_facts=0 honest-empty, discovery regression no score inflation, AAPL/AMAT partial=Azure TPM environmental). Carry-forward: (1) facts-present happy path proven by unit fixtures only — staging reaches scanned/index-only PDFs (no-OCR) → 0 facts; (2) scoring/completeness credit capability-only, not wired to a production caller |
| 29B.4 | EU/UK regulated-disclosure connectors (umbrella) | ✅ COMPLETE | 4A #58 `5138725` · 4B #59 `1d97612` · 4C #60 `de126ee` | no | **COMPLETE** — all three subphases closed + staging-validated: 4A UK FCA NSM ✅ (#58 `5138725`), 4B Euronext ✅ (#59 `1d97612`), 4C Swiss/Nordic/Germany ✅ (#60 `de126ee`). Shipped pattern across all three: promote-scaffold→dedicated regulator connector emitting ONE bounded **T2 venue *reference*** + honest `primary_filing_unavailable` content gap, network-free at report time, verified-issuer-gated, with per-jurisdiction `requires_translation` semantics. Source registry now **11 enabled / 2 scaffolded** (only SEDAR+ + ASX regulator connectors remain scaffolded — deferred to a later batch). **Live regulator-venue CONTENT fetch deferred (reference-only) across all 29B.4 connectors** — future follow-up. Next: **Phase 29C** (macro/commodity/policy evidence connectors). |
| 29B.4A | UK FCA NSM/RNS regulated-disclosure connector | ✅ | #58 `5138725` | no | promotes `uk_fca_nsm` scaffold → dedicated `UkFcaNsmConnector`: a verified UK-regulated LSE issuer (BRBY.LSE / BA.LSE, never Boeing/SEC) → ONE bounded **T2 UK FCA NSM/RNS venue *reference*** (fixed public NSM URL, no fabricated filing/headline/date/RNS number) + honest `primary_filing_unavailable` content gap; **network-free at report time**. New `regulator_connector_for()` exchange→regulator map (LSE/GB → uk_fca_nsm) + tightened `_relevant_scaffold_ids` (UK → uk_fca_nsm only; DE/FR unchanged); `registry.py` moves uk_fca_nsm scaffold → enabled `regulator`/T2 (scaffold set now 5). **Live-content-fetch deferred** (NSM is a JS SPA) — future 29B.4 follow-up. Backend 2046 pass / 12 skip / 0 fail (+12), ruff clean, no new mypy, security PASS. Staging VALIDATED (clean): API `5138725` (3 stable polls), web unchanged `793e0a7` (backend-only), head `011`, registry shows uk_fca_nsm=enabled/T2 + scaffolded=5, BA.LSE→BAE T2 reference + honest gap (boeing/sec.gov/rns#=0, 8/8 agents, schema+safety valid, publication_ready false), non-UK unchanged, flags KEPT ON. Closure: `docs/development/closures/phase-29b4a.md` |
| 29B.4B | Euronext regulated-disclosure connector | ✅ | #59 `1d97612` | no | promotes `euronext_regulated_info` scaffold → dedicated `EuronextRegulatedConnector`: a verified Euronext issuer — Euronext Paris FR (MC.PA / RMS.PA / KER.PA) or Amsterdam NL (ASML.AS), never a fabricated filing/headline/date — → ONE bounded **T2 Euronext regulated-information venue *reference*** (+ AMF/AFM regulator; fixed public URL `https://www.euronext.com/en/regulated-information`, no fabricated filing/headline/date/notice number) + honest `primary_filing_unavailable` content gap; **network-free at report time**. French-jurisdiction issuers (MC/RMS/KER) carry a `requires_translation` (`GapType.translation_required`) marker — honest "not translated this phase", not an official-translation claim. `company_evidence.py` extends the exchange→regulator map (Euronext Paris PA/FR + Amsterdam AS/NL → euronext_regulated_info) + tightens `_relevant_scaffold_ids` (UK still uk_fca_nsm; DE unchanged); `registry.py` moves euronext_regulated_info scaffold → enabled `regulator`/T2 (registry now **8 enabled / 4 scaffolded**: SEDAR+/ASX/Deutsche Börse/Nordic); non-Euronext → honest `source_not_eligible` gap. **Live-content-fetch deferred** (carried from 29B.4A) — future 29B.4 follow-up. One adjacent 29B.1 honesty test (`test_26_27`) now allows the promoted regulator-reference source_ids. Backend 2058 pass / 12 skip / 0 fail (+12 `test_phase29b4b`), ruff clean, no new mypy (71 baseline), security PASS (pre-PR review APPROVED 10/10). **Staging VALIDATED (2026-07-27):** API `1d97612` (3 stable polls), web unchanged `793e0a7` (backend-only), head `011`; registry shows euronext_regulated_info=enabled/T2 + 8 enabled/4 scaffolded; MC.PA (LVMH FR) T2 reference + honest gap + `requires_translation`=true/French; ASML.AS (NL) T2 reference without translation; BA.LSE still uk_fca_nsm (4A unaffected, no Boeing); AAPL/AMAT + CFR.SW unchanged (honest not-eligible gap); schema/safety valid, publication_ready false, flags KEPT ON. Connector delta observed via authed evidence-preview on all 6 issuers; report-body/safety fields validated by untouched-code invariant (4A already proved a regulator reference flows into a full council FINAL report); fresh full-council runs skipped to avoid Azure OpenAI TPM burn. Closure: `docs/development/closures/phase-29b4b.md` |
| 29B.4C | Swiss / Nordic / Germany regulated-disclosure connectors | ✅ | #60 `de126ee` | no | Three new dedicated regulator connectors following the 29B.4A/4B pattern (ONE bounded **T2 venue *reference*** + honest `primary_filing_unavailable` content gap, network-free at report time, verified-issuer-gated): `DeutscheBoerseConnector` (`deutsche_boerse.py`, promotes the `deutsche_boerse` scaffold) — `SAP.DE` (Germany/Xetra) → T2 reference to the German regulated-info venue (Bundesanzeiger / Deutsche Börse / BaFin), fixed URL `https://www.bundesanzeiger.de`, + German `requires_translation` (`GapType.translation_required`); `NordicDisclosuresConnector` (`nordic_disclosures.py`, promotes the `nordic_disclosures` scaffold) — `PNDORA.CO` (Denmark/Nasdaq Copenhagen) → T2 reference (Nasdaq Nordic company news `https://www.nasdaqomxnordic.com/news/companynews` / Finanstilsynet) + Danish `requires_translation` (generalizes to ST/HE/OL); `SixSwissConnector` (`six_swiss.py`, **NEW `six_swiss` source — no Swiss scaffold existed**, resolves the honest gap the 4B plan flagged) — `CFR.SW` Richemont + `UHR.SW` Swatch on SIX (SW/VX) → T2 reference (SIX Swiss Exchange / SIX Exchange Regulation official notices) + honest content gap, **NO `requires_translation` claim** (Switzerland is multilingual and major issuers publish English — only a neutral DE/FR/IT multilingual note in warnings). `company_evidence.py` extends the exchange/country→regulator map (DE/Xetra/Frankfurt+Germany→`deutsche_boerse`; CO/Nasdaq-Copenhagen+Denmark→`nordic_disclosures`; SW/VX/SIX+Switzerland→`six_swiss`) + `REGULATOR_REFERENCE_IDS` + tightened `_relevant_scaffold_ids`; `registry.py` promotes `deutsche_boerse` + `nordic_disclosures` out of the scaffold table and adds `six_swiss` as a **new enabled `regulator`/T2 source** → registry now **11 enabled / 2 scaffolded** (remaining scaffolds: SEDAR+, ASX only). **Live-content-fetch deferred** (reference-only, carried from 29B.4A/4B). Backend **2071 pass / 12 skip / 0 fail** (+16 `test_phase29b4c`; adjacent scaffold-count tests updated, no ripple), ruff clean, mypy `71` baseline (no new), security PASS (pre-PR review APPROVED 10/10). **Staging VALIDATED (full, 2026-07-27):** merged `de126ee`, API `commit_sha=de126ee` (3 stable polls), web unchanged `793e0a7` (backend-only), head `011`, AUTH_TEST_MODE absent, logs current-build clean (sole `api_token=` is known 2026-07-22 historical). C: registry/health show all three enabled `regulator`/T2 + **11 enabled / 2 scaffolded**, honest content-not-fetched notes, `six_swiss` asserts NO translation, secret-free. D: `SAP.DE`→`deutsche_boerse` ref (`bundesanzeiger.de`) + German `requires_translation` + honest gap; `company_ir` present. E: `PNDORA.CO`→`nordic_disclosures` ref (`nasdaqomxnordic.com`) + Danish `requires_translation` + honest gap; `company_ir` present. F: `CFR.SW`+`UHR.SW`→`six_swiss` ref `requires_translation`=false (the `translation_required` gap present is the pre-existing `company_ir`'s, honest); `company_ir` present. G: `BA.LSE` still `uk_fca_nsm` (no DE/Nordic/Swiss item, BA→BAE never Boeing), `MC.PA` still `euronext` (no leakage), AAPL guardrail → three new connectors `source_not_eligible`/0 items. schema/safety valid, publication_ready false, human_review_required true, flags KEPT ON, publication admin-gated. **Completes the Phase 29B.4 umbrella.** Closure: `docs/development/closures/phase-29b4c.md` |
| 29C | Macro/commodity/policy evidence connectors (umbrella) | ✅ COMPLETE | 29C.1 #61 `a8ac580` · 29C.2 #62 `80c8454` · 29C.3 #63 `ad6dde5` | no | **COMPLETE — all three subphases closed + deployed + staging-validated: 29C.1 (macro baseline) ✅ #61 `a8ac580`, 29C.2 (commodity + energy) ✅ #62 `80c8454`, 29C.3 (policy + government) ✅ #63 `ad6dde5`.** Shipped pattern across all three: a single generic `MacroReferenceConnector` over a single-source-of-truth `ALL_MACRO_SOURCES` table emits, per relevant theme, ONE bounded **T2/T3 `macro_report` SOURCE REFERENCE** (fixed official public URL + which datasets it covers) + an honest `data_not_sourced` gap — **reference-only, network-free, no API key, no figures/index-levels/tariff-rates/subsidy/arms/production/reserve/release-date ever emitted**; reuses one OFF-by-default flag `SOURCE_MACRO_ENABLED` (+ `SOURCE_MACRO_MAX_ITEMS`), the `collect_theme_macro_evidence` collector, the discovery-council `R#` citation path and the report `industry_macro_context` block; macro is theme-scoped CONTEXT (never company-specific, never a catalyst, never a recommendation); dark-by-default byte-identical when off. Splits: **29C.1 macro baseline** (FRED/IMF/Eurostat/World Bank Pink Sheet/national stats), **29C.2 commodity+energy** (USGS/EIA/IEA/IRENA/ENTSO-E), **29C.3 policy+government** (USTR-TARIC/UN Comtrade promoted + NATO/SIPRI/OECD new). Source registry now **26 enabled / 2 scaffolded / 7 planned / 35 total** (only SEDAR+/ASX regulator scaffolds remain; USAspending/EU TED/OpenBB + patent rows stay planned → 29D). **Live macro/commodity/policy FIGURE fetch deferred (reference-only) across the whole umbrella** — a keyless official-data-API fetch is a documented follow-up. **Carry-forward (known limitation, not a defect):** coarse company/discovery theme→source keyword mapping means not every specialist source surfaces for every theme (the citation mechanism + registry are proven — refine the mapping in a follow-up). Next: **Phase 29D** (event-trigger connectors — procurement/tenders/patents/permits). |
| 29C.1 | Macro reference evidence connectors | ✅ | #61 `a8ac580` | no | **Reference-only + OFF-by-default** macro layer (first Phase 29C subphase). Generic `MacroReferenceConnector` (`connectors/macro_reference.py`) over 5 official sources (FRED/IMF/Eurostat/World Bank Pink Sheet/national stats+central banks): `fetch_macro_context` (was a dead hook) emits ONE bounded **T2 `macro_report` SOURCE REFERENCE** (fixed public token-free URL + which indicators covered) + honest `data_not_sourced` gap — **no figures/index levels/release dates/forecasts, network-free, no API key**. `collect_theme_macro_evidence` (`sources/macro_evidence.py`) dark when `source_macro_enabled` False, bounded by `source_macro_max_items` (3). Registry promotes the 5 macro sources PLANNED→enabled → **16 enabled / 2 scaffolded** (only SEDAR+/ASX remain; total 32). Discovery council cites macro refs as `R#` run facts (+gaps); company report gains OPTIONAL `industry_macro_context` block (beside `industry_context_events`; macro CONTEXT label, NOT company evidence, never a catalyst, no figures); `CouncilResult.macro_context` via `to_metadata_dict` (empty [] off). schema/safety valid, publication_ready false, human_review_required true; **dark-by-default byte-identical when off**. New flags `SOURCE_MACRO_ENABLED`(false)/`SOURCE_MACRO_MAX_ITEMS`(3). **Deferral:** live macro-FIGURE fetch deferred (reference-only) — keyless official-data-API fetch is a documented 29C follow-up. Backend **2092 pass / 12 skip / 0 fail** (+21 net `test_phase29c1`; adjacent count tests updated, no ripple), ruff clean, mypy `71` baseline no-new, security PASS (pre-PR review APPROVED 10/10). **Staging VALIDATED (2026-07-27):** merged `a8ac580`, API `commit_sha=a8ac580` (3 stable polls), web unchanged (backend-only), head `011`, AUTH_TEST_MODE absent, logs current-build clean. **OFF-state VALIDATED (clean):** registry 16 enabled/2 scaffolded, 5 macro sources enabled/T2/reference-only, fully dark at runtime, no regression. **ON-state (human-approved `az` flip `SOURCE_MACRO_ENABLED=true`) VALIDATED-WITH-ENVIRONMENTAL-NOTE:** SCCO (Southern Copper) report gains `industry_macro_context` block (`world_bank_pink_sheet`, T2, token-free URL, NO figures/dates, "not company-specific/not a catalyst" note), `llm_council.macro_context` non-empty, schema/safety valid, publication_ready false, human_review_required true; discovery "copper mining" cites macro refs (World Bank ×3, Pink Sheet ×2) + honest gaps, candidate returned, macro is CONTEXT not a candidate; safety clean (no fabricated figures, forbidden terms only in negated disclaimers); council discovery 8/8, company report 7/8 (1 failed = Azure gpt-4.1-mini TPM ENVIRONMENTAL not a code defect); publication admin-gated. Architecture: macro is **theme-scoped** (reports + discovery), NOT issuer-scoped (correctly absent from company evidence-preview). **`SOURCE_MACRO_ENABLED` KEPT ON** (validation clean, reference-only low-risk, matches keeping SOURCE_CONNECTOR/SOURCE_DOCUMENT_EXTRACTION on). Closure: `docs/development/closures/phase-29c1.md`. |
| 29C.2 | Commodity + energy reference connectors | ✅ | #62 `80c8454` | no | **CLOSED + validated (VALIDATED-WITH-ENVIRONMENTAL-NOTE).** Extends the 29C.1 reference-only macro layer to **commodity + energy** with **zero new wiring** (same generic `MacroReferenceConnector`, `collect_theme_macro_evidence` collector, discovery-council `R#` + report `industry_macro_context` paths, and the existing `SOURCE_MACRO_ENABLED` flag — **no new flag/host/endpoint/migration**). New `COMMODITY_ENERGY_SOURCES` table (+ combined `ALL_MACRO_SOURCES`): **USGS** (`usgs.gov`, T3; copper/lithium/rare-earths/critical minerals/cobalt/nickel/mining/uranium), **US EIA** (`eia.gov`, T2, no API key; uranium/nuclear/oil/gas/energy/electricity), **IEA** (`iea.org`, T3; energy/power-grid/nuclear/renewables/energy-transition), **IRENA** (`irena.org`, T3; renewables/solar/wind/hydrogen), **ENTSO-E** (`transparency.entsoe.eu`, T3; power-grid/electricity/grid/transmission) — all `commodity` provider type. Each emits ONE bounded **T2/T3 `macro_report` SOURCE REFERENCE** (fixed official URL + which datasets covered) + honest `data_not_sourced` gap — **no tonnage/price/capacity/production/reserve figures or dates, network-free, no API key**. Registry promotes the 5 commodity/energy rows PLANNED→enabled → **21 enabled / 2 scaffolded / 9 planned** (only SEDAR+/ASX scaffolds; openbb + trade/procurement/patent rows stay planned; total 32). **Deferral (carried from 29C.1):** live commodity/energy FIGURE fetch deferred (reference-only) — keyless official-data-API fetch is a documented 29C follow-up. Backend **2119 pass / 12 skip / 0 fail** (+27 net `test_phase29c2_commodity_energy_connectors.py`; adjacent count tests updated, no ripple), ruff clean, mypy `71` baseline no-new, security PASS (pre-PR review APPROVED 10/10). **Staging VALIDATED (2026-07-27) — VALIDATED-WITH-ENVIRONMENTAL-NOTE:** merged `80c8454`, API `commit_sha=80c8454` (3 stable polls), web unchanged (backend-only), head `011`, AUTH_TEST_MODE absent, logs current-build clean; **no app-setting flip needed** (`SOURCE_MACRO_ENABLED` already ON from 29C.1). B: registry/health show usgs/iea/irena/entsoe (commodity, T3) + eia (commodity, T2) all enabled, **21 enabled/2 scaffolded/9 planned**, honest "reference only, no figures, no API key" notes, secret-free. C: uranium discovery run cites US EIA + IEA as macro `R#` references + honest gaps, 0 figure tokens, candidates returned (CCJ/UEC/UUUU/FCX), macro is CONTEXT not a candidate, discovery council 8/8 — **proves the new specialist sources surface**. D (PARTIAL, environmental): 2 company reports (Cameco, UEC) render `industry_macro_context` reference-only (no figures), `macro_context` non-empty, schema/safety valid, publication_ready false, company_ir/SEC present — but the NEW specialist sources did NOT surface for these two names (see carry-forward). E: no fabricated figures, forbidden terms only in negated disclaimers. F: company council 7/8 (1 agent Azure TPM ENVIRONMENTAL). G: admin-gated, 5 flags ON. **⚠️ Carry-forward (KNOWN LIMITATION, NOT a defect):** the **company-report** macro theme is derived from the company's **sector/industry**, which is **coarse for commodity/energy names** (`free_real` often returns `sector="Materials"`/no-industry), so specialist commodity/energy sources (USGS/EIA/IEA) may **UNDER-SURFACE in company reports** until the company→theme derivation is improved (e.g. map `sector+ticker/name` → the right specialist). The sources work correctly at the discovery-council + registry level (proven B+C, render unit-test-covered). Flags KEPT ON (all 5). Closure: `docs/development/closures/phase-29c2.md`. |
| 29C.3 | Policy + government reference connectors | ✅ | #63 `ad6dde5` | no | **CLOSED + validated (VALIDATED-WITH-ENVIRONMENTAL-NOTE) — completes the Phase 29C umbrella.** The **third and LAST** Phase 29C subphase — extends the reference-only macro layer with **policy + government** context (**zero new wiring**). New `POLICY_GOVERNMENT_SOURCES` table (+ combined `ALL_MACRO_SOURCES`) served by the SAME generic `MacroReferenceConnector`: **USTR / EU TARIC** (`ustr_taric`, `trade_policy`, T2; tariffs/trade/customs) + **UN Comtrade** (`un_comtrade`, `trade_policy`, T2; tariffs/trade/customs) **promoted PLANNED→enabled**; plus new **NATO defence expenditure** (`nato`, `trade_policy`, T2, `nato.int`; defense/military-spending/procurement/arms), **SIPRI military expenditure** (`sipri`, `trade_policy`, T3, `sipri.org`; defense/military-spending/procurement/arms) and **OECD** (`oecd`, `macro_statistics`, T2, `oecd.org`; subsidies/industrial-policy/state-aid/energy-transition/grid-investment). Each emits ONE bounded **T2/T3 `macro_report` SOURCE REFERENCE** (fixed official URL + which datasets covered) + honest `data_not_sourced` gap — **no budget/spending-%/tariff-rate/subsidy figures or dates, network-free, no API key**. Reuses the SAME collector (`collect_theme_macro_evidence` iterates `ALL_MACRO_SOURCES`), discovery-council `R#` path, report `industry_macro_context` block, existing `SOURCE_MACRO_ENABLED` flag — **no new flag/host/endpoint/migration**. *Note: `ProviderType` has no `government_data` member → government sources reuse `trade_policy`/`macro_statistics`.* Registry promotes `ustr_taric` + `un_comtrade` and adds `nato`/`sipri`/`oecd` → **26 enabled / 2 scaffolded / 7 planned** (only SEDAR+/ASX scaffolds; USAspending/EU TED/OpenBB + patent rows stay planned → 29D; total **35**). **Deferral (carried from 29C.1/29C.2):** live policy/government FIGURE fetch deferred (reference-only). Backend **2150 pass / 12 skip / 0 fail** (new `test_phase29c3`; two stale `enabled==21` count tests fixed to `26`, no functional ripple), ruff clean, mypy `71` baseline no-new, security PASS (pre-PR review APPROVED 10/10). **Staging VALIDATED (2026-07-27) — VALIDATED-WITH-ENVIRONMENTAL-NOTE:** merged `ad6dde5`, API `commit_sha=ad6dde5` (3 stable polls), web unchanged (backend-only), head `011`, AUTH_TEST_MODE absent, logs current-build clean; **no app-setting flip** (`SOURCE_MACRO_ENABLED` already ON from 29C.1). B: registry/health show `ustr_taric`/`un_comtrade`/`nato`/`oecd` (T2) + `sipri` (T3) all enabled (`trade_policy`/`macro_statistics`), **26 enabled / 2 scaffolded / 7 planned / 35 total**, honest "policy/government reference only; live figures not fetched" notes, secret-free. C: a defense-themed discovery run (candidates Thales/BAE/Rheinmetall) cites **NATO (×13) + USTR-TARIC (×3)** as macro `R#` run facts + honest gaps, macro is CONTEXT not a candidate; **SIPRI/OECD/UN-Comtrade did NOT surface for this specific theme** (coarse theme→source keyword map — same carry-forward as 29C.2; citation mechanism proven). D: `safety_valid` true, no fabricated figures, no recommendation language. E: discovery council 6/8 agents (2 failed = Azure gpt-4.1-mini TPM ENVIRONMENTAL). F: logs clean, AUTH_TEST_MODE absent, admin-gated, 5 flags ON. G: company `industry_macro_context` render inferred from 31 unit tests + reference-only behaviour (full-council run skipped to avoid TPM burn on unchanged render). **⚠️ Carry-forward (KNOWN LIMITATION, NOT a defect):** coarse company/discovery theme→source keyword mapping means not every specialist source surfaces for every theme (NATO/USTR-TARIC surfaced, SIPRI/OECD/UN-Comtrade did not for the defense theme); the citation mechanism + registry are proven — refine the theme→source mapping in a follow-up. Flags KEPT ON (all 5). Closure: `docs/development/closures/phase-29c3.md`. |
| 29D | Event-trigger connectors (procurement / patents / permits) | ✅ COMPLETE | 29D.1 #64 `a671e97` · 29D.2 #65 `1c6b1c9` · 29D.3 #66 `d567019` | no | **COMPLETE (umbrella)** — all three subphases closed + deployed + staging-validated. Timely, sourced event signals — split **29D.1 procurement / tenders** (EU TED / USAspending — promoted the `eu_ted` / `usaspending` planned registry rows) **✅ CLOSED (#64 `a671e97`, staging-validated, `SOURCE_EVENT_ENABLED` kept ON)**, **29D.2 patents** (promoted the `google_patents` / `uspto` / `epo_espacenet` planned rows — reference-only, **no legal / infringement conclusions**) **✅ CLOSED (#65 `1c6b1c9`, staging-validated VALIDATED-WITH-ENVIRONMENTAL-NOTE)**, **29D.3 permits / regulatory-event venues** — **the LAST 29D subphase** — **✅ CLOSED (#66 `d567019`, staging-validated VALIDATED-WITH-ENVIRONMENTAL-NOTE)**: NEW `ferc` / `us_nrc` / `us_epa` `permits` (a NEW `ProviderType.permits`) T2 reference-only sources (energy / grid / nuclear / environmental permits where safe; metadata-first; honest gaps; **no fake permits**; **also folds in** the `_event_discovery_facts` per-provider label tidy — procurement / patents / permits — from 29D.2). Metadata-first, honest gaps, **no fake awards / contracts / permits, no broad crawling**; an event is an **internal research-priority evidence signal only, never a recommendation**. Same evidence-first / source-registry / `EvidenceItem`-tier / explicit-`SourceGap` discipline, OFF-by-default flags. Registry now **34 enabled / 2 scaffolded (SEDAR+/ASX) / 2 planned (`openbb` + local-language press) / 38 total**; **live event FETCH deferred (reference-only) across the whole umbrella**. **🎯 ALL OF PHASE 29 is now COMPLETE:** 29A source-registry + connector framework · 29B filings/regulator + primary docs/facts (29B / 29B.1 / 29B.2 / 29B.3) · 29B.4 EU/UK regulated disclosures (4A/4B/4C) · 29C macro/commodity/policy reference connectors (29C.1/29C.2/29C.3) · 29D event-trigger connectors (29D.1/29D.2/29D.3). **Next: Phase 30A (language detection + translation foundation).** |
| 29D.1 | Procurement / tender event-trigger reference connectors | ✅ | #64 `a671e97` | no | **CLOSED + validated (OFF-state VALIDATED + ON-state VALIDATED-WITH-ENVIRONMENTAL-NOTE).** First Phase 29D subphase — a NEW reference-only event-trigger evidence category (parallel to the 29C macro layer), OFF by default. New `EventReferenceConnector` (`sources/connectors/event_reference.py`) over 2 procurement/tender venues — **EU TED** (`ted.europa.eu`) + **USAspending.gov** (`usaspending.gov`); the previously theme-dead `fetch_events` hook emits, per relevant theme, ONE bounded **T2 SOURCE REFERENCE** (`source_type="government_data"` — deliberately NOT "government_contract"; `ProviderType` `procurement`; fixed official public URL, no API key, **NO specific tender/award/contractor/amount/contract-number/date**) + honest `data_not_sourced` gap ("live tenders/awards not fetched at report time"). Each item is a **WEAK** internal research-priority signal + `needs_human_review`, records freshness via `stale_after_days`, and is **NOT a materiality claim / candidate / catalyst / trade signal**; network-free. `collect_theme_event_evidence(theme, region, cfg)` (`sources/event_evidence.py`) is theme-keyed, DARK when `source_event_enabled` False, bounded by `source_event_max_items`. Registry: `eu_ted` + `usaspending` promoted PLANNED→**enabled** → **28 enabled / 2 scaffolded (SEDAR+/ASX) / 5 planned (google_patents/uspto/epo_espacenet + openbb + local-language press → 29D.2/later) / 35 total**. Discovery council threads event refs as citeable `R#` run facts (in `evidence_ids()`) + honest gaps; company report gains an OPTIONAL `industry_event_context` block (WEAK event CONTEXT — not company-specific, not a catalyst/materiality/trade signal, no figures), rendered only when `source_event_enabled` + a reference exists; `CouncilResult.event_context` via `to_metadata_dict` (empty `[]` off). New OFF-by-default flags `SOURCE_EVENT_ENABLED`(false)/`SOURCE_EVENT_MAX_ITEMS`(3), **INDEPENDENT of `SOURCE_MACRO_ENABLED`**; dark-by-default byte-identical when off. **Deliberate deferral:** live EU TED / USAspending tender/award FETCH deferred (reference-only), mirroring the 29B.4/29C deferrals; full candidate-generation-from-events deferred (AC1 satisfied at context level — candidates come from the curated registry). Backend **2184 pass / 12 skip / 0 fail** (+34; adjacent registry count assertions updated to 28 enabled/5 planned, no ripple), ruff clean, mypy `71` baseline no-new, security PASS (pre-PR review APPROVED 10/10). **Staging VALIDATED (2026-07-28):** merged `a671e97`, API `commit_sha=a671e97` (3 stable polls), web unchanged (backend-only), head `011`, AUTH_TEST_MODE absent, logs current-build clean, publication admin-gated. **OFF-state VALIDATED:** registry **28 enabled / 2 scaffolded / 5 planned / 35 total**, `eu_ted` + `usaspending` enabled `procurement`/T2, fully dark at runtime, **macro independence confirmed**. **ON-state (human-approved `az` flip `SOURCE_EVENT_ENABLED=true`) VALIDATED-WITH-ENVIRONMENTAL-NOTE:** a "defense procurement Europe" discovery run (candidates Thales/BAE/Saab/Leonardo/Rheinmetall) cites **USAspending (×3) + "EU TED and USAspending.gov"** as reference-only `R#` run facts + honest gap ("live tenders/awards not fetched; only references to official datasets"); events are **WEAK CONTEXT, NOT candidates**; safety valid — proves ON-state citeability. Company report valid (8/8 agents, schema/safety valid, publication_ready false, human_review_required true) **but the `industry_event_context` + `industry_macro_context` blocks did NOT render for the tested thin European defense names** (their `free_real` profiles had no sector/industry → empty theme → collectors correctly dark; context keys present-but-empty) — **data-thinness environmental condition, NOT a code defect** (render unit-test-covered; ON-state citeability proven by the discovery run). Councils 8/8 (no TPM failure this run). **⚠️ Carry-forward (KNOWN LIMITATION, NOT a defect):** the company-report event/macro context blocks depend on a non-thin company sector/industry classification (`free_real`) to derive a theme; thin/mock profiles → collectors stay dark → blocks don't render (same coarse/thin company→theme carry-forward first noted in 29C.2) — refine the company→theme derivation in a follow-up. **DECISION: `SOURCE_EVENT_ENABLED` KEPT ON** (validation clean, reference-only low-risk, matches keeping the other source flags on) → **6 flags ON now**. Closure: `docs/development/closures/phase-29d1.md`. |
| 29D.2 | Patent event-trigger reference connectors | ✅ | #65 `1c6b1c9` | no | **CLOSED + validated (VALIDATED-WITH-ENVIRONMENTAL-NOTE).** Second Phase 29D subphase — extends the 29D.1 reference-only event-trigger layer to **patents** with **zero new wiring**. New `PATENT_SOURCES` (into the combined `ALL_EVENT_SOURCES`) served by the SAME generic `EventReferenceConnector`: **Google Patents** (`patents.google.com`, `T5` aggregator index, provider `patents`), **USPTO** (`uspto.gov`, `T2`, provider `patents`) + **EPO Espacenet** (`worldwide.espacenet.com`, `T2`, provider `patents`), all promoted PLANNED→**enabled**. A per-kind `_EventFlavor` makes patent refs **purely thematic** (innovation/R&D/patent/IP/technology/semiconductor/pharma/battery/EV/materials), `source_type="government_data"`. Each emits ONE bounded **T2/T5 SOURCE REFERENCE** (fixed official public URL, no API key, **NO specific patent number/title/inventor/assignee/claim/date**) + honest `data_not_sourced` gap + an explicit disclaimer that **NO legal/infringement/validity/patentability/ownership/competitive-strength conclusion is drawn** — patents are **WEAK internal research-priority CONTEXT only**, `needs_human_review`, `stale_after_days` freshness; network-free. Reuses the SAME collector (`collect_theme_event_evidence` iterates `ALL_EVENT_SOURCES`), the discovery-council `R#` path, the `industry_event_context` block and the existing `SOURCE_EVENT_ENABLED` flag — **no new flag/host/endpoint/migration**. Registry → **31 enabled / 2 scaffolded / 2 planned / 35 total** (only `openbb` + local-language business press remain planned; SEDAR+/ASX scaffolds). **Deliberate deferral (carried from 29D.1):** live patent-filing FETCH deferred (reference-only). Backend **2210 pass / 12 skip / 0 fail** (+26; adjacent registry count assertions updated to 31 enabled/2 planned, no ripple), ruff clean, mypy `71` baseline no-new, security PASS (pre-PR review APPROVED 10/10). **Staging VALIDATED (2026-07-28) — VALIDATED-WITH-ENVIRONMENTAL-NOTE:** merged `1c6b1c9`, API `commit_sha=1c6b1c9` (3 stable polls), web unchanged (backend-only), head `011`, AUTH_TEST_MODE absent, publication admin-gated; **no app-setting flip** (`SOURCE_EVENT_ENABLED` already ON from 29D.1). B: registry/health show `google_patents` (patents, T5), `uspto` (patents, T2), `epo_espacenet` (patents, T2) all enabled, **31 enabled / 2 scaffolded / 2 planned / 35 total**, honest "patent venue reference; live filings not fetched; no legal/infringement conclusions; WEAK" notes, secret-free. C: a "semiconductor innovation" discovery run (3 real semis candidates incl. AMAT) cites **Google Patents / USPTO / Espacenet (×11 each)** as reference-only `R#` run facts + honest "not fetched" gaps; patents are CONTEXT, NOT a candidate — proves ON-state citeability. D: no fabricated patent numbers, no legal/infringement/validity conclusions, forbidden terms only negated; `safety_valid` true. F: council `llm_used`, `completed_with_warnings` (Azure gpt-4.1-mini TPM ENVIRONMENTAL). G: 6 flags ON, AUTH_TEST_MODE absent, admin-gated. **⚠️ az-log-timeout note (operational, NOT a defect):** staging log secret-scan UNKNOWN — the `az` log download **timed out >90s**; mitigated because (1) the API response surface scanned clean and (2) **29D.2 adds no logging** (reference-only, keyless) — re-run opportunistically. **⚠️ KNOWN FOLLOW-UP (non-blocking tidy, NOT a defect):** at `1c6b1c9`, `_event_discovery_facts` (`apps/api/app/services/llm/discovery_council.py`) labels **patent** event references with generic **"procurement/tender venue reference (T2)"** boilerplate — a **cosmetic mislabel** in the discovery-pack narration. NOT a safety issue (no fabrication, no legal conclusion; patent-specific honesty is carried by the gap messages + registry `reliability_note`). **Fix:** make `_event_discovery_facts` label per `provider_type` (procurement vs patents) — **fold into Phase 29D.3.** **DECISION: `SOURCE_EVENT_ENABLED` KEPT ON** (already ON from 29D.1; validation clean, reference-only low-risk) → **6 flags ON**. Closure: `docs/development/closures/phase-29d2.md`. 29D.3 (permits/regulatory-event metadata) is the LAST 29D subphase. |
| 29D.3 | Permit / regulatory-event reference connectors | ✅ | #66 `d567019` | no | **CLOSED + validated (VALIDATED-WITH-ENVIRONMENTAL-NOTE) — the LAST 29D subphase; completes the Phase 29D umbrella AND all of Phase 29.** PR #66 → `main` `d567019`, API `commit_sha=d567019` (3 stable polls), web unchanged (backend-only), head `011`. The **third and LAST** Phase 29D subphase — completes the reference-only event-trigger layer by extending it to **permits / regulatory-event venues** with **zero new wiring** (SAME generic `EventReferenceConnector`, `collect_theme_event_evidence` collector iterating `ALL_EVENT_SOURCES`, discovery-council `R#` + report `industry_event_context` paths, and the existing OFF-by-default `SOURCE_EVENT_ENABLED` flag — **no new flag/host/endpoint/migration**). New `PERMIT_SOURCES` (into the combined `ALL_EVENT_SOURCES` = procurement + patents + permits), all provider **`permits`** (a NEW `ProviderType.permits`), **T2**, `source_type="government_data"`: **FERC** (`ferc.gov`; energy/grid/transmission/pipeline/LNG dockets & permits), **US NRC** (`nrc.gov`; nuclear reactor licensing/permits), **US EPA** (`epa.gov`; environmental/emissions/industrial permitting) — added as **NEW enabled** (not promoted from planned). A per-kind `_PERMIT_FLAVOR` makes permit refs **purely thematic** (energy/grid/transmission/pipeline/nuclear/environmental/emissions/mining/lng/power-plant/permit/licensing — **bare "industrial" deliberately EXCLUDED** to avoid a GICS-Industrials substring collision with the 29D.1 defense path). Each emits ONE bounded **T2 SOURCE REFERENCE** (fixed official public URL, no API key, **NO specific docket/case/permit number/applicant/decision/outcome/date**) + honest `data_not_sourced` gap + an explicit disclaimer that **NO regulatory-outcome/approval/denial/materiality conclusion is drawn** — permits are **WEAK internal research-priority CONTEXT only**, `needs_human_review`, `stale_after_days` freshness; network-free. **Folds in the 29D.2 follow-up:** `_event_discovery_facts` (`discovery_council.py`) now labels each run-fact **per `provider_type`** (procurement byte-identical; patents → "patent office / index venue reference"; permits → "permit / regulatory-event venue reference"). Registry → **34 enabled / 2 scaffolded / 2 planned / 38 total** (only `openbb` + local-language business press remain planned; SEDAR+/ASX scaffolds). **Deliberate deferral (carried from 29D.1/29D.2):** live permit/docket FETCH deferred (reference-only) — keyed FERC eLibrary / EPA / NRC ADAMS APIs NOT used. Backend **2238 pass / 12 skip / 0 fail** (+28 net; adjacent count tests updated to 34/38, no ripple), ruff clean, mypy `71` baseline no-new, security PASS (pre-PR review APPROVED 10/10). **Staging VALIDATED (2026-07-28) — VALIDATED-WITH-ENVIRONMENTAL-NOTE:** no flip (`SOURCE_EVENT_ENABLED` already ON), AUTH_TEST_MODE absent, admin-gated. B: registry/health show `ferc`+`us_nrc`+`us_epa` enabled/`permits`/T2, **34 enabled / 2 scaffolded / 2 planned / 38 total**, honest "permit/regulatory-event venue reference; live dockets not fetched; no regulatory-outcome conclusions; weak" notes, secret-free. C: a "US nuclear power + grid" discovery run (uranium candidates UEC/UUUU) cites the permit sources as reference-only weak run-fact gaps ("no live FERC permit data included"); permits are CONTEXT, NOT a candidate. D: `safety_valid` true, no fabricated dockets/outcomes, no regulatory-outcome conclusion, no recommendation; publication_ready false, human_review_required true. E: per-provider label fix confirmed (permits labelled "permit/regulatory-event venue reference", not procurement/tender — the 29D.2 patent-label tidy is done). F: council 7/8 (1 agent Azure gpt-4.1-mini TPM ENVIRONMENTAL). G: logs clean (archive through 07-27; today's build runtime not yet archived → API response surface scanned clean; 29D.3 adds no logging), 6 flags ON. **⚠️ Minor note (item G):** current-build live log tail is a follow-up (log archive lagged the 07-28 build) — mitigated by clean API surface + reference-only (no logging added). **Decision:** `SOURCE_EVENT_ENABLED` KEPT ON → 6 flags ON. Closure: `docs/development/closures/phase-29d3.md`. |
| 30 | Translation / local-language (umbrella) | ✅ COMPLETE | 30A #67 `fa3632a` · 30B #68 `e1d2d8d` | no | **COMPLETE (umbrella)** — both subphases closed + deployed + staging-validated: **30A** (language detection + translation foundation) ✅ #67 `fa3632a` (OFF-state validated, `SOURCE_TRANSLATION_ENABLED` KEPT OFF — see the 30A row); **30B** (local-language business-press evidence sources — allowlisted connectors that produce the non-English excerpts the 30A layer translates) ✅ #68 `e1d2d8d` (VALIDATED clean — see the 30B row). Together they make non-English primary sources usable: 30B emits FR/DE/IT/DA local-language SOURCE REFERENCES (Les Échos / Handelsblatt / Milano Finanza / Børsen) carrying genuine non-English excerpts marked `requires_translation`, and 30A translates them (bounded, machine-assisted / NOT-official / human-review-required) when `SOURCE_TRANSLATION_ENABLED` is flipped on. **On staging the local-language references are already demonstrable with translation OFF** (evidence-preview shows the non-English excerpt + translation state); `SOURCE_TRANSLATION_ENABLED` KEPT OFF (conservative). Registry now **35 enabled / 2 scaffolded / 1 planned / 38 total**. **Live local-language CONTENT fetch deferred (reference-only); PDF table extraction + OCR deferred to later-area work — NOT blocking Phase 31.** **Next: Phase 31 (source-aware internal research memo builder — the FINAL phase).** |
| 30A | Language detection + translation foundation | ✅ | #67 `fa3632a` | no | **CLOSED + validated (OFF-state VALIDATED).** PR #67 → `main` `fa3632a`, API `commit_sha=fa3632a` (3 stable polls), web unchanged (backend-only), head `011`. Foundation-only, backend-only, **OFF by default**, no new host/endpoint/secret. New `sources/language.py` (`detect_language(text, hint)` for en/fr/de/it/da — generalizes the extractor stopword heuristic, adds Danish/Nordic; `LANGUAGE_NAMES`/`language_name()`; `document_text_extractor._detect_language` now delegates, behaviour preserved) + `sources/translation.py` (`TranslationResult` + `TranslationProvider` ABC + **`FakeTranslationProvider`** — the DEFAULT honest placeholder, never fabricates fluent English + **`LLMTranslationProvider`** composing the existing `get_llm_client()` — bounded input+output, **text-free logging** (ONLY counts + lang codes, never prompt/original/translated text — regression-tested); `get_translation_provider(cfg, *, client)` factory). Council `_collect_translated_excerpts` (gated by `source_translation_enabled`) → bounded machine-assisted translations on `CouncilResult.translated_excerpts` (**metadata-only** via `to_metadata_dict` → `source_summary_json.llm_council.translated_excerpts`, empty `[]` off — mirrors `macro_context`/`event_context`/`primary_documents`), each keeping the original + secret-stripped `source_url` as the **citation of record**. Report gains optional `report_content["translated_evidence"]` block (safety-scanned before validation, rendered only when present). Every result carries a `MACHINE_TRANSLATION_WARNING` (machine-assisted, NOT official, human-review-required) + `needs_human_review=True`; **never whole-document; original always preserved**. Four new OFF-by-default flags `SOURCE_TRANSLATION_ENABLED`/`SOURCE_TRANSLATION_MAX_CHARS`(400)/`SOURCE_TRANSLATION_MAX_EXCERPTS`(3)/`TRANSLATION_PROVIDER`(`fake`). **Interpretation call (documented):** translated excerpts are council metadata + a report block (each retaining the original `source_url`), **not** injected into the single-company evidence pack — mirrors the macro/event precedent; original evidence untouched. Reuses existing infra (`EvidenceItem.language`/`original_language`/`requires_translation`, `GapType.translation_required`, 29B.4 euronext-FR / deutsche_boerse-DE / nordic-DK `requires_translation` markers). Dark-by-default byte-identical when off; schema/safety valid, publication_ready false, human_review_required true. Backend **2264 pass / 12 skip / 0 fail** (+26 `test_phase30a_translation.py`), ruff clean, mypy `71` baseline no-new, security PASS (ib-security-agent — off-by-default, bounded, text-free logging, machine-assisted-not-official warning, no new credential/host/SSRF; pre-PR review APPROVED 10/10). **Staging validation (2026-07-28) — OFF-state VALIDATED:** merged `fa3632a`, API `commit_sha=fa3632a` (3 stable polls), web unchanged, head `011`, AUTH_TEST_MODE absent, admin-gated. Registry unchanged **34 enabled / 2 scaffolded / 2 planned / 38 total** (30A adds no source), secret-free. OFF-state no-regression: a **POWL** company report council **8/8**, `translated_excerpts=[]`, **NO `translated_evidence` block**, body unchanged, schema/safety valid, `publication_ready` false, `human_review_required` true. Log hygiene clean (0 translation events since gated off; text-free by construction; caveat: today's `docker.log` not yet in the log download → current-build tail is a follow-up, API surface scanned clean). `SOURCE_TRANSLATION_ENABLED` absent→false + `TRANSLATION_PROVIDER` absent→fake (dark); 6 prior flags ON. **KNOWN LIMITATION (recorded):** the non-English→translated-excerpt **happy-path is unit-fixture-proven, NOT staging-demonstrable** — extraction hits scanned/English PDFs → no non-English excerpts on staging (same no-OCR/scanned-PDF condition as 29B.2); becomes demonstrable once **30B** provides local-language sources. **DECISION: `SOURCE_TRANSLATION_ENABLED` KEPT OFF on staging** (flipping has no validation value — no non-English extracted excerpts to translate; meaningful only once 30B lands). **Deliberate:** LLM-backed translation OFF by default (default provider `fake`) — this is a FOUNDATION; **Phase 30B** (local-language evidence sources) will consume it. Closure: `docs/development/closures/phase-30a.md`. **The Phase 30 umbrella remains 🟡 IN PROGRESS** (30B + PDF table extraction + OCR remain). |
| 30B | Local-language business-press evidence sources | ✅ | #68 `e1d2d8d` | no | **CLOSED + validated (VALIDATED, clean — no environmental note). Completes the Phase 30 umbrella (30A + 30B).** PR #68 → `main` `e1d2d8d`, API `commit_sha=e1d2d8d` (3 stable polls), web unchanged (backend-only), head `011`. The **second** Phase 30 subphase — adds the local-language evidence SOURCES that produce the non-English excerpts the 30A layer translates. New `LocalLanguagePressConnector` (`sources/connectors/local_language_press.py`) + `LOCAL_LANGUAGE_PRESS_SOURCES` allowlist — **Les Échos** (FR) / **Handelsblatt** (DE) / **Milano Finanza** (IT) / **Børsen** (DK), fixed public HTTPS landing pages, no API key. For a **verified non-US FR/DE/IT/DA issuer** it emits ONE bounded **T4_QUALITY_MEDIA `news_article` SOURCE REFERENCE** (provider `ProviderType.news`) carrying a **GENUINE short local-language descriptive excerpt** (what the venue covers + full article content NOT fetched here — **never a fabricated article / headline / quote / figure / date**), marked `requires_translation=True` + `original_language`, `low` confidence / `metadata_only`, + honest `translation_required` & content-not-fetched gaps + `needs_human_review` — it deliberately **LOWERS** source quality (a WEAK research-priority signal, never a recommendation / catalyst / materiality / valuation); a non-eligible / US company → honest `source_not_eligible` gap, never a reference; **network-free**. `company_evidence.py` gains a dedicated `LOCAL_LANGUAGE_REFERENCE_IDS` collection block in `collect_company_source_evidence` (verified non-US FR/DE/IT/DA issuers; regulator mapping untouched). **Consumed by Phase 30A:** with `SOURCE_TRANSLATION_ENABLED` on, the 30A layer detects the non-English excerpt and produces a bounded machine-assisted `translated_excerpt` (original preserved + `needs_human_review` + NOT-official warning); dark when off — the local-language REFERENCE itself is emitted under the already-ON `SOURCE_CONNECTOR_ENABLED`, **independent of** the translation flag. `registry.py` promotes `local_language_business_press` PLANNED→**enabled** (`news` / `T4_quality_media`, `PHASE_30B` label) → **35 enabled / 2 scaffolded / 1 planned / 38 total** (only `openbb` remains planned; SEDAR+/ASX scaffolds). **NO new flag** — reuses the OFF-by-default `SOURCE_CONNECTOR_ENABLED` (collection) + `SOURCE_TRANSLATION_ENABLED` (30A). Dark-by-default byte-identical when off; schema/safety valid, publication_ready false, human_review_required true. Backend **2278 pass / 12 skip / 0 fail** (+14 over 30A; +`test_phase30b_local_language_sources.py`; adjacent count tests to 35/1, no ripple), ruff clean, mypy `71` baseline no-new, security PASS (ib-security-agent; pre-PR review APPROVED 10/10); 15 files (incl. tests). **Staging VALIDATED (2026-07-29, clean):** merged `e1d2d8d`, API `commit_sha=e1d2d8d` (3 stable polls), web unchanged (backend-only), head `011`, AUTH_TEST_MODE absent, admin-gated. **B:** registry shows `local_language_business_press` enabled (`news`/T4), summary **35 enabled / 2 scaffolded / 1 planned (only `openbb`) / 38 total**, honest note, secret-free. **C (AC1/AC2 directly demonstrated, `SOURCE_TRANSLATION_ENABLED` OFF):** evidence-preview → `MC.PA`→French (`lesechos.fr`), `SAP.DE`→German (`handelsblatt.com`), `MONC.MI`→Italian (`milanofinanza.it`), `PNDORA.CO`→Danish (`borsen.dk`), each returning the local-language reference with `requires_translation=true` + correct `original_language` + a **NON-ENGLISH excerpt** + honest `translation_required` gap; company_ir/regulator co-present (no regression). **D:** `AAPL` (US) → no local-language item. **E:** genuine local-language excerpts (no fabricated news), T4/low/needs-review, no reco/valuation, admin-gated. **F:** AUTH_TEST_MODE absent, logs clean, flags correct. **DECISION: `SOURCE_TRANSLATION_ENABLED` KEPT OFF** (conservative default; local-language references still appear in preview with translation state flagged — the machine-assisted translation stays dormant until a human flip; the ON-state `translated_evidence` render is fixture-proven + optional). **Deferral (carried):** live local-language CONTENT fetch deferred (reference-only). Final flags (6 ON; `SOURCE_TRANSLATION_ENABLED` OFF): `SOURCE_CONNECTOR`/`SOURCE_MACRO`/`SOURCE_EVENT`/`SOURCE_DOCUMENT_EXTRACTION`/`LLM_COUNCIL`/`LLM_DISCOVERY_COUNCIL` ON. Closure: `docs/development/closures/phase-30b.md`. **Completes the Phase 30 umbrella; next: Phase 31 (source-aware internal research memo builder — the FINAL phase).** |
| 31 | Source-aware internal research memo | ✅ | #69 `b89d5c5` | no | **CLOSED + validated (VALIDATED, full — no environmental note). The FINAL campaign phase — its closure completes the entire Phase 0 → 31 roadmap.** PR #69 → `main` `b89d5c5` (merge SHA `b89d5c584ecdb07be5eeafe3c32fb4ca4f1a73c6`); **full-stack** — API `commit_sha=b89d5c5` (3 stable polls, build 30445905928) + Web `b89d5c5` (**advanced `793e0a7`→`b89d5c5`**, build 30445901677, triple-corroborated via `/api/version` + homepage meta), head `011`, AUTH_TEST_MODE absent. ONE new OFF-by-default flag `SOURCE_RESEARCH_MEMO_ENABLED`; no new host/endpoint/secret/migration. New backend `_build_research_memo(report_content, council_result, *, source_tier)` (`services/final_report_generator.py`) — a **DETERMINISTIC synthesis** of already-assembled `report_content` sections + `CouncilResult` metadata + gaps (no external call / no LLM / no ORM / no recompute) — emits `report_content["research_memo"]` (attached BEFORE the safety gate), 18 keys: header (internal-only/not-advice), company_identity, why_surfaced, what_is_sourced, **what_is_missing (PROMINENT)**, primary_evidence_summary (primary_documents + primary_facts, cited w/ token-stripped `source_urls`), catalyst_event_evidence, financial_facts_summary (T5 EODHD + T1 `*_primary_filing`; no derived valuation), business_risk_summary, council_disagreement_red_team (red_team dissent + `unsupported_claims` + committee label), research_next_steps, human_review_checklist (references existing checklist, no recompute), source_appendix, `disallowed_outputs` notice, note + disclaimer + `human_review_required=True`. **Citation-bound**; forbidden BUY/SELL/HOLD/WATCH + price-target/fair-value/upside/downside literals appear ONLY in the scanner-exempt `disallowed_outputs` field (key in `_EXEMPT_FIELD_NAMES`); every other field safety-clean. **Honest degradation** on thin evidence (`what_is_missing` prominent, `provenance=missing_data`, honest-empty, never fabricates). NOT in `_REQUIRED_SECTIONS` (schema_valid unaffected); `publication_ready` False; `human_review_required` True; **dark-by-default byte-identical when off**. Frontend: `research_memo` in `SECTION_ORDER` + "Internal Research Memo" label (`finalReportContent.ts`); bespoke `ResearchMemoSection` (`FinalReportRenderer.tsx`) renders sub-blocks readably in the **Readable** tab (Raw JSON hidden-by-default; legacy reports unchanged); `disallowed_outputs` → plain NOTICE (no rating/BUY-SELL UI). Backend **2289 pass / 12 skip / 0 fail** (+11 `test_phase31_research_memo`), ruff clean, mypy `71` baseline no-new; web typecheck/lint/build pass; e2e **196/196** (+2 memo tests); security PASS, review APPROVED 10/10. **Staging VALIDATED (2026-07-29, full — ON-state directly demonstrated, unlike 30A):** `STAGING_BASIC_AUTH` authorized via a scoped read-only `az appsettings list` Bash allow rule. A: SHA convergence both (API+Web `b89d5c5`). B: head `011`, AUTH_TEST_MODE absent. C: registry **35 enabled / 2 scaffolded / 1 planned / 38 total** (unchanged — adds no source). D-off: report regenerated on `b89d5c5` flag-OFF (`from-report` → `a1afdedc`) has **NO `research_memo`**, 22 sections incl. macro+council, body consistent w/ 30B. D-on: after human-approved `SOURCE_RESEARCH_MEMO_ENABLED=true` flip, regenerated report (`a8995bab`/`cb6c0a40`) includes the 18-key memo, `schema_valid=true`, **`safety_valid=true`**, `publication_ready=false`, `human_review_required=true`, citation-bound source_appendix. Safety: every field except `disallowed_outputs` independently scanned → **NO forbidden terms outside the exempt notice** (gate passed WITH memo attached). Degrade: thin source (source_count 0) → `what_is_missing.prominent=true` / `provenance=missing_data`, no fabrication. E: macro/event/council/connector ON, translation OFF, memo flipped ON. F: logs secret-free, clean warm boot. Frontend memo render covered by e2e 196/196 on web `b89d5c5` (report pages OAuth-gated → UI validated via SHA+e2e+API-data, as in 28A.2). **DECISION: `SOURCE_RESEARCH_MEMO_ENABLED` KEPT ON** (validation clean, internal-admin-only, adds analyst value, matches keeping the other validated source flags on) → **7 flags ON now** (`SOURCE_TRANSLATION_ENABLED` the sole OFF source flag). Closure: `docs/development/closures/phase-31.md`. Campaign report: `docs/development/CAMPAIGN_REPORT.md`. **🎯 Phase 0 → 31 COMPLETE.** |
| 31-hotfix | Surface verified primary source references in report/memo | ✅ | #70 `8cc21a6` | no | **CLOSED + validated (VALIDATED — WITH ENVIRONMENTAL NOTES). Focused hotfix ON TOP OF the already-closed Phase 31 — Phase 31 itself stays ✅ closed (NOT reopened, NOT a new phase, roadmap status unchanged).** Merged `8cc21a6` (merge SHA `8cc21a6f505ed8ec3093dd89765e6a902e31c9f1`; branch `hotfix/phase-31-source-reference-surfacing` deleted on merge), deployed full-stack to staging (Deploy API run `30521771452` + Deploy Web run `30521771446`, both success at `8cc21a6`); API `/health` + Web `/api/version` `commit_sha=8cc21a6` (3/3 stable polls), head `011` (no migration — `git show --stat 8cc21a6` has no alembic file), AUTH_TEST_MODE absent (unauth POST `/api/v1/sources/evidence-preview` → 401), registry unchanged **35 enabled / 2 scaffolded / 1 planned / 38 total**. Fixes a Phase-31 report/memo source-surfacing gap: when `company_ir` returns verified **METADATA-ONLY T1 primary-source references** (issuer IR page / annual-report index / press index) but **NO extracted document text and NO parsed primary facts** (scanned / JS-gated PDFs, no OCR), the full-analysis report previously showed **Primary Document Count = 0 / Primary Fact Count = 0 / Source Appendix 0/0** and a memo saying no issuer primary document was extracted — i.e. the verified references were **invisible** even though the live evidence-preview shows them. **Root cause:** the report path collects those metadata-only `EvidenceItem`s into `collected.evidence_items` (no network), but `_primary_document_summary` counts only extracted-excerpt source types and `_primary_facts` counts only high-confidence parsed facts → both empty → the memo fell into the honest-empty "0 primary facts" branch; the Source Citation Appendix counts only DB `Source`/`Citation` rows (never written for connector evidence) → 0/0. The "Company Website / IR / Newsroom not discovered" line was the **unrelated legacy Phase-24 catalyst-discovery** section. **Fix (minimal, additive, dark-by-default, no fabrication, no migration):** new deterministic `_source_reference_summary` (`services/llm/council.py`) classifies metadata-only PRIMARY-source references (tier T1/T2) vs extracted documents vs parsed facts; new `CouncilResult` fields `primary_source_references` / `source_reference_counts` / `source_gaps` (persisted to `source_summary_json.llm_council`); the memo `primary_evidence_summary` gains a **THIRD honest branch** (references-available while extracted-text/facts-unavailable) with counts `primary_source_reference_count` / `primary_document_reference_count` / `extracted_primary_document_count` / `primary_fact_count` / `metadata_only_source_count` / `source_gap_count` + explicit booleans `extracted_document_text_available` / `primary_facts_available`, and the memo `source_appendix` sub-block gains the reference count + a note; the top-level `source_citation_appendix` gains an honest `primary_source_reference_count` + note (**no fabricated `Source`/`Citation` rows**); frontend `AppendixSection` no longer implies zero sources when references exist; the legacy catalyst section header is scoped to "Company News Sources (Catalyst / News Discovery)" + a scoping line. **Guardrails held:** metadata-only references **never** become primary facts; T1/T2 checklist and source-quality / tier scoring untouched; `schema_valid` / `safety_valid` stay true; `publication_ready`=false; `human_review_required`=true. **Still NO OCR / extraction** — this surfaces *existing* verified references, it does not add new evidence. Tests: backend **2297 pass / 12 skip / 0 fail** (+8 `test_hotfix_phase31_source_reference_surfacing.py`), ruff clean, mypy `71` baseline no-new; web typecheck / lint / build pass; e2e **197/197** (0 flakes; 196 baseline +1 focused); both GitHub CI checks green (Lint & Test; Typecheck, Lint & Build); security PASS, pre-PR review GO. No migration (DB head `011`). **Staging VALIDATED (2026-07-30) — WITH ENVIRONMENTAL NOTES.** NEW CFR/SW full report `1e18aa4d-d6f4-4865-ae40-93984c10032c` (company `041cc7e4-0802-4505-9e25-63898f04d12a`, via POST `/api/v1/final-reports/from-company`; distinct from the old buggy report `692b2343`): council **8/8**, `llm_used`, `schema_valid=true`, `safety_valid=true`, `publication_ready=false`, `human_review_required=true`, `research_complete=false`. Memo `primary_evidence_summary`: **`primary_source_reference_count=6`** (5 richemont.com + 1 six-group.com T2 regulator venue), `primary_document_reference_count=4`, `metadata_only_source_count=6`, `source_gap_count=6`, `extracted_primary_document_count=0` + `extracted_document_text_available=false`, `primary_document_count=0`, `primary_fact_count=0` + `primary_facts_available=false` — references surfaced, **NOT presented as facts or DB citations**; note distinguishes references-available vs extracted-text-unavailable (scanned/JS-gated, no OCR) vs facts-unavailable. `source_citation_appendix`: DB sources 0 / citations 0 (distinct from the 6 references) + reconciling note (no longer implies zero sources). CFR/SW evidence-preview (company_ir, annual-report text ON): **5 evidence_items** (4× `company_ir_annual_report` `T1_primary_filing` link-metadata-only + 1× `company_ir_profile` `T1_primary_company_source`, all richemont.com) + 4 honest gaps incl. "PDF appears scanned or text extraction returned no usable text." Forbidden BUY/SELL/HOLD/WATCH + price-target/fair-value/intrinsic-value/upside/downside appear **ONLY** inside the memo's exempt `disallowed_outputs` notice; nowhere else. Regressions PASS: AAPL/US → SEC T1 filing evidence; BA/LSE → "BAE Systems"/"Boeing" only in negated disambiguation (never as the issuer) + `uk_fca_nsm` reference; KER/PA + UHR/SW → metadata-only references + honest gaps, no fabricated filings. **Still NO OCR / extraction added; translation stays OFF; publication stays disabled.** **Environmental notes (non-failures):** (1) full app-log tail not read-only accessible (staging auth allow-rule scoped to `appsettings list`) → secret-scan done over all API response bodies (clean) + code review (hotfix adds only integer-count logging), no secret leak observed; (2) migration head `011` inferred (not queryable via /health) but the hotfix provably ships no migration; (3) the `catalyst_agent.py` scoped heading "Company News Sources (Catalyst / News Discovery)" is verified in merged source but is a CATALYST-DISCOVERY markdown surface the from-company FINAL-report path does not render (that path emits `news_catalyst_discovery` as structured JSON whose no-IR wording is already scoped in `.warnings`) — the "legacy IR-not-discovered text scoped, not global" AC is satisfied in the final report via the memo/appendix surfacing 6 references + the already-scoped news warnings; the catalyst-markdown edit is correct defense-in-depth not exercised by this report path (a known nuance, NOT a defect). Flags UNCHANGED (7 ON; `SOURCE_TRANSLATION_ENABLED` the sole OFF source flag). Closure: `docs/development/closures/phase-31-hotfix.md`. |
| 32 | Durable queues / cost / observability | 🔜 | — | tbd | reliable async, cost ceilings, telemetry |
| 32A.1 | E2E pipeline repair — Slice 1 (lineage / identity / real-mock provenance + deterministic-section preservation) | ✅ | Slice 1 CLOSED: PR #71 **MERGED** → `main` `a26be30` + closing hotfix PR #72 **MERGED** → `main` `cf9d147`; deployed (Slice-1 API 30744460879 / Web 30744498753 @ `a26be30`; hotfix API 30752027507 / Web 30752042756 @ `cf9d147`); **STAGING-VALIDATED + CLOSED 2026-08-02** | no | **✅ CLOSED + STAGING-VALIDATED (`cf9d147`) 2026-08-02.** Merge SHAs: Slice-1 PR #71 `a26be30`, closing hotfix PR #72 `cf9d147`. Golden path PASSED on `a26be30` (new AAPL legacy `d147cdd2` → final `f5fedc5c`: Apple Inc. / AAPL / Nasdaq, `is_mock=false`, `data_provenance=real`, all 6 deterministic sections populated, lineage preserved, flags correct; dark-safety [mock draft → no envelope, honest `unknown`] + secret-scan clean; council 4/8 = env Azure TPM, sections rendered regardless). ONE FINDING surfaced (Phase-31 `research_memo` embedded a STALE "Schema invalid" checklist copy) → closing hotfix (helper `_memo_human_review_checklist_snapshot` refreshes the memo snapshot from the authoritative checklist AFTER the RC-6 recompute; memo prose still built once pre-validation, stays under the safety gate; dark-safe). **Re-validated on `cf9d147`** (regen from `d147cdd2` → final `4c354f29`): memo checklist `not_completed_count=5` == authoritative 5, item set identical, **"Schema invalid" ABSENT** (contradiction GONE); header + `workflow_status.schema_valid`=true consistent with the authoritative checklist; Apple identity + real provenance + all 6 sections preserved; `safety_valid`/`human_review_required`=true, `publication_ready`=false; secret-scan CLEAN; unauth→401 (no auth/publish regression). Closure report `docs/development/closures/phase-32a-slice1.md`. Hotfix tests: backend 2312/20/0, mypy 71, ruff clean, security PASS, PR-review GO. First slice of the Phase 32A E2E real-data pipeline repair (full architecture + RC-1..RC-6 in `docs/development/PHASE_32A_PIPELINE_REPAIR.md`; Slice 1 merges the originally-proposed provenance + section-preservation slices because research showed they are driven by ONE defect). **Root cause (dominant defect — lossy markdown state reconstruction):** the admin "current-schema report" regeneration paths (`generate_from_report` / `generate_from_company`) reconstruct workflow `state` by regex-scraping ` ```json ` fenced blocks out of the legacy report's `content_markdown`, but the Phase-9 writer serialized only the `{"catalyst_discovery": …}` block — company snapshot / financials / bull-bear-risk-valuation-committee summaries / source_tier / `is_mock` were **prose-only**, so 14 of the 15 keys the adapter reads came back `None` → identity "Unknown", `is_mock` default-True, deterministic sections `available:false`, empty financial snapshot, plus a stale "schema invalid" checklist note contradicting the final `schema_valid=true`. **Fix (backend-only, additive, dark-safe, no fabrication, no migration):** (1) the Phase-9 writer now emits a bounded / secret-stripped / **FLAT** structured-state JSON envelope (**14 adapter keys** at top level — the 15 the adapter reads minus `catalyst_discovery`, which keeps its own block per RC-3; safety-scanned + `redact_mapping` + recursive `strip_url_secrets`, size-capped, raw document text dropped, **self-gated dark-safe**) so regeneration recovers `company_snapshot` + the deterministic sections; (2) NEW `data_provenance.py` derives a tri-state `data_provenance ∈ {real, mock, unknown}` from explicit signals (explicit `is_mock=True` ⇒ mock; real provider / real evidence ⇒ real; **absence ⇒ unknown, NEVER mock**), replacing all ~11 default-True `is_mock` sites, and the completer now suppresses numbers **only on explicit mock**; (3) DB identity fallback (`_resolve_company_record_from_lineage`, **PUBLIC research entities only** — `DiscoveryCandidate` / `Scorecard`→`Company`, never personalized/private tables) so a known parent never becomes "Unknown", a genuinely-unknown identity is flagged for human review, never fabricated; (4) checklist + `workflow_status.schema_valid` recomputed from the FINAL validation and persisted before save → kills the stale schema-invalid contradiction (recompute does NOT flip `publication_ready`=false / `human_review_required`=true). **Files changed:** `apps/api/app/services/final_report_generator.py`, `apps/api/app/services/real_asset_report_completer.py`, `apps/api/app/workflows/company_analysis.py`, NEW `apps/api/app/services/data_provenance.py`, NEW test `apps/api/tests/test_phase32a_slice1_lineage_provenance.py`. **Migration: NONE (AD-5 — the markdown envelope mirrors the established catalyst-block pattern, no schema change; a dedicated `analysis_state_json` JSONB column deferred). Feature flags: NONE new** (correctness fix; envelope additive + dark-safe). Tests: backend **2308 pass / 20 skip / 0 fail** (baseline 2297/12/0; +19 new tests; the +8 skips are environmental Azure-gated `test_phase7_azure_openai_real.py`, benign), ruff clean, mypy **71** = baseline (no new app/ errors); web unaffected (no web files changed). Reviews: architecture **GO-WITH-NITS**, security **PASS** (all RC-1..RC-6 controls met — envelope secret-free / bounded / under the safety gate / network-free; one non-blocking advisory: codebase-wide `strip_url_secrets` strips only query-param creds, a pre-existing Phase-29A contract, not introduced here), PR-review **GO-WITH-NITS**. Staging-validation plan drafted at the PR gate: `docs/development/PHASE_32A_SLICE1_STAGING_VALIDATION.md` (execute AFTER human-approved merge/deploy; verifies the AAPL golden path from a NEWLY generated legacy draft carrying the envelope + a dark-safety / byte-identical check + a log secret scan; note that from-report regeneration of the OLD report `23cc7a2f` recovers only IDENTITY via DB fallback — no envelope in the pre-existing draft). **Forward pointer — Slices 2–5 REMAIN:** Slice 2 = evidence-pack financial facts (`fundamentals_data` / `financial_data_summary` / `trend_signal_summary` → `build_evidence_pack`) + category budgets/floors; Slice 3 = citation backfill (real `report_id` UPDATE replacing the `pass`) + honest E#-evidence vs DB-citation distinction; Slice 4 = council retry / fallback / critical-agent reliability; Slice 5 = deeper document ingestion (SEC/8-K/earnings HTML, table extraction, bounded OCR). **Slice 1 COMPLETE — Slices 2–5 remain (do NOT auto-start; each is a fresh bounded phase).** |
| 32A.2 | E2E pipeline repair — Slice 2 (evidence-pack financial-fact wiring + category evidence budgets) | ✅ | #73 `3237d27` | no | **✅ CLOSED + STAGING-VALIDATED (`3237d27`, 2026-08-02) — WITH ENVIRONMENTAL NOTE.** PR #73 squash-merged → `main` `3237d27`; API deploy run `30757018955` + Web run `30757048621` both success; `/health` + `/api/version` = `3237d27` ×3 (env=staging); head `011` (no migration); AUTH_TEST_MODE absent. Human-approved flip `LLM_COUNCIL_EVIDENCE_BUDGETS_ENABLED` OFF→**true** (companion knobs default 3/3/8/4; `LLM_PROVIDER_COUNCIL=azure_openai`, `SOURCE_CONNECTOR_ENABLED=true`). **The OFF→ON contrast IS the fix:** OFF report `74dac609` FA cites ONE blob `E8` and states margins/FCF/capex/debt NOT provided; ON report **`104ecb50`** FA cites tier-split **`E8` income / `E9` cash-flow / `E10` balance-sheet** → revenue $265.6B / gross $195.2B / op $133.1B / net $112.0B / OCF $111.5B / capex $12.7B / assets $359.2B / liab $285.5B / equity $73.7B / cash $35.9B / LT-debt $90.7B / shares 14.8B (reproduced on `42848523`). `financial_floor=3` → the 3 financial items survived among 20 items alongside news `E11`–`E20` (floor held). `data_provenance=real`, `is_mock=false`, Apple/AAPL/Nasdaq, `schema_valid`/`safety_valid`=true (gate passed, 0 warnings), `human_review_required`=true, `publication_ready`=false; no fabricated EBITDA/EV-EBITDA/beta (disclaimer text only); annual never TTM. **CFR fallback `a39ac390`** (council 8/8, Richemont/CFR/SW, `fundamentals_available=false`): `sec_financial_statement`=0, `primary_facts`=0, metadata-only refs stay refs (`metadata_only_source_count=6`); FA honest, VG (completed) withholds valuation citing absent P/E-EV/EBITDA-ROE. Secret scan (response surface) CLEAN; unauth from-report/run/GET → 401; publication admin-gated. **ENV NOTE:** valuation_guard TMP-fails on the large 20-item AAPL pack (AAPL councils 4/8; smaller CFR pack 8/8) — the documented Azure-TPM limiter **Slice 4** owns; VG's correct pack-handling IS shown on CFR; criterion is conditional ("when the agent completes"). Closure: `docs/development/closures/phase-32a-slice2.md`. Flag KEPT ON. **Slices 3–5 remain (do NOT auto-start).** Repairs the assembly-half defect where structured SEC/XBRL financials never reached the council as tiered facts and catalyst/news volume crowded them out. **Gated by ONE new default-False flag `llm_council_evidence_budgets_enabled`** (the same flag gates the build-time tier-split + the category-aware budgeter, so both move together; flag OFF ⇒ evidence pack + budgeter byte-identical to Phase 29B.2). **Changes:** (1) `evidence_pack.py` — `_add_sec_fundamentals` tier-splits `company_snapshot["fundamentals_summary"]` (SEC EDGAR XBRL, ALL ANNUAL 10-K, never TTM) + `market_metrics_summary.source_tiers` into separate items: income/cash-flow/balance-sheet **T1 content / T2 EDGAR transport**; derived margins/ROE/FCF/YoY **DERIVED T6** (never T1/T2); market cap/EV/P-E **T6**; latest close / 52-week **T5**; only present fields emitted (missing ⇒ omitted, never fabricated); honours `period_basis` (labels quarterly on 10-Q fallback); defensively surfaces `financial_data_summary`/`trend_signal_summary` only when populated. `_add_catalysts` carries upstream `relevance_level` + a conservative deterministic derivative-instrument detector (near-ticker symbol AND leverage/inverse cue, e.g. `(AAPU)`+"leveraged" → demote to low-tier/irrelevant, never silently dropped). (2) `schemas.py` — additive optional `EvidenceItem.relevance_level` (default None). (3) `evidence_budget.py` — pure `evidence_category()` classifier + category-aware selection: reserve a floor of `financial_fact` slots, near-dup-dedup news by normalised title, rank within category by `(tier, materiality, factual, order)`, fill by global rank enforcing `price_trend_cap`/aggregate `news_cap`/strict `low_tier_news_cap`; total+per-item char budgets + stable `E1..En` re-id + honest omitted count/reason preserved; flat Phase 29B.2 path kept byte-identical when flag off. (4) `config.py` — 5 new knobs (`llm_council_evidence_budgets_enabled=False`, `financial_floor=3`, `price_trend_cap=3`, `news_cap=8`, `low_tier_news_cap=4`); the pinned 20/24000/1200 UNCHANGED. **Files:** `apps/api/app/services/llm/evidence_pack.py`, `apps/api/app/services/llm/evidence_budget.py`, `apps/api/app/services/llm/schemas.py`, `apps/api/app/core/config.py`, NEW `apps/api/tests/test_phase32a_slice2_evidence_budgets.py`, docs `PHASE_32A_PIPELINE_REPAIR.md` §8 + this row. **Invariants held:** identity always built; financial-fact floor; primary/filing outranks aggregator news; news can't consume whole pack; low-tier strict cap; near-dup events removed; deterministic selection; materiality affects ranking; metadata-only refs NEVER facts (CFR `_source_reference_summary` untouched); derived never T1/T2; annual never TTM; Slice-1 provenance untouched (absence⇒unknown, is_mock never coerced True); publication_ready False / human_review_required True unchanged; no new network, counts-only logging. **Tests:** new file 26/26 pass; at-risk regression set (28A/29B2/29A/28A1/29B1/32A-slice1/31-hotfix) 180/180 pass; ruff clean; mypy 71 = baseline (0 new). **Migration: NONE. Merged/deployed/STAGING-VALIDATED — Slices 3–5 remain (do NOT auto-start).** |
| 32A hotfix | Company-scoped `from-company` final-report selection | ✅ | PR #74 (squash) → `main` `cd0eb87`; API deploy `30802241000` + Web `30802320097` (success); **STAGING-VALIDATED 2026-08-03** | **yes (`012`, applied+verified)** | **✅ CLOSED + STAGING-VALIDATED (`cd0eb87`, 2026-08-03).** Closure `docs/development/closures/phase-32a-hotfix-from-company.md`. Precedes Slice 3. NARROW correctness fix: `generate_from_company` selected the GLOBALLY-newest completed report (no company predicate) → `from-company/CFR` could return an Apple analysis. Root cause: NO DB link existed from a `Report` to a `company_id`. Adds a real `reports.company_id` FK (migration `012_add_report_company_id.py` — nullable, `ON DELETE SET NULL`, index `ix_reports_company_id`; **head 011→012 applied+verified on staging**, no backfill) written at draft creation (`report_service` + `company_analysis` workflow), and rewrites the source-report query to filter `Report.company_id == company_id` with deterministic tie-break `(created_at desc, id desc)`; **fails clearly (ValueError → 404) when the company has no eligible report — NO cross-company fallback**. Pre-012 reports keep `company_id=NULL` → unreachable via from-company (404, by design; API.md operator note). Invariants preserved (human_review_required True, publication_ready False, safety/provenance/is_mock unchanged; `generate_from_report`/`from-scorecard`/`from-candidate`/`from-workflow-state` untouched). **Pre-merge:** new suite `test_hotfix_from_company_scoping.py` 13/13; backend 2359/12/0; ruff clean; mypy 242=baseline 0-new; security PASS; PR-review GO; CI pass. **Staging A–I VALIDATED (both creation orders):** Order A (CFR globally-newest) from-company(AAPL)→AAPL#1 `2733cf87` / from-company(CFR)→CFR#1 `2984ddec`; Order B (AAPL globally-newest) from-company(CFR)→still CFR#1 / from-company(AAPL)→AAPL#2 `fd023aa0` (newest-of-multiple); pre-012-NULL + unknown + no-report all 404 with 0 fabricated rows; from-report unchanged; every from-company success `schema_valid`/`safety_valid`/`human_review_required`=true, `publication_ready`=false, `data_provenance=real`, `is_mock=false`, forbidden_terms=[]; unauth→401, /health→200; response+log secret scan CLEAN. **Slices 3–5 remain (do NOT auto-start).** |
| 32A.3 | E2E pipeline repair — Slice 3 (source/citation persistence + honest reconciliation) | ✅ | PR #75 (squash) → `main` `3efda60`; API deploy `30826659074` + Web `30826658153` (success); **STAGING-VALIDATED 2026-08-03** | no | **✅ CLOSED + STAGING-VALIDATED (`3efda60`, 2026-08-03) — WITH ENVIRONMENTAL NOTE (Azure-TPM partial councils, owned by Slice 4).** Closure: `docs/development/closures/phase-32a-slice3.md`. Reviewed head `1a3e76b` (= merge SHA code). **B1 fixed pre-merge:** the ib-pr-review agent found that stamping lineage on the final report made a prior final report selectable by `generate_from_company` (2nd from-company would source it → Slice-1 lossy re-parse + count inflation, stored 7 vs fresh 4); fixed by sourcing ONLY analysis drafts (`final_report_version IS NULL`) + counting only non-`council:%` rows as deterministic + per-claim citation dedup. **Full A–I validated (flag flipped absent→true):** OFF dark regression (AAPL final `76d3bfe0` — six count keys absent, byte-compatible); ON (AAPL r1 `5b7b464d`/r2 `d56d3d89`/from-report `a788a002`, CFR `5c59cf78`) — stored==fresh counts in every case (AAPL r2 `30`=7 draft-det+23 council, NOT inflated to r1's 85; B1 confirmed r2 sourced draft `53dc7b5d` not r1), six counts distinct, SEC/XBRL T1 provenance retained, CFR `structured_financial_fact_count=0` metadata-only intact, cross-company disjoint (AAPL `5d36744d`≠CFR `041cc7e4`), no secrets in persisted URLs (eodhd canonicalized without api_token), logs+response CLEAN, unauth→401, schema/safety valid, publication_ready false, human_review_required true. Reviews: ib-security PASS, ib-pr-review GO (post-B1), empirical atomicity/dedup/url probes GREEN. Flag `REPORT_CITATION_PERSISTENCE_ENABLED` KEPT ON. Full source/citation persistence + honest reconciliation behind ONE new default-OFF flag `REPORT_CITATION_PERSISTENCE_ENABLED` (`report_citation_persistence_enabled`); **NO migration** (DB head stays `012`); OFF ⇒ byte-identical. Fixes the §1.4 orphan-citation defect + the disconnected FINAL report: (1) **Layer 1** replaces the `pass` backfill with `citation_service.link_citations_to_report` (scoped idempotent `UPDATE citations SET report_id WHERE agent_run_id=:arid AND report_id IS NULL`); (2) **Layer 2** `_save_final_report_draft` now carries lineage (`company_id`+`created_by_agent_run_id` from explicit signals only, stops discarding `source_report_id`), `_load_citations_for_report` unions `report_id` rows + lineage DETERMINISTIC rows (`field_path NOT LIKE 'council:%'` — council rows loaded ONLY by report_id → no cross-report leak), `run_council` snapshots the pack onto `CouncilResult.persistable_evidence` (runtime-only `exclude=True`) so a cited `E#` resolves to a canonical `Source`+`Citation` persisted flush-only in the single transaction, deduped by a synthesized `content_hash` (url-less SEC facts dedup), delete-before-insert idempotent, failed/skipped agents persist nothing, `canonicalize_source_url` strips userinfo+fragment+credential-params, metadata-only refs persisted as REFERENCES (never facts); (3) **Layer 3** appendix gains SIX honest side-by-side counts (NEVER summed) + reconciling `note`; frontend renders them + no longer says "No sources cited yet" when council/DB citations exist. Stable UUIDs = `Source.id`/`Citation.id`; E# stays a run-local alias. **Migration NONE (`013` not required)** — reuses existing `sources`/`citations` cols; a Source unique constraint + a dedicated `evidence`/`claim_link` table deferred (nullable cols defeat the constraint; would break on staging dups). **Compat:** old pre-slice reports keep honest zero-count appendices (not force-backfilled — safely unrecoverable); `company_id=NULL` behavior explicit. **Files:** `apps/api/app/services/final_report_generator.py`, `citation_service.py`, `services/llm/council.py`, `evidence_pack.py`, `schemas.py`, `sources/redaction.py`, `core/config.py`, `workflows/company_analysis.py`, `apps/web/src/components/reports/FinalReportRenderer.tsx`, NEW `apps/api/tests/test_phase32a_slice3_source_citation_persistence.py`, docs `PHASE_32A_PIPELINE_REPAIR.md §9` + `API.md` + `DATABASE.md` + this row. **Pre-merge tests:** slice-3 suite 13/13; at-risk set (from-company/phase16/slice2/phase31/31-hotfix/sources/citations/source-service/citation-service/28a-council) 203/203; ruff clean; web typecheck+lint clean (build deferred to CI — worktree node_modules symlink). Architecture + research: `PHASE_32A_PIPELINE_REPAIR.md §9`. **Slices 4–5 remain (do NOT auto-start).** |
| 32A.4 | E2E pipeline repair — Slice 4 (LLM council reliability: bounded transient-only retry + reserved critical budget + deterministic chair fallback) | 🟡 | branch `phase-32a-slice4-council-reliability` `5bbaaf4` (PR open; **NOT merged / deployed / validated**) | no | **🟡 IMPLEMENTED ON BRANCH — PR OPEN, PENDING STAGING VALIDATION.** Do NOT mark ✅ until a closure report with staging validation exists. Repairs the RELIABILITY half of the pipeline: the single-company council runs strictly sequentially and INLINE in the HTTP request handler (bounded by the ~230s Azure gateway), so under Azure `gpt-4.1-mini` TPM limits a large pack (AAPL) left ~4/8 agents `failed` on a single 429 and the last-place chair (no fallback) produced a null `committee_label` — this owns the "Azure-TPM partial councils" environmental note carried from Slices 2–3. **Gated by ONE new default-OFF master flag `LLM_COUNCIL_RETRY_ENABLED` (`llm_council_retry_enabled`); flag OFF ⇒ council byte-for-byte identical (one attempt/agent, no retry, no fallback).** NO migration (head stays `012`); no auth/publish/SSRF change; `publication_ready` stays False, `human_review_required` stays True; failed agents still create no citations. **Changes:** (1) `services/llm/client.py` — new transient error types `LLMRateLimitError` (429, carries a bounded numeric `retry_after`) / `LLMServerError` (5xx) + `is_transient_llm_error` (timeout also transient; `LLMJsonError`/`LLMUnavailableError`/generic `LLMError`/safety-quarantine `failed` status = PERMANENT, never retried). (2) `services/llm/azure_openai_client.py` — `_classify_provider_error` duck-types a raw provider exception into those types by status-code + class-name only (never imports the SDK; carries only numbers/type-names; a `_coerce_retry_after` reads only numeric `retry-after`/`retry-after-ms`; nothing logged). (3) `services/llm/council.py` — `run_council` splits into an OFF path (verbatim single pass) and an ON path (`_run_council_with_retries`): initial pass under a strict TOTAL wall-time deadline `llm_council_total_budget_seconds`(150) then a priority-ordered retry pass over ONLY transiently-failed agents; per-agent attempt caps `llm_council_max_retries`(2, optional)/`llm_council_critical_max_retries`(3, critical), capped jittered exponential backoff (`_base_backoff`(1.0)/`_max_backoff`(20.0)) and a capped honored provider retry-after (`_max_retry_after`(30.0)); a `llm_council_critical_reserve_seconds`(45) reserve protects the RESERVED agents (`red_team`+`committee_chair`); critical set = `financial_analyst`/`source_quality_critic`/`red_team`/`committee_chair` (+`valuation_guard` only when `has_financial_evidence(pack)`); selective retry (completed agents untouched, failed placeholder REPLACED in place + failure warning cleared, chair prompt rebuilt from recovered summaries); deterministic `_deterministic_chair_fallback` (`committee_label="insufficient_data"`, empty `key_points`⇒no citations, non-consensus, safety-scanned) attached as new `CouncilResult.chair_fallback_used`/`deterministic_chair` while the failed LLM chair entry is KEPT in `agents` (visibly partial). Injectable `clock`/`sleeper`/`rng` for deterministic tests. (4) `services/llm/schemas.py` — `CRITICAL_ALWAYS`/`RESERVED_AGENTS` constants + `has_financial_evidence`; `to_report_dict`/`to_metadata_dict` additively surface `chair_fallback_used` + `deterministic_committee_chair` ONLY when the fallback fired. (5) `services/final_report_generator.py` — memo `council_disagreement_red_team` gains `committee_chair_fallback_used` + `committee_chair_synthesis` (`provenance=deterministic_fallback`) when the fallback fired. (6) `services/llm/fake_client.py` — per-agent scripted failure QUEUE + `calls`/`user_prompts` assertion state. (7) `core/config.py` — the 8 new knobs above. NEW `tests/test_phase32a_slice4_council_reliability.py`. **Deferred (see DECISIONS ADR-013):** concurrency (worsens TPM 429s on the inline path) + per-agent evidence projection (risks evidence loss / reopens Slice 2). Docs: AGENTS.md ("LLM Council Reliability") · DEPLOYMENT.md (8-env-var section) · DECISIONS.md (ADR-013) · GAP_ANALYSIS.md caveat · API.md (Phase 32A Slice 4 response-field section) · this row. **Slice 5 remains (deeper document ingestion). Do NOT auto-start; staging validation of Slice 4 is a later human-gated step.** |
| tooling | Claude Code agents + phase workflow skills | ✅ | #55 `c98adca` | no | this dev-workflow system; no app runtime change; merged |

DB head baseline: **012** (`012_add_report_company_id.py`, added by the Phase 32A
from-company hotfix; the prior long-standing baseline was `011`). Phase 32A Slice 3
adds **no** migration (head stays `012`). Confirm `alembic current` when validating
any phase that claims a schema change.

## How to update
- Move a phase's status as it advances; fill PR/SHA/migration when known.
- Never set ✅ until a closure report with staging validation exists.
- Keep one row per phase; link the closure report if archived elsewhere.
