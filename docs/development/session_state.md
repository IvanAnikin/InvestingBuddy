# Session State — Phase 31 (source-aware internal research memo builder) · Stage: NOT STARTED · updated 2026-07-29

> Resumable snapshot. Overwrite at each checkpoint (context-compaction skill).
> Keep decisions + evidence, not raw logs.

## Current position
- On **`main`** @ **`e1d2d8d`**, clean tree. Autonomous multi-phase campaign (Phase 0 → 31). **Phase 31 is the FINAL phase.**
- **Phase 30B — local-language business-press evidence sources: ✅ CLOSED + staging-validated (VALIDATED, clean — no environmental note).** Merged (PR #68 `e1d2d8d`), deployed (API `commit_sha=e1d2d8d`, 3 stable polls; web unchanged — backend-only), staging-validated. Backend-only, 15 files, **NO migration** (DB head `011`), no new host/endpoint/secret, **NO new flag**. Closure: `docs/development/closures/phase-30b.md`.
- **Phase 30 umbrella — ✅ COMPLETE** (30A translation foundation `fa3632a` + 30B local-language evidence sources `e1d2d8d`, both merged + deployed + staging-validated). PDF table extraction + OCR are deferred later-area work — **NOT blocking Phase 31**.
- **Phase 31 — source-aware internal research memo builder: 🔜 NEXT, NOT STARTED.** Assemble a readable **internal admin research memo** from data the system already holds (evidence packs, explicit source gaps, LLM-council outputs incl. disagreement, primary facts, red-team critiques) — most likely a **new report section / renderer** over existing `final_report` / council / evidence data (no new evidence sources). Must be **citation-bound** (source tier per claim), **degrade honestly when evidence is thin**, and stay **strictly internal**: no public publishing, no recommendation labels, no valuation, no price targets.
- **ALL OF PHASE 29 + PHASE 30 COMPLETE** (closed + staging-validated): 29A framework · 29B filings/regulator + primary docs/facts (29B/29B.1–3) · 29B.4 EU/UK regulated disclosures (4A/4B/4C) · 29C macro/commodity/policy (29C.1–3) · 29D event-trigger (29D.1–3) · 30A translation foundation + 30B local-language sources.

## Phase 30B — condensed closure evidence
- New **`app/services/sources/connectors/local_language_press.py`**: `LocalLanguagePressConnector` + `LOCAL_LANGUAGE_PRESS_SOURCES` allowlist — **Les Échos** (FR) / **Handelsblatt** (DE) / **Milano Finanza** (IT) / **Børsen** (DK); fixed public HTTPS landing pages, no API key. For a verified non-US FR/DE/IT/DA issuer → ONE bounded `T4_QUALITY_MEDIA` `news_article` SOURCE REFERENCE (provider `news`) with a **GENUINE non-English descriptive excerpt** (full article NOT fetched — never a fabricated article/headline/quote/figure/date), `requires_translation=True` + `original_language`, `low`/`metadata_only`, honest `translation_required` + content-not-fetched gaps, `needs_human_review`; US/non-eligible → honest `source_not_eligible`. Network-free. `company_evidence.py` `LOCAL_LANGUAGE_REFERENCE_IDS` block; `registry.py` promotes `local_language_business_press` PLANNED→enabled (`news`/T4) → **35 enabled / 2 scaffolded / 1 planned (only `openbb`) / 38 total**. **NO new flag** (reuses `SOURCE_CONNECTOR_ENABLED` collection + `SOURCE_TRANSLATION_ENABLED` 30A).
- Tests: backend **2278 pass / 12 skip / 0 fail** (+14; adjacent count tests to 35/1, no ripple), ruff clean, mypy `71` baseline no-new, security PASS (pre-PR review 10/10).
- **Staging VALIDATED (2026-07-29, clean):** API `e1d2d8d` (3 stable polls), web unchanged, head `011`, AUTH_TEST_MODE absent, admin-gated. **B:** registry `local_language_business_press` enabled (`news`/T4), 35/2/1/38, honest note, secret-free. **C (AC1/AC2 direct, translation OFF):** evidence-preview `MC.PA`→French (`lesechos.fr`), `SAP.DE`→German (`handelsblatt.com`), `MONC.MI`→Italian (`milanofinanza.it`), `PNDORA.CO`→Danish (`borsen.dk`) — each with `requires_translation=true` + correct `original_language` + NON-ENGLISH excerpt + honest `translation_required` gap; company_ir/regulator co-present (no regression). **D:** `AAPL` (US) → no local-language item. **E:** genuine excerpts (no fabricated news), T4/low/needs-review, no reco/valuation, admin-gated. **F:** AUTH_TEST_MODE absent, logs clean, flags correct.

## Decisions (this phase + carried)
- **`SOURCE_TRANSLATION_ENABLED` KEPT OFF on staging** — conservative default; the local-language references still appear in evidence-preview with their translation state flagged (C above), so the machine-assisted translation stays dormant until a human flip. The ON-state `translated_evidence` render is **fixture-proven + optional** (needs a human flip).
- **Live local-language CONTENT fetch DEFERRED (reference-only)** — the connector emits a venue reference + genuine descriptive excerpt, never the full article body; a live local-language content fetch (allowlisted, bounded, reviewed) is a documented follow-up.
- **PDF table extraction + OCR deferred** to later-area work — NOT blocking Phase 31.
- **Standing (carried):** reference-only + deferred fetch across 29B.4 / 29C / 29D / 30; allowlisted-only (no broad/arbitrary web search); evidence-first, citation-bound; no recommendations / ratings / valuations / price-targets; `human_review_required=true` / `publication_ready=false`; `/admin/*` OAuth-gated. Azure OpenAI gpt-4.1-mini TPM quota is a standing staging environmental limiter (NOT a code defect).

## Staging flags (6 ON; 30A translation flag stays OFF)
`LLM_COUNCIL_ENABLED` · `LLM_DISCOVERY_COUNCIL_ENABLED` · `SOURCE_CONNECTOR_ENABLED` · `SOURCE_DOCUMENT_EXTRACTION_ENABLED` · `SOURCE_MACRO_ENABLED` · `SOURCE_EVENT_ENABLED` — all ON. **`SOURCE_TRANSLATION_ENABLED`=false (KEPT OFF)**; `TRANSLATION_PROVIDER`=`fake` (default). DB head `011`.

## Phase 31 — scope seed (the FINAL phase, memo per campaign spec)
Internal admin research memo assembled from EXISTING data (no new evidence sources). Citation-bound (source tier per claim), degrades honestly when evidence is thin, strictly internal — **no public publishing, no recommendation labels, no valuation, no price targets, no BUY/SELL/HOLD/WATCH / fair-value / upside**. Memo structure: header (internal-only — NOT advice) · company identity · why surfaced · what's sourced · what's missing · primary evidence · catalyst/event evidence · financial facts · business/risk · council disagreement / red-team · research next steps · human-review checklist · source appendix · disallowed-outputs notice. Likely a new report section / renderer over `final_report` / council / evidence data; `human_review_required=true` / `publication_ready=false` unchanged; `/admin/*` OAuth-gated.

## Known limitations / carry-forward
- 30B is reference-only (venue reference + genuine excerpt, not article content); `needs_human_review` on every item; translation (when enabled) is machine-assisted, bounded (400 chars), never official.
- The 30A translation happy-path (ON-state `translated_evidence` render) remains fixture-proven + gated OFF — but the non-English references themselves are now staging-demonstrable (30B, translation OFF).

## Next exact command / action
- **Scope Phase 31 (source-aware internal research memo builder) and create branch `feature/phase-31-research-memo-builder`.** Then implement the internal memo (assemble from existing `final_report`/council/evidence data), keep it citation-bound + honest (degrade when evidence thin) + no recommendation/valuation, run checks + ib-security-agent + ib-pr-review-agent, and STOP at the merge gate (do NOT merge/deploy/mark closed until human approval + staging validation is on file).
