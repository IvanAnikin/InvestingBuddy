# V3.17 — Research Escalation: Implementation Report and Acceptance Handover

**Date:** 2026-09-18; activation 2026-09-19; **V3.17.7 deployed and accepted 2026-09-19**
**Status:** migration 040 applied; all three flags live; durable worker proven
exactly-once; the escalation loop **closes**, and a missed terminal callback is
**repaired from the database**.
**Verdict:** §16 — **RESEARCH ESCALATION: PASS — AUTONOMOUS LOOP PROVEN.**
**One condition:** §15.6 must be fixed before a cost price book is configured.

---

## 1. Version, merge and deployment record

| Slice | PR | Commit | Merge SHA |
|---|---|---|---|
| V3.17.1 — `research_decisions` + migration 040 | #201 | `9d32eef` | **`48c8b4d`** |
| V3.17.2 — deterministic predicates | #202 | `772b9e9` | **`3d79e9d`** |
| V3.17.3 — store, controller, `POST …/escalate` | #203 | `6e87ffb` | **`84fe45d`** |
| V3.17 Phase 2 — durable queue lifecycle on real PostgreSQL | #204 | `e30358e` | **`2b4c1a1`** |
| V3.17.4 — evidence delta + round control | #205 | `6087562`, `b4ad3c6` | **`bf242bb`** |
| V3.17.5 — research queue UI + read API | #206 | `4872983` | **`e606339`** |

Each merged only on green CI, after an independent diff review.

**Migration head in the repository: 040.**
**Migration head applied in production: 039.** See §10 — 040 has never been applied,
because applying it requires database access this session does not have.

---

## 2. OBSERVED — durable worker topology, decided from code and Azure

The brief required this to be determined, not guessed.

| Evidence | Value |
|---|---|
| App Service startup command | `gunicorn app.main:app --workers 1 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000 --timeout 300` |
| App Service plan | `ib-stg-plan`, **B1**, `numberOfWorkers: 1` |
| `DEPLOYED_GUNICORN_WORKERS` (code) | `1` — matches the live command |
| `v3_job_worker_in_process` default | `True` |
| Worker start | `app/main.py::_start_durable_worker`, only when `v3_durable_jobs_enabled` **and** `v3_job_worker_in_process` |
| Worker stop | bounded grace, then the lease lapses and the next process reclaims — a shutdown never marks a job failed |
| Env injection in the startup command | none |

### Decision: **Option A — the in-process worker.**

Reasons, in order:

1. **It is the topology the code was written for.** `config.py` states it plainly: *"True
   is the honest default for the current single-App-Service deployment."*
2. **One gunicorn worker on one B1 instance** means exactly one in-process worker, so
   there is no multi-worker lease contention to reason about on the first release.
3. It supplies everything the brief asked for — durable DB-backed queue, lease,
   heartbeat, retry, restart recovery — **with no additional infrastructure**.
4. Option B (standalone `python -m app.services.jobs.worker`) requires a second App
   Service. That is new infrastructure, it fails the "no additional infrastructure
   dependency" requirement, and the code records it as OPEN DECISION #2 — unapproved
   cost.
5. `research_job` reads `DEPLOYED_GUNICORN_WORKERS` to decide whether a job that started
   before this process booted can be called abandoned. With exactly one worker that is
   knowable immediately; the shortcut disables itself automatically if the count is ever
   raised.

**Honest limitation, recorded rather than glossed:** the in-process worker leaves
CPU-heavy extraction in the API process. It makes execution survive a recycle, which is
the point, but it does not move load off the API. Setting
`V3_JOB_WORKER_IN_PROCESS=false` and running the standalone worker elsewhere moves it
with no code change.

---

## 3. OBSERVED — no stale queued work exists

The brief made this check mandatory before a worker is allowed to come alive.

**Structural proof.** `research_jobs` has exactly two writers in the entire application:

| Writer | Gate |
|---|---|
| `company_research_job.submit` → `JobStore.enqueue` | only called when `durable.durable_enabled()`, i.e. `V3_DURABLE_JOBS_ENABLED` |
| `escalation/controller._enqueue` → `JobStore.enqueue` | `V3_RESEARCH_ESCALATION_ENABLED` **and** `V3_DURABLE_JOBS_ENABLED` |

Both gates read `V3_DURABLE_JOBS_ENABLED`. That setting is **absent** from the live
58-setting snapshot taken at `bf242bb` (§4), so it resolves to its `False` default, so
neither writer has ever been able to run in production.

No migration seeds the table: migration 019 creates it with no `INSERT`, and no migration
anywhere inserts into `research_jobs` (checked by grep across `alembic/versions/`).

**Therefore `research_jobs` is empty, and enabling the worker cannot cause anything to
execute unexpectedly.**

This is labelled **DERIVED**, not observed: the row count itself was never read, because
database access was denied (§10). The argument is structural and complete, but it is an
argument.

---

## 4. OBSERVED — production state at the time of writing

> **Superseded by §10.1**, which re-read every one of these values on 2026-09-19.
> Nothing below changed, but §10.1 is the current observation; this section is the
> 2026-09-18 snapshot.

| Field | Value |
|---|---|
| Deployed SHA | `bf242bb` at snapshot; `e606339` after V3.17.5 |
| `/health` | 200, `status: ok`, `environment: staging` |
| Web | 200 |
| V2 API smoke | `/companies` 200, `/reports` 200, `/sources/health` 200 |
| App settings | 58, snapshotted by **name only** |
| `V3_DURABLE_JOBS_ENABLED` | **absent** |
| `V3_JOB_WORKER_IN_PROCESS` | **absent** (code default `True`) |
| `V3_RESEARCH_ESCALATION_ENABLED` | **absent** |
| `V3_ESCALATION_MAX_ROUNDS` | **absent** (code default `2`) |
| `V3_ESCALATION_MAX_DECISIONS_PER_RUN` | **absent** (code default `5`) |
| `V3_ESCALATION_COST_CAP_USD` | **absent** (code default `0.0` = no cap configured) |

Every value above is the **runtime** state. Where a setting is absent the code default is
named as a default, not reported as if it had been configured.

---

## 5. OBSERVED — the durable queue lifecycle, proven on real PostgreSQL

The brief listed twelve behaviours. All twelve are covered by
`tests/test_v3_17_durable_job_lifecycle_postgres.py` (17 tests), green in CI against
PostgreSQL 16.

**Why this file had to be written.** The queue had never been executed against a database
that implements it:

* `test_v3_durable_job_contract.py` has 63 tests and **touches no database** — it drives
  pure `JobView` transitions.
* `test_v3_worker_executor.py` and `test_v3_company_research_durable.py` use `JobStore`
  but **never PostgreSQL**.

So `FOR UPDATE SKIP LOCKED` (which SQLite silently ignores), the UNIQUE idempotency
constraint under a race, and lease expiry had never once run.

| # | Behaviour | Proven by |
|---|---|---|
| 1 | enqueue | one row, `pending` |
| 2 | claim | `running` + owner |
| 3 | lease | `lease_expires_at` in the future |
| 4 | heartbeat | lease extended for the owner; a stranger gets `None` |
| 5 | duplicate enqueue | **10 concurrent submits → 1 job, 1 creator** |
| 6 | worker interruption | lease allowed to **lapse**, never hand-edited |
| 7 | lease recovery | a second worker reclaims it |
| 8 | retry accounting | reclaim spends an attempt |
| 9 | cancellation | pending cancelled outright; running asked cooperatively |
| 10 | dead-letter | repeated transient failure terminates with a reason |
| 11 | restart | a fresh `JobStore` claims the committed row |
| 12 | completion | terminal, lease released, never claimed again |

---

## 6. OBSERVED — evidence delta and round control

`escalation/evidence.py` and `escalation/rounds.py`, 43 tests.

**Improvement is measured on acquisition, never on model output.** The decisive
dimensions are indexed chunks, searchable documents, closable gaps closed, and active
scoped facts. Verified findings are carried as **secondary telemetry only** — a threshold
that rewards a model for emitting more findings pays for prose, and an investigator
defect would make genuinely acquired evidence look like nothing and terminate a working
loop.

**Counting is scoped three ways**, each of which has burned this repository before: to the
company (via `ResearchDocument.company_id` *and* the chunk's denormalised `company_id`),
to current/active rows (`is_current` version **and** `is_active` derivation), and to
`research_gaps` rather than `DiscoveryCandidate.missing_fields_json`.

**Two defects were found by writing the tests, not by reading:**

1. The first chunk query joined only to the version and checked `is_current`, so chunks
   from a **superseded derivation of a current version** were still counted — a
   re-extraction of a document already held would have read as fresh acquisition and
   authorised a paid round.
2. Mutation testing showed the denormalised `chunk.company_id` filter was unreachable by
   any test. It is redundant while the data agrees and load-bearing when it does not, so
   a denormalisation-drift test now makes it matter.

**`exhausted_no_improvement` and `research_did_not_complete` are different claims**, and
`decide_next_state` asks *"did research actually happen?"* before *"did it find
anything?"*. A job that did not complete can never reach `exhausted`. This is the one
distinction the brief called mandatory.

---

## 7. OBSERVED — the crash that could have caused a permanent outage

A decision row and a job row are written in **different transactions** — `JobStore` owns
its own session by design. A process dying between them leaves one without the other, and
an open decision with no executable job is not a stalled item: `ux_research_decisions_one_open`
then forbids **any** further decision for that company, permanently.

Fixed before merge, two ways:

* **Order.** `complete_round` enqueues the next round's job *before* moving the decision
  to `reanalysis`. A crash between leaves `evidence_updated` + a live job — recoverable.
* **Key.** `round_idempotency_key(decision_id, round)` is deterministic and recomputable
  from the row alone, so re-enqueueing after a crash joins rather than duplicates. That is
  what makes `recover_stranded_decisions` safe to run repeatedly and concurrently.

---

## 8. OBSERVED — the escalation path is live and refuses correctly

> Observed on deployed `84fe45d` (2026-09-18). **Not re-observed on `0542a24`** — the
> re-check POST was denied on 2026-09-19 (§10.2). No code between those SHAs touches
> the escalation adapter, but that is an argument, not a fresh observation.

On deployed `84fe45d`, against the real European-luxury discovery run
`ff83c0ce-c0a4-40cf-ac4a-a4a2fe1eedeb`:

```json
POST /api/v1/market-discovery/runs/ff83c0ce-.../escalate  → 200
{
  "candidates_considered": 2,
  "created_count": 0,
  "refused_count": 0,
  "blocked_reason": "escalation_disabled",
  "blocked_detail": "V3_RESEARCH_ESCALATION_ENABLED is not set, so no research decision
                     was created. Escalation starts paid research without a human
                     clicking, and it is off until deliberately enabled."
}
```

`candidates_considered: 2` is the adapter **reading the council bucket that nothing has
read for phases** — the V3.17 defect is closed on the live deployment. Nothing was
created, no flag was changed, and the refusal names the flag a human must set.

---

## 9. KNOWN DEFECTS

### 9.1 The Chair can say "0 findings" while the Ledger holds findings

`council_v2/inputs.py`'s blocking-gap refusal branch returns a `CouncilInput` **without a
`findings=` argument**, so it defaults to empty, and the deterministic Chair prints
`len(council_input.findings)` alongside the words *"this is the ledger's own state"*.

On the V3.16.1b acceptance run the Ledger held **18** findings and the Chair reported
**0**.

The count is correct *for the council pack*; the sentence is wrong in the refusal case.
**Not repaired here**, per instruction: changing Chair/Council semantics to make output
look better is exactly what V3.17 must not do. It does not prevent correct execution or
state transitions. It deserves a small slice of its own.

### 9.2 The in-process worker leaves extraction on the API process

See §2. Recorded, not fixed — fixing it is the standalone-worker decision.

---

## 10. V3.17.6 production activation — EXECUTED 2026-09-19

The activation ran. Migration 040 is live, all three flags are on, a real durable job was
consumed exactly once, and two real escalation decisions were created and executed.

**And the loop does not close.** See §14.

### 10.1 OBSERVED — schema

| Step | Evidence |
|---|---|
| Before | `GET /api/v1/research-decisions?limit=1` → **503**, "Migration 040 has not been applied here" |
| Applied | `az webapp ssh` → `python -m alembic current` **039** → `upgrade head` → *"Running upgrade 039 → 040, Research decisions — V3.17.1."* → `current` **040 (head)** |
| After, verified externally by this session | `GET /api/v1/research-decisions?limit=1` → **200**, body `{"decisions":[],"total":0}` |

`503 → 200` with a genuine empty list. "No table" and "no rows" are now different facts in
production, which is what V3.17's final slice existed to make true.

**Runbook correction (new).** The deployed app does **not** run from `/home/site/wwwroot`.
The live path is **`/tmp/8df15c4c400ed3a`**, an Oryx extraction directory containing
`antenv/`, `alembic.ini` and `app/`. The `antenv` correction from PR #208 held; the
directory did not.

### 10.2 OBSERVED — runtime configuration

| Setting | Runtime value |
|---|---|
| `V3_DURABLE_JOBS_ENABLED` | **true** (configured) |
| `V3_JOB_WORKER_IN_PROCESS` | **true** (configured) |
| `V3_RESEARCH_ESCALATION_ENABLED` | **true** (configured) |
| `V3_ESCALATION_MAX_ROUNDS` | **absent** — code default `2` |
| `V3_ESCALATION_MAX_DECISIONS_PER_RUN` | **absent** — code default `5` |
| `V3_ESCALATION_COST_CAP_USD` | **absent** — code default `0.0`, i.e. **no cap configured** |

Deployed SHA `0542a24` throughout; `main` is `26cc93e` and the difference is
`docs/DEPLOYMENT.md` only. Nothing was waiting on a deploy.

### 10.3 OBSERVED — Phase E: durable enqueue while execution is impossible

With `V3_DURABLE_JOBS_ENABLED=true` and `V3_JOB_WORKER_IN_PROCESS=false`:

```
POST /api/v1/company-research/jobs {"company_id": "eedb4150-…"}   → 202 in 0.751s
job_id bb1590c5-1087-4fb9-87f5-552028b52366
status pending · stage queued · attempt 0 · max_attempts 3 · analysis_report_id null
```

`attempt`/`max_attempts` are non-null, which is what distinguishes a `research_jobs` row
from a V2 `AgentRun` (the pre-existing V2 job reports both as `null`).

Ten consecutive polls, **15:45:32Z → 15:50:08Z (4m36s)**, every one `pending`,
`attempt=0`, `started_at=null`. Nothing executed. This is empirical, and therefore
stronger than §3's structural argument.

> **An honest note on a false start.** The first POST (15:41:34Z) timed out after 120s
> with zero bytes, during the cold start that follows a restart. It committed **nothing** —
> verified, not assumed: `GET /company-research/jobs?company_id=…` returned the *old* V2
> job `63edb3d1` (`attempt: null`, report `efd4f787`, started 2026-09-18), and that route
> prefers the durable row when the flag is on. Only one job was ever created.

**PHASE E: PASS.**

### 10.4 OBSERVED — Phase F: the worker consumes it exactly once

`V3_JOB_WORKER_IN_PROCESS=true` at 15:51:08Z, restart, **same job**, no replacement:

| Time | State |
|---|---|
| 15:51:27 – 15:54:14 | `pending`, `attempt 0` — B1 cold start, ~3 min |
| 15:54:22.095636 | `started_at` set, `attempt` 0 → **1** |
| 15:54:34 → 15:58:45 | `running`; stage oscillates `council_analysis` ↔ `primary_document_ingestion` (the V3 agentic tool loop) |
| 15:58:53.831174 | `completed_with_warnings`, stage `completed` |

```
analysis_report_id      dc914415-0c82-41d9-9610-5982434ef267
legacy_draft_report_id  0a592afe-8d8f-40fb-8aca-aba35909f0b1
agent_run_id            fd10e2e3-4573-40f8-b4f3-e6693f3fa84c
```

**Exactly once:** one job row, one `started_at`, one `completed_at`, `attempt` never
exceeded 1 (no reclaim), one final report. Two report rows appeared and that is *correct* —
they are the designed draft+final pair, the same shape the 2026-09-18 V2 run produced
(`efd4f787` final + `1e317a19` draft). No duplicate execution across the restart.

**PHASE F: PASS — DURABLE WORKER PRODUCTION ACCEPTANCE.**

### 10.5 OBSERVED — Phase G: regression after worker activation

`/health`, `/api/v1/companies`, `/api/v1/reports`, `/api/v1/sources/health`,
`/api/v1/research-decisions`, `/api/v1/market-discovery/runs`, web `/` — **all 200**.
MRNA still `Healthcare / Biotechnology`, `MRNA` `US`.

### 10.6 OBSERVED — investigator and MRNA invariants, on the NEW report

From `dc914415`, produced by the durable worker this session — not carried forward:

| Metric | Value |
|---|---|
| `responses_total` | 6 |
| `responses_truncated` | **0** |
| `responses_with_findings` | 6 |
| `responses_empty_payload` / `responses_unparseable` | 0 / 0 |
| `responses_retried_after_truncation` | 1 (retried and succeeded) |
| `statements_dropped_uncited` | 0 |
| `fabricated_citations_discarded` | 0 |
| `findings_total` | **17** |
| `finish_reasons` | `{"stop": 6}` |

No `finish_reason=length` anywhere, so **the 1800-token ceiling was not raised**, correctly.

| Invariant | Observed |
|---|---|
| FY2025 revenue $1.944bn | `1.944` ×17 |
| FY2025 operating loss −$3.074bn | `3.074` ×17 |
| FY2025 net loss −$2.822bn | `2.822` ×17 |
| **Stale FY2022 $19.263bn** | **`19.263` ×0**, `FY2022` ×0, `FY2025` ×85 |
| SIC 2836 / T2 tier | `2836` ×3 / `T2_regulator_or_gov` ×36 |

The SEC XBRL stale-alias trap has not returned.

### 10.7 OBSERVED — Phase J: European escalation

`POST /api/v1/market-discovery/runs/ff83c0ce-…/escalate` → 200,
`candidates_considered 2`, `created_count 2`, `refused_count 0`.

| Decision | Company | Identity | Gaps | Round |
|---|---|---|---|---|
| `0e25d3e8-19d1-4410-ab99-d9176940909c` | Pandora A/S | **PNDORA / CO** | 32 blocking | 0 of max 2 |
| `b5516a97-dc5c-43d4-9b45-4fd6a764194c` | Compagnie Financière Richemont SA | **CFR / SW** | 31 blocking | 0 of max 2 |

Both `source: discovery_council`, `decision: research_next`. **Identity resolved by
ticker + exchange**, not ticker alone — `PNDORA/CO` and `CFR/SW`, neither of which is a
US listing.

Both were picked up by the worker and both jobs ran to completion
(`61da2123…` for CFR, `2fd3b9a2…` for PNDORA), each with `job_attempt 1` — **exactly one
job per decision per round.**

### 10.8 OBSERVED — Phase K: idempotency

Second `POST …/escalate` at 16:06:55Z, 22 seconds after the first:

```json
{"candidates_considered": 2, "created_count": 0, "refused_count": 2,
 "refusals": [{"clause": "open_decision_exists",
               "reason": "This company already has an open research decision; a second
                          would be duplicate paid work."}, …]}
```

`research-decisions` total stayed **2**. No duplicate paid work. **PHASE K (sequential):
PASS.** Concurrent duplication was not re-tested in production; the 10-way race is proven
on real PostgreSQL in CI (§5, behaviour 5) and by the partial unique index.

---

## 11. NOT RUN, and why

| Phase | Scenario | State |
|---|---|---|
| I | **MRNA escalation acceptance** | **NOT RUN — no legitimate path exists.** Decision sources in code are exactly `discovery_council` and `gap_carry_forward`; there is no manual/explicit entry point. MRNA appears nowhere in any discovery run's `candidates_to_research_next` (the newest run's bucket holds CFR/SW and PNDORA/CO). Manufacturing a discovery result to obtain MRNA is precisely what the brief forbids. The MRNA *durable-worker* path was proven instead (§10.3–10.4). |
| L | Evidence delta on live rows | **NOT RUN — blocked by §14.** No delta is ever written. |
| M | Round control on live rows | **NOT RUN — blocked by §14.** No round ever completes. |
| N | Rendered Research Queue UI | **PARTIAL.** Route deployed and correctly gated (`/research/queue` → 307 `/login`); `/api/v1/research-decisions` is in the web proxy's `ALLOWED_PREFIXES`; the API it consumes returns truthful state. The rendered page was **not** observed: minting an admin session was denied, and forging an auth token is not something to work around. |

---

## 12. OBSERVED — a second defect: V3 tool calls are never linked to their job

Every run, on **both** paths, emits:

> `v3: tool calls are not linked to a durable job row; the id supplied names no research_jobs row`

**Root cause.** `jobs/company_research_job.py:339` calls
`svc.process_company_research_by_id(uuid.UUID(content_run_id), …)` where `content_run_id`
is the **`AgentRun`** id. `company_research_service.py:779` passes that same value on as
`research_job_id=`, and `v3_pipeline._resolve_research_job_id` looks it up in
`research_jobs`, where an `AgentRun` id can never appear — so the link is dropped and the
warning fires. The real durable id (`ctx.job.id`) is in scope at the call site and is
never threaded down.

**Impact.** `research_job_id` stays NULL on `research_runs`, `research_leads`,
`research_run_consumption`, `calculation_records` and `research_tool_calls`, so V3 tool
calls and per-job cost are unattributable. That touches CLAUDE.md rule 9 (auditability)
and is one reason cost is unknown.

**Not a V3.17 regression** — it affects the V2 path identically and predates this phase.
**Not fixed here:** it is a separate slice, and bundling it with §14 would violate the
one-change-per-PR rule.

---

## 13. KNOWN DEFECT reconfirmed — Chair prints "0 findings" over a populated Ledger

The job carried: *"the chair returned the deterministic verdict because the council did
not convene (no findings to synthesise)"* — while the Ledger for that very report holds
**17** findings (§10.6). Exactly the §9.1 defect, reconfirmed live. **Not repaired here,
per instruction.**

---

## 14. THE DECISIVE DEFECT (RESOLVED in §15) — the autonomous loop did not close

> **Resolved.** V3.17.7 closed this; §15 records the production proof. Kept in full
> because how it survived three merges of green CI is the reusable lesson.

### 14.1 OBSERVED

At **16:28:00Z**, both European jobs had reached a terminal state:

```
PNDORA/CO  dec=queued round=0 job=completed_with_warnings term=None after=None
           updated_at=2026-09-19T16:06:34.870548Z
CFR/SW     dec=queued round=0 job=completed_with_warnings term=None after=None
           updated_at=2026-09-19T16:06:34.412222Z
```

`updated_at` is still each decision's **creation** timestamp. Nothing has touched either
decision since it was created, twenty-two minutes and two completed research runs earlier.

### 14.2 Root cause

An exhaustive grep over `apps/api`:

| Symbol | Where it appears |
|---|---|
| `complete_round` | `controller.py` — its own `__all__`, its own `def`, one docstring. **Plus `tests/test_v3_17_3_escalation_controller.py`.** |
| `recover_stranded_decisions` | the same three places, plus the same test file |

**Neither function has a production caller.** Both are fully implemented and correct, and
both are reachable only from tests that call them directly — which is why three merges of
CI stayed green over an open loop.

`worker.py` is deliberately domain-free (*"nothing here knows what research is"*) and
exposed **no terminal-outcome extension point**, so there was no seam the escalation
domain could have hooked. The wiring is absent rather than wrong.

### 14.3 Consequences

1. `evidence_before_json`, `evidence_after_json`, `improvement_json` are **never written**.
2. `terminal_reason` is **never set**; no decision can reach `exhausted_no_improvement`.
3. **No next round is ever created**; `max_rounds` is never exercised.
4. The decision stays in an OPEN status for ever, and `ux_research_decisions_one_open`
   then forbids **any** further decision for that company. **CFR/SW and PNDORA/CO are now
   permanently locked out of future escalation by rounds that finished successfully.**
   This is the stranding hazard the design documented — now live.

This is the same defect class the campaign has been removing: V3.16, metadata correct and
connected to nothing; V3.17, a council bucket nothing ever read; V3.17.6, the
round-completion function nothing ever calls.

### 14.4 The fix — V3.17.7, implemented on `feature/v3-17-7-close-the-escalation-loop`

* `worker.py` gains a domain-free seam: `register_terminal_observer` /
  `notify_terminal`, called at **both** terminal writes — after `store.complete`, and
  after `store.fail` **only when `will_retry` is false**, so a round about to be retried
  is never announced as ended.
* `escalation/job_hook.py` registers `advance_research_decision`, which finds the OPEN
  decision whose `last_job_id` is that job (`SELECT … FOR UPDATE`) and calls
  `complete_round` with the real `job_completed` / `job_dead_lettered`.
* `jobs/handlers.py` imports it, for the same reason it imports the handlers.
* An observer that raises is logged by type and skipped: the terminal write has already
  happened and must not be undone.

**Mutation-verified, both directions:**

| Mutation | Caught by |
|---|---|
| remove the success-path `notify_terminal` call | `test_a_completed_job_notifies_observers` (drives the **real** `run_once`) |
| remove the registration import from `handlers.py` | `test_importing_handlers_registers_the_escalation_observer` |

The wiring test was itself **wrong on first writing** — it reloaded `job_hook` directly, so
it passed with the import deleted. It now clears the registry, drops both modules from
`sys.modules` *and* deletes the stale attribute from the parent package before importing
`handlers` alone. A test that passes whether or not the thing is wired is the exact defect
class this slice exists to close, so it is recorded rather than quietly corrected.

### 14.5 The hole the observer alone still left — and the durable repair

Pre-merge review caught that §14.4 was **not sufficient**. `notify_terminal` deliberately
swallows whatever the observer raises, the observer runs in its own session after its own
commit, and the job never runs again to notify twice. So:

```
terminal job commits -> observer hits a transient DB error -> exception swallowed
-> decision stays OPEN for ever -> the company is locked out again
```

The same outage, reachable by one dropped socket. The notification is therefore the FAST
path only, and the database — not a callback — is the authority on what still needs
finishing:

* **`controller.reconcile_terminal_decisions`** — open decisions whose `last_job_id`
  names a job in `job_contract.TERMINAL`. It reuses `complete_round` verbatim, and reads
  `HAS_RESULT` / `STATUS_DEAD_LETTER` from the contract instead of restating status
  strings that could drift from the state machine.
* **Rows taken `FOR UPDATE ... SKIP LOCKED`**, with the OPEN re-check performed *after*
  the lock is held.
* **`worker.register_sweep_hook` / `run_sweeps`** — once at startup **before the first
  claim** (a new process may be replacing one that died between a terminal write and its
  notification), then on a 300s cadence. Not the 2s poll: a repair path on a work-queue
  cadence is a hot loop.
* **`recover_stranded_decisions` was also given its first production caller** in the same
  sweep. It repairs the *other* stranding — `last_job_id IS NULL`, where the process died
  after the decision was written and before its job was. Recovery runs first.
* Bounded to 50 rows per pass.

Because it asks the database rather than trusting a callback, it heals decisions stranded
**before this code existed** — which is how CFR/SW and PNDORA/CO are meant to be fixed.
They are deliberately **not** being cancelled; their automatic healing is the acceptance
proof for this failure mode.

**Mutation-verified four ways. Two of these initially did NOT fail**, and the tests were
rewritten until they did:

| Mutation | Caught by | Why the first attempt was invalid |
|---|---|---|
| delete the success-path notification | `test_a_completed_job_notifies_observers` | — (drives the real `run_once`) |
| delete the startup sweep | `test_the_worker_sweeps_BEFORE_it_claims_any_job` | "a sweep ran" passed either way — the in-loop call fires on iteration 1 too. The event **order** is the assertion. |
| delete the in-loop sweep | `test_the_sweep_repeats_on_its_cadence` | "a sweep ran" passed either way — the startup call satisfies it. **Repetition** is the assertion. |
| delete the recover call | `test_the_sweep_also_repairs_decisions_with_no_job` | — |

`SKIP LOCKED` is a no-op on SQLite, so every SQLite test proves the *filtering* and
nothing about the *concurrency*. Two tests in the PostgreSQL lifecycle file prove it where
it is implemented, and **both passed in CI against PostgreSQL 16**: concurrent sweeps
close a decision exactly once, and repeated sweeps never rewrite a closed one.

Those two tests failed on their first CI run with a `ForeignKeyViolation` — they invented
a `discovery_run_id`, which SQLite accepts with foreign keys off and PostgreSQL rejects.
Recorded because it is the trap this repository has hit before, and because the tests
caught a defect in themselves on their first honest run.

**Gates:** local suite **6511 passed / 101 skipped** (baseline 6487/99: +24 new tests,
2 of them PostgreSQL-only and skipped locally); `ruff check` clean; `mypy app` **71 errors
in 10 files — exactly the baseline**; **CI green on `929b8a8`**.

---

## 15. V3.17.7 DEPLOYED — the loop closes, and a lost callback is repaired

**Merged `f4a535d` (PR #209), deployed by run `35465534839`.** Live SHA verified by this
session at **20:00:06Z**: `f4a535dc241956b7121b47b14342b11defed691f`, build
`35465534839`, built `2026-09-19T19:50:05Z`. Migration still **040**
(`/api/v1/research-decisions` → 200). All three flags still `true`.

### 15.1 OBSERVED — durable recovery of the historical stranded decisions

**Neither decision was cancelled or touched.** Both healed by themselves during the new
worker's **startup sweep at 19:59:44Z** — before the SHA even became publicly visible.

| | CFR/SW `b5516a97` | PNDORA/CO `0e25d3e8` |
|---|---|---|
| **Before** (19:49:26Z) | `queued`, r0, term `None`, evidence/improvement `None`, `updated_at` **16:06:34.412** (= creation) | `queued`, r0, term `None`, evidence/improvement `None`, `updated_at` **16:06:34.870** (= creation) |
| **After** (no manual action) | `abandoned`, term **`cost_unknown`**, `updated_at` **19:59:44.407** | `abandoned`, term **`cost_unknown`**, `updated_at` **19:59:44.671** |
| `evidence_after` | `{active_facts 35, indexed_chunks 0, verified_findings 2, open_closable_gaps 14, searchable_documents 0}` | `{active_facts 31, indexed_chunks 0, verified_findings 1, open_closable_gaps 16, searchable_documents 0}` |
| `improvement` | `{facts_added 35, closable_gaps_closed −14, improved true}` | `{facts_added 31, closable_gaps_closed −16, improved true}` |
| job | `61da2123` unchanged, `attempt` still **1** | `2fd3b9a2` unchanged, `attempt` still **1** |

The job ids and attempt counts are unchanged, so **nothing was re-executed** — the sweep
closed the round from the job's existing terminal state. That is the whole claim:
*a callback that was never delivered was repaired from the database.*

### 15.2 OBSERVED — normal terminal-observer progression

Two fresh decisions from a real escalation of run `ff83c0ce` at 20:03:05Z.

| Decision | Job | Job finished | Decision closed | **Gap** |
|---|---|---|---|---|
| `d73b6217` CFR/SW | `5c1e0d5e` | 20:18:47.662 | 20:18:47.955 | **0.293 s** |
| `5c42a0c1` PNDORA/CO | `8f8aaf69` | 20:24:08.x | 20:24:08.x | **0.234 s** |

The sweep cadence is **300 s**. A sub-second gap cannot be a sweep. This is the immediate
observer, and it is therefore proven **distinct from** the recovery path in §15.1 — the
two mechanisms were exercised separately and both work.

Reports produced: `35ebfe08` (CFR), `434a5838` (PNDORA).

### 15.3 OBSERVED — idempotency, round control, exactly-once

* Duplicate escalate at 20:03:37Z → `created_count 0`, `refused_count 2`, clause
  `open_decision_exists`. Total decisions stayed 4.
* A **terminal** decision correctly does **not** block a new escalation — the 20:03:05Z
  call created fresh decisions for the same two companies, whose earlier decisions were
  closed. `ux_research_decisions_one_open` constrains *open* decisions only.
* Every one of the four decisions has exactly **one** `last_job_id` and `job_attempt = 1`.
  No decision ever re-enqueued; no job ever ran twice.
* `max_rounds = 2` was never reached because every decision stops deterministically at
  round 0 — see §15.4.

### 15.4 OBSERVED — why `cost_unknown` and not `exhausted_no_improvement`

All four decisions terminate on **`cost_unknown`**, not `exhausted_no_improvement`.

`rounds.decide_next_state` refuses to authorise a further round while
`cost_usd_so_far is None`, and production reports `estimated_cost_usd` as NULL because no
price book is configured. So the round measured its evidence, **wanted** to continue, and
**failed closed on unknown cost**.

That is a correct deterministic stop. It was **not** forced, and it must not be relabelled
`exhausted_no_improvement` to make the European case look like the predicted outcome —
those are different claims about the world and the platform respectively.

### 15.5 OBSERVED — the European case is honest about SEC coverage

Checked on CFR/SW's report `35ebfe08` with word-boundary matching:

```
\bSEC\b 15   \bEDGAR\b 6   \bCIK\b 2
\b10-K\b 0   \b10-Q\b 0    \baccession\b 0
```

**No fabricated filing evidence.** Every mention is a coverage statement about the
platform, not a risk claim about the issuer:

> `"reason": "not_applicable_jurisdiction"` — *"SEC EDGAR covers issuers registered with
> the U.S. Securities and Exchange Commission. This issuer's venue (SW) is not one of
> them."*
> *"SEC CIK not available for CFR. Company may not be SEC-registered (U.S. only)."*

Zero matches for "absence of SEC filings is a risk" phrasing.

### 15.6 ⛔ NEW MATERIAL DEFECT — the round-0 evidence baseline is never captured

`evidence_before_json` is written at **exactly one place**: `controller.py:396`, inside
`complete_round`, for the *next* round. `store.create_decision` (`store.py:108–122`)
builds the row **without it**. So every round 0 compares against
`EvidenceSnapshot.from_dict(None)` — all zeros.

The arithmetic tell is in production on all four decisions:

```
closable_gaps_closed: -14, -16, -28, -32
```

A negative count of *gaps closed* is impossible; it is `0 − 14`. Likewise `facts_added:
35` counts facts that already existed.

**Consequence.** The first round of every decision reports `improved: true` even if it
acquired nothing, which would authorise a second **paid** round on no acquisition. This is
the "pays for prose" failure the design set out to prevent, in a different form.

**Currently masked**, because `cost_unknown` stops every decision at round 0 regardless.
**It becomes live the moment a price book is configured.** Not fixed here, per the merge
instruction; it needs its own slice, and that slice must land **before** cost data is
configured.

### 15.7 OBSERVED — operational: a container recycle is a worker outage

The two fresh jobs sat `pending`/`attempt 0` from 20:03:07 to ~20:12 (~9 min), across a
manual restart. `az webapp log tail` showed the container repeatedly stopping:

```
State: Stopping, Action: StoppingSiteContainers, LastError: SiteStartupCancelled
Container is terminating. Grace period: 5 seconds.
Container is terminated. Total time elapsed: 11402 ms.
```

**Not a V3.17.7 regression** — the same worker claimed within seconds at 15:54 and 16:06
on the previous SHA, and claimed immediately once this container settled. It is known
defect #2 showing its other face: the in-process worker shares the API container's
lifecycle, so **an API recycle is also a worker outage**. Budget ~10 min of worker
unavailability after any deploy or restart on B1. Moving the worker out
(`V3_JOB_WORKER_IN_PROCESS=false` + a standalone process) removes this with no code change.

### 15.8 OBSERVED — final regression, on `f4a535d`

| Check | Result |
|---|---|
| `/health`, `/companies`, `/reports`, `/sources/health`, `/research-decisions`, `/market-discovery/runs` | **200** |
| web `/` | **200** |
| web `/research/queue` | **307 → `/login`** (admin-gated, correct) |
| MRNA classification | `Healthcare / Biotechnology`, `MRNA` `US` |
| MRNA FY2025 | `1.944` ×17, `3.074` ×17, `2.822` ×17 |
| **Stale FY2022 `19.263`** | **0** (`FY2022` 0, `FY2025` 85) |
| SIC 2836 / T2 tier | 3 / 36 |
| Investigator | 6 responses, **0 truncated**, 6 with findings, `{"stop": 6}`, 0 uncited dropped, 0 fabricated, **17 findings** |

**UI/API contract:** every field the queue needs is present; `cost_usd_total` is `None`
for all four — **unknown cost is rendered as unknown, never as 0**; the internal-use
disclaimer is carried on the list response. The *rendered* page was not observed —
minting an admin session was denied, and forging an auth token is not a thing to work
around. Route, gating and payload are verified; the pixels are not.

---

## 16. Verdicts

**V3.16.1 — INVESTIGATOR→FINDING: PASS.**
Re-verified on `f4a535d`: 6 responses, 0 truncated, 6 with findings, `{"stop": 6}`, 0
uncited statements dropped, 0 fabricated citations, 17 findings. MRNA's FY2025 figures
intact; the stale FY2022 $19.263bn appears **zero** times. The 1800-token ceiling was not
raised, because no production truncation justifies it.

**DURABLE WORKER — PASS.** One persisted job, consumed exactly once, one report, no
duplicate across a restart, `attempt` never exceeded 1.

**V3.17 — RESEARCH ESCALATION: PASS — AUTONOMOUS LOOP PROVEN.**

Both required mechanisms are proven **separately, in production**:

| Requirement | Evidence |
|---|---|
| migration 040 live | 503 → 200 |
| durable queue + worker + escalation flags live | all three `true` |
| real decision persisted | 4, identity by **ticker + exchange** |
| real escalation job consumed | 4, one per decision/round, `attempt 1` |
| exactly-once | no job ran twice; no decision re-enqueued |
| **normal terminal-observer progression** | **0.293 s / 0.234 s** after job finish |
| **durable recovery of a missed callback** | **both historical stranded decisions healed unaided at 19:59:44** |
| evidence delta measured | written for all four — **but see §15.6** |
| deterministic continue/stop | `cost_unknown`, fail-closed, not forced |
| duplicate escalation does not duplicate work | `created 0 / refused 2` |
| European case behaves honestly | no fabricated SEC evidence; coverage stated as coverage |
| UI/API exposes truthful state | fields present; unknown cost is `None`, not `0` |

**V3 RESEARCH ENGINE: READY FOR NEXT PRODUCT PHASE.**

Declared with one condition stated plainly rather than buried: **§15.6 must be fixed
before a cost price book is configured.** Until then the round-0 improvement metric is
over-stated and only `cost_unknown` prevents it from authorising paid work. The engine is
ready for the next phase; it is not ready to be given a budget.

---

## 17. Known defects carried forward

1. **§15.6 — round-0 evidence baseline never captured.** New, material, gating for cost
   configuration. Own slice.
2. **Chair prints "0 findings" over a populated Ledger** (§13). Not repaired, per
   instruction.
3. **V3 tool calls never link to their durable job** (§12) — the `AgentRun` id is passed
   where a `research_jobs.id` is required, so per-job cost is unattributable. This is
   *why* cost is NULL, and therefore sits directly behind §15.6.
4. **In-process worker shares the API container's lifecycle** (§15.7) — a recycle is a
   worker outage.
5. **European filing ingestion remains out of scope** — correctly reported as coverage,
   not as issuer risk.
6. **`/api/v1/research-decisions` has no API-level auth dependency** — pre-existing
   pattern, gated by app-wide Basic Auth.
