# Architecture

## Status: Private-Use Production Readiness — PR-D: Current-Period (Interim) Evidence (**branch, pre-merge**; **no migration**, Alembic head stays `018`; extraction `pipeline_version` `12 → 13`). Every target issuer had published something newer than its last annual report, and none of it could reach a report: interim documents could never survive the per-issuer selection cap, interim figures were stamped with bare years, and an interim figure could occupy the canonical annual slot. Selection is now period- and recency-aware with a reserved current-period slot; prose and table headers recognise interim markers from the value's own local window; and `<field>_primary_filing` (latest ANNUAL) and `<field>_current_period` (latest INTERIM) are separate, explicitly non-comparable slots. Nothing is annualised. See ADR-034.

<details>
<summary>Previous status (Readiness PR-C)</summary>

## Status: Private-Use Production Readiness — PR-C: Richer Canonical Snapshot, Source-Neutral Copy, Per-Company DFR Gaps (merged `99df1b9`; **no migration**, Alembic head stays `018`). The canonical snapshot field set is now DERIVED from the parser's own exported vocabulary (`primary_fact_parser.FINANCIAL_STATEMENT_FIELDS`), so it cannot drift from what is actually extractable — it went from 7 fields to 15, and `total_equity` / `net_cash` / `operating_margin` stopped counting as no fundamental anywhere. US filing vocabulary ("10-K / 40-F", "SEC statement fundamentals") is replaced by jurisdiction-resolved wording. The Deep Field Review pack now states per-company identity completeness so a comparative council cannot generalise one company's missing LEI onto another. See ADR-033.

</details>

<details>
<summary>Previous status (Readiness PR-B)</summary>

## Status: Private-Use Production Readiness — PR-B: Historical Financial Series (merged `8b516e3`; **no migration**, Alembic head stays `018`). The multi-year facts Phase 32D learned to extract now reach a human and the council. `app/services/sources/financial_period.py` can represent `FY2025` and `H1 2026` at once and refuses to compare across period types; `app/services/sources/financial_history.py` builds bounded series keyed by the FULL identity (metric, scope, period type, currency, unit, scale), so a currency mismatch yields two series rather than one bad trend. A new `historical_trends` report section and a bounded 8-line council slice carry it. See ADR-032.

</details>

<details>
<summary>Previous status (Readiness PR-A)</summary>

## Status: Private-Use Production Readiness — PR-A: Persisted Fact Scope (merged `6b7b4cb`; migration `018`, Alembic head `017 → 018`; extraction `pipeline_version` `11 → 12`). Fact scope stops being an in-memory string. `app/services/sources/fact_scope.py` is now the single vocabulary for Group-vs-segment, and `extracted_facts` persists `scope_type` / `scope_name` / `scope_key` so a segment figure cannot lose its attribution across a DB round-trip and be promoted into a canonical Group slot on a cache-reused run. See `docs/PRIVATE_USE_PRODUCTION_READINESS.md` and ADR-031. Production is not provisioned and is untouched.

</details>

<details>
<summary>Previous status (Phase 32D)</summary>

## Status: Phase 32D — Multi-Year Financial Table Extraction (**merged + deployed to STAGING**; API/web `bfac6e1`; PRs #144 #145 #146 #147; **no migration** (Alembic head stays `017`), no schema change, no new flag; extraction `pipeline_version` `9 → 11`). A second, geometry-driven table pass (`app/services/sources/financial_table_reconstructor.py`) rebuilds BORDERLESS multi-year financial tables from a PDF page's positioned words, so a metric row, its value cell and its year/period COLUMN HEADER are associated deterministically before anything is promoted to a fact. `page.extract_tables()` is ruling-line driven and recovers nothing usable from a glossy "Five-year summary" page — on the real 169-page Pandora Annual Report 2025 it returned a degenerate one-column artifact, and every figure on that page reached the pipeline only as flattened prose with its column→year mapping already destroyed. See "Multi-Year Financial Table Reconstruction" below and ADR-030.

**Live staging acceptance:** the real Pandora annual report now yields **52 validated period-scoped facts** (was 2) — revenue, EBIT, EBIT margin, net income, total assets, equity, net interest-bearing debt, operating cash flow, free cash flow and headcount, each for FY2021–FY2025 in `DKK million`, each carrying `page=14` and its own reconstructed-table locator. Richemont regressed nothing (21 facts, was 15; every accepted Group and segment figure intact, and no segment figure reached a Group slot). The Deep Field Review moved Pandora from `second_tier` ("limited financial data restricted to revenue for 2025") to `strongest_candidates`, citing free cash flow, net debt, total assets and equity by name. Production is not provisioned and is untouched.

</details>

<details>
<summary>Previous status (Phase 32D2)</summary>

## Status: Phase 32D2 — One Final Reconciled Research State (**merged + deployed to STAGING**; API `f627666`, web `0584ff1`; PRs #138 #139 #140 #141 #142; **no migration**, no schema change, no new flag). After ingestion and council completion the final-report generator builds ONE reconciled research state (`app/services/final_research_state.py`) and rebuilds every deterministic human-facing surface from it — see "One Final Reconciled Research State" below and ADR-025..ADR-029. Production is not provisioned and is untouched.

</details>

<details>
<summary>Previous status (Phase 31)</summary>

## Status: Phase 31 — Source-Aware Internal Research Memo Builder (**PR open / pre-staging** — NOT merged/deployed/validated; branch `feature/phase-31-research-memo-builder` @ `618344d`; full-stack, **7 files incl. tests**, **no migration** (still DB head `011`), **no new host/endpoint/secret**; ONE new OFF-by-default flag `source_research_memo_enabled` / `SOURCE_RESEARCH_MEMO_ENABLED`). **The FINAL campaign phase** — with the Phase 30 umbrella complete this is the last phase; on merge + staging validation the whole roadmap through Phase 31 is complete. Adds a source-aware **INTERNAL research MEMO builder**, OFF by default. **Backend:** a new `_build_research_memo(report_content, council_result)` in `app/services/final_report_generator.py` performs a **DETERMINISTIC synthesis** of the already-assembled `report_content` sections + `CouncilResult` metadata + known gaps — **no external call, no LLM, no ORM, no recompute** — emitting `report_content["research_memo"]` (attached BEFORE the safety gate) with: `header` (internal-only / not-advice), `company_identity`, `why_surfaced`, `what_is_sourced`, **`what_is_missing` (PROMINENT)**, `primary_evidence_summary` (primary_documents + primary_facts, each cited with token-stripped `source_urls`), `catalyst_event_evidence`, `financial_facts_summary` (T5 EODHD + T1 `*_primary_filing` datapoints; **no derived valuation**), `business_risk_summary`, `council_disagreement_red_team` (the `red_team` agent's dissent + `unsupported_claims` + committee label), `research_next_steps`, `human_review_checklist` (references the EXISTING checklist, no recompute), `source_appendix`, a `disallowed_outputs` notice, plus `note` + `disclaimer` + `human_review_required=True`. **Citation-bound** — each claim ties back to an existing provenance / source / citation. The forbidden **BUY/SELL/HOLD/WATCH** + **price-target / fair-value / upside / downside** literals appear ONLY inside the scanner-exempt `disallowed_outputs` notice field (its key is in `_EXEMPT_FIELD_NAMES`); **every other memo field is safety-clean** (the memo is attached before the safety gate). **Honest degradation** on thin evidence — `what_is_missing` stays prominent, `provenance=missing_data`, sections go honest-empty, and **it never fabricates** a figure or citation. The memo is NOT in `_REQUIRED_SECTIONS` (so `schema_valid` is unaffected); `publication_ready` False; `human_review_required` True; **dark-by-default byte-identical when off**. **Frontend:** `research_memo` is added to `SECTION_ORDER` with an "Internal Research Memo" label (`apps/web/src/components/reports/finalReportContent.ts`); a bespoke `ResearchMemoSection` (`apps/web/src/components/reports/FinalReportRenderer.tsx`) renders the memo's sub-blocks readably in the **Readable** tab (Raw JSON stays the hidden-by-default developer tab; legacy reports without the memo render unchanged); `disallowed_outputs` renders as a plain NOTICE (never a rating / BUY-SELL UI). Verified GREEN (pre-staging): backend **2289 pass / 12 skip / 0 fail**, ruff clean, mypy `71` baseline (no new); web typecheck / lint / build pass; e2e **196/196**; security scan PASS. Internal admin only, no recommendation / valuation outside the exempt notice, `human_review_required=true` / `publication_ready=false`, no publish route, `/admin/*` OAuth-gated, no auth change. **Phase 31 is PR-open — STOP at the merge gate; on merge + staging validation it CLOSES Phase 31 and, as the FINAL phase, completes the entire roadmap through Phase 31.**

</details>

---

## Multi-Year Financial Table Reconstruction (Phase 32D)

An issuer's most information-dense page is usually its **five-year summary** or
its primary financial statements — one metric per row, one reporting period per
column. Those tables are almost always **borderless**: nothing but whitespace
alignment holds them together.

`pdfplumber.Page.extract_tables()` finds tables from RULING LINES, so on such a
page it recovers nothing usable. Measured on the real 169-page Pandora Annual
Report 2025, page 14, it returned `[['2025'], ['32,549'], ['6%'], …]` — a
one-column artifact whose only column is the row-header column
`extracted_fact_validator` deliberately skips, yielding **zero** candidates. The
same page's text still reached the prose path, but FLATTENED: one metric label
followed by five side-by-side values, the column→year mapping already gone. The
validator then correctly refused to promote almost any of it. The refusal was
right; the missing capability was upstream of it.

### The pass

`app/services/sources/financial_table_reconstructor.py` works from the only
representation that still carries the answer — the positioned word boxes
`page.extract_words()` returns:

1. **Rows** — words clustered by `top` only. (`primary_document_extractor.
   _group_words_into_lines` additionally splits a row on any wide gap, which is
   exactly wrong here: in a multi-year table those gaps ARE the columns.)
2. **Header rows** — a row carrying ≥ 2 period tokens (`2025`, `FY2025`, and
   the interim/split-year forms it detects but refuses to promote).
3. **Column groups** — those tokens split into one group per PHYSICAL table, so
   two tables printed side by side never share a header map. Pandora page 14 is
   exactly this: "Financial highlights" and "Stock ratios" share every printed
   row, with token gaps `42.0 / 42.5 / 42.5 / 42.5 / **257.0** / 42.5 / …`.
4. **Qualification** — a group is only a header when its column pitch is
   uniform, its periods are distinct and strictly monotonic, its period types
   are homogeneous, and its header band is clean. The pitch test is what
   rejects a body-text or footnote line that merely names several years.
5. **Column bands** — x-intervals midway between adjacent header centres.
6. **Cell assignment** — each numeric word goes to the band containing its own
   x-centre, and only when it is comfortably clear of both band edges.
7. **Region end** — the table stops where prose intrudes into the value zone.

The result is handed back as a plain header-first grid, so the EXISTING,
already-validated `extracted_fact_validator` machinery consumes it unchanged:
`_column_periods` for the column→year map, `_table_currency_scale` for
`DKK million`, `_match_label` for the metric vocabulary, the subtotal and
balance-sheet cross-checks, scope grouping, and cross-method reconciliation.
The reconstructor decides **layout only** — it never decides what a number
means, never normalizes a value, and never promotes anything to a fact.

### Fail-closed by construction

Every ambiguity produces LESS, never a guess: a value equidistant between two
columns is dropped; two values landing in one column discard the row; an
irregularly-pitched header is not a header; and a period form
`ExtractedFact.period` (a bare year) cannot represent losslessly — interim
`H1 2026`, split-year `2025/26` — is detected, reported as a source gap, and
deliberately **not** promoted rather than flattened onto a fiscal year it does
not mean. Each refusal records a machine-readable reason
(`ambiguous_period_column`, `column_alignment_uncertain`,
`irregular_column_pitch`, …) so an operator can see why a visible number did
not become a fact.

A ruled grid the line-based detector already recovered properly (≥ 2 rows AND
≥ 2 columns) is **not** rebuilt a second time.

### Reaching the page at all

The capability is worthless if extraction never reaches the page the table is
on. Measured live on staging (B1) against the real Pandora annual report:
~1.95s per page, and the previous 20s per-document extraction budget stopped at
page **eleven** — three short of the five-year summary. Extraction was not
failing; it was simply never arriving. Parsing a page IS the cost
(`page.objects`, which `extract_words` / `extract_text` / `extract_tables` all
need first, accounts for essentially all of it), so there is no cheaper
relevance pre-scan available and no per-page work that can be skipped. The
budgets moved instead: per-document extraction `20 → 60`s, per-document total
`45 → 90`s, aggregate ingestion `60 → 180`s. The aggregate was held small
because ingestion once ran inline inside a request bound by the ~230s gateway
timeout; Full Analysis is an async background job now, so that ceiling no
longer applies. These are ceilings, not targets — a document that finishes
early spends nothing.

### Scope

A reconstructed table's scope is resolved ONLY from headings inside or directly
above its own region — never from the page-level running scope, which is
derived from an arbitrary text block and carried across pages, and on the real
Pandora report would have attached a stale segment-shaped label from page 9 to
page 14's Group summary. A segment signal anywhere in the region wins over a
Group one. A page's largest-type SECTION title carries across a contiguous page
run, because a multi-page segment review states "… by segment" once and then
prints only each segment's own name.

### Downstream

Two validator rules changed with it, both in the same bug class:

* A prose candidate that is a **degraded read of a page whose table was
  reconstructed** (same label, page and scope) is superseded by it. The two are
  one printed table read twice, not independent corroboration — so the flattened
  read can no longer manufacture a same-method "conflict" that demotes the
  column-anchored figure.
* Two candidates may only be judged to **CONTRADICT** each other when BOTH are
  fully qualified. A candidate whose currency/scale/period could not be
  established has unknown units; comparing its bare digits against a fully
  specified figure is a category error, and it let an uninterpretable number
  veto a completely specified one.

See ADR-030.


## High-Level System Architecture

```
Browser / User
    ↓
Next.js Frontend (Azure App Service / Static Web App)
    ↓
FastAPI Backend (Azure App Service)
    ↓
┌──────────────────────────────────────────────┐
│ Agent Orchestration Layer (LangGraph)         │
│  Research Team → Analysis Council → Validation│
└──────────────────────────────────────────────┘
    ↓                    ↓                    ↓
Azure OpenAI      Azure AI Search      Azure Blob Storage
(LLM runtime)     (RAG / embeddings)   (documents / PDFs)
    ↓
Azure Database for PostgreSQL
(structured research data, recommendations, audit logs)
    ↓
Azure Application Insights
(monitoring, alerting)
```

---

## Layers

### Frontend (`apps/web/`)
- Next.js 16, React 19, TypeScript, Tailwind CSS v4, App Router
- Public report pages, admin dashboard, user account (V2)
- Communicates with backend via server-side proxy (Phase 17+) — credentials never in browser
- Status: **Phase 23 — admin authentication + allowlist enforced on `/admin/*` and the admin proxy, on top of the Phase 22.3 modern dark glassmorphism UI + safe markdown report preview**
  - `src/proxy.ts` — Next 16 **Proxy** (renamed `middleware`, Node runtime); gates `/admin/:path*` (→ `/login` unauthenticated, `/unauthorized` when not allowlisted) and `/api/admin/proxy/:path*` (401/403); `/`, `/login`, `/unauthorized`, `/api/auth/*`, `/api/version` and static assets stay public
  - `src/lib/auth/*` — dependency-free HMAC-signed httpOnly session cookie (`session.ts`), server-session reader (`server.ts`), forwarded-host/callback URL helpers (`url.ts`)
  - `/api/auth/github` + `/api/auth/callback/github` — GitHub OAuth (secret used server-side only; access token read once for the verified email then discarded); `/api/auth/signout`; `/api/auth/dev-login` — deterministic sign-in gated on `AUTH_TEST_MODE` (local/CI only, 404 in prod)
  - `/login`, `/unauthorized` — public auth pages; the admin shell shows the signed-in identity + Sign out
  - `/api/admin/proxy/[...path]` — server-side proxy route; **independently re-checks admin session + allowlist (401/403)** before adding `Authorization: Basic` server-side; path allowlist; adds advisory `X-IB-Admin-Email`/`X-IB-Admin-Name` audit headers (never the OAuth token); rejects unknown paths; sanitizes errors; never exposes credentials to browser
  - `/api/version` — public build-metadata endpoint (Phase 22.3.1); returns `{ app, commit_sha, build_id, build_time, environment }` for deploy verification; build identifiers only, no secrets; `force-dynamic` + `no-store`
  - `src/lib/api.ts` — smart base URL: server components call `BACKEND_API_BASE_URL` directly; client components use `/api/admin/proxy/…`
  - `/admin` — dashboard (health, company count, latest reports)
  - `/admin/companies/new` — create company form
  - `/admin/analysis` — trigger 19-node workflow; full result display
  - `/admin/reports` — draft report list with review_status column (dynamic rendering restored)
  - `/admin/reports/[id]` — report detail with review action panel + event timeline + **rendered markdown preview**
    - `ReviewPanel` (client component) — interactive review buttons, note textarea, warnings
    - Review event timeline — chronological audit log display
    - `MarkdownReportPreview` (client component) — safe rendered markdown (`react-markdown` + `remark-gfm` + `rehype-sanitize`, no `dangerouslySetInnerHTML`) with a Preview/Raw toggle
  - `/admin/backtesting` — backtesting runs list; create run form; run detail with evaluate/refresh
  - `src/types/api.ts` — TypeScript types (includes ReviewActionRequest, ReviewEvent, BacktestRun, BacktestResult, etc.)

#### UI Design System (Phase 22.3 — presentation only)
Modern dark, glassmorphism design system layered over the existing pages. It changes **presentation only** — no analysis, workflow, or report-generation logic, and no public publishing. Shared primitives under `src/components/`:
  - `ui/AnimatedBackground` — decorative CSS aurora background rendered once at the root layout; `aria-hidden`, `pointer-events:none`, disabled under `prefers-reduced-motion`
  - `ui/GlassCard`, `ui/StatusPill`, `ui/SafetyBanner` — translucent panels, status pills, and the mandatory-disclaimer banner (safety copy passed in verbatim)
  - `ui/AppShell` — admin chrome: top compliance strip, glass nav with active-route highlighting, footer disclaimer
  - `reports/MarkdownReportPreview`, `reports/ReportSectionNav`, `reports/markdownUtils` — the sanitized report preview, sticky mini table of contents, and heading-slug helpers
  - `src/app/globals.css` — fixed dark theme palette, aurora keyframes, dark markdown ("prose") styles, and a `prefers-reduced-motion` block that disables decorative motion

#### Admin Auth + Proxy — Request Flow (Phase 17 proxy, hardened in Phase 23)

```text
Browser → /admin/*  or  /api/admin/proxy/*
  → Next 16 Proxy (src/proxy.ts)
       verify httpOnly HMAC session cookie (AUTH_SECRET)
       no session   → pages redirect to /login?callbackUrl=… ; proxy API → 401
       not allowlisted (ADMIN_ALLOWED_EMAILS) → /unauthorized ; proxy API → 403
  → /api/admin/proxy/[...path] route handler
       re-verify session + allowlist (401/403)   [defense-in-depth]
       validate backend path against allowlist (404 otherwise)
       adds Authorization: Basic <base64(BACKEND_BASIC_AUTH)>   [server-only]
       adds X-IB-Admin-Email / X-IB-Admin-Name (advisory audit; NOT the OAuth token)
  → FastAPI backend
       checks STAGING_BASIC_AUTH; reads X-IB-Admin-* only AFTER Basic Auth passes
  → response forwarded back to browser   (Authorization header stripped)

Sign-in:  /login → /api/auth/github → GitHub OAuth → /api/auth/callback/github
          (server-side token exchange; access token discarded after reading the
          verified email) → sets ib_admin_session cookie → back to callbackUrl.
```

Required App Service env vars for `ib-stg-web` (server-only, no `NEXT_PUBLIC_` prefix):
- `AUTH_SECRET` — signs the admin session cookie (Key Vault ref)
- `ADMIN_ALLOWED_EMAILS` — comma-separated admin allowlist
- `AUTH_GITHUB_ID` / `AUTH_GITHUB_SECRET` — GitHub OAuth App (secret in Key Vault)
- `AUTH_TRUST_HOST=true` (+ optional `AUTH_URL`) — resolve OAuth redirect URIs
- `BACKEND_API_BASE_URL` — full URL of the FastAPI backend
- `BACKEND_BASIC_AUTH` — `user:password` matching `STAGING_BASIC_AUTH` on the API
  (now server-to-server defense only; the browser authenticates first)

#### Web Deploy Cache Hardening (Phase 22.3.1)
`ib-stg-web` runs the Next.js standalone bundle with `WEBSITE_RUN_FROM_PACKAGE=1` and `alwaysOn=false`. Under those settings a **statically prerendered homepage `/` could keep serving the previous build** after a deploy until a manual `az webapp restart` flushed it (dynamic routes like `/admin` picked up the new build immediately). Phase 22.3.1 hardens `deploy-web-staging.yml` so a stale homepage is prevented and, if it ever occurs, is caught loudly:

- **Build metadata baked into the bundle** — the workflow injects `NEXT_PUBLIC_COMMIT_SHA` / `NEXT_PUBLIC_BUILD_ID` / `NEXT_PUBLIC_BUILD_TIME` / `NEXT_PUBLIC_APP_ENV` at build time. Next.js statically inlines `NEXT_PUBLIC_*`, so the values are available at runtime on App Service with no runtime configuration. `src/lib/build-info.ts` reads them (with a runtime `COMMIT_SHA` fallback and safe `"unknown"` placeholders).
- **`/api/version` endpoint** — exposes the baked build metadata (build identifiers only, never secrets) so the deployed web commit can be verified from the app itself.
- **`x-ib-build-commit` meta tag** — the root layout embeds the build commit into every page's `<head>`, so a stale prerendered `/` is detectable by comparing the served commit to the deployed SHA.
- **Homepage rendered dynamically** — `src/app/page.tsx` is `force-dynamic`, so `/` always reflects the currently-mounted bundle instead of a cached prerender.
- **Post-deploy restart (best-effort, optional)** — the workflow restarts `ib-stg-web` after deploy when an `AZURE_CREDENTIALS` service principal is configured. A true restart requires the Azure ARM API (Kudu / publish profile cannot restart the site), so with only a publish profile the step is skipped cleanly. Provision `AZURE_CREDENTIALS` (Website Contributor on `ib-stg-web`) once RBAC/OIDC is granted to automate it.
- **SHA-verified smoke check** — the deploy is confirmed only when `/api/version` reports the deployed `github.sha` (multiple consecutive matches), `/` and `/admin` return `200` and contain the dark-UI marker (`bg-[#060913]`), and `/` embeds the current build commit. A `403` ("Site Disabled") is surfaced explicitly rather than treated as transient. This never silently false-greens on a stale worker.

### Backend (`apps/api/`)
- FastAPI, SQLAlchemy async, Pydantic v2, Alembic
- All business logic, database operations, agent orchestration triggers
- Status: **company endpoints + workflow trigger live in Phase 2**

### Agent Layer (`apps/api/app/agents/`, `apps/api/app/workflows/`)
- LangGraph `StateGraph` workflows
- Four agent teams: Research, Analysis Council, Validation, Judge
- All runs logged to `agent_runs` and `agent_steps` tables
- Status: **Phase 22.1 — `company_analysis` is a 19-node workflow (v6.0.0) with 4 Research Team + 5 Analysis Council + 1 Scoring agents (all deterministic), 1 optional LLM node, and full source/citation tracking. `investment_committee_chair` forces `human_review_required=True` when safety guard triggers (safety fix 2026-07-12). `TrendSignalEngine` exists but not yet wired in (Phase 19.2). `BacktestingService` + `ResearchJudgeService` live (Phase 22). Admin Backtesting UI live (Phase 22.1).**
- Research Team agents (Phase 8, `apps/api/app/agents/research_team/`):
  - `financial_data_agent.py` — classifies available vs missing financial data; source tier accounting
  - `source_quality_agent.py` — T1–T6 source classification; enforces T5 providers never promoted
  - `research_completeness_agent.py` — schema-driven gap analysis; blocking vs non-blocking gaps
  - `citation_validator_v2.py` — checks DB citations AND schema draft datapoints; flags bare numbers
- Analysis Council agents (Phase 9, `apps/api/app/agents/analysis_council/`):
  - `bull_case_agent.py` — positive thesis points, sector tailwinds, evidence, assumptions; forbidden word gate
  - `bear_case_agent.py` — negative thesis points, headwinds, key unknowns; challenges bull case
  - `risk_agent.py` — 6-category risk classification; always includes data-quality and source-quality risks
  - `valuation_guard_agent.py` — blocks valuation when mock/T5/T6 data; no price target ever produced
  - `investment_committee_chair.py` — synthesises council; quality gate; assigns provisional_internal_status from whitelist only
- Scoring agent (Phase 15, `apps/api/app/agents/analysis_council/`):
  - `score_research_attractiveness.py` — Node 17; deterministic 10-dimension research attractiveness scorecard; non-fatal; no price targets; no recommendations; T6/mock ≤ 30, T5 ≤ 60, T1/T2 ≤ 100
- Final Report Generator (Phase 16, `apps/api/app/services/final_report_generator.py`):
  - `FinalReportGeneratorService` — assembles a 19-section internal draft from scorecard/candidate/company/report inputs
  - **Phase 28A.1 `generate_from_workflow_state`** — the entry point the discovery **"Run Full Analysis"** flow (`market_discovery_service.run_candidate_analysis`) uses: it feeds the company-analysis workflow's rich in-memory final state straight into `_generate_and_save` (running the Phase 28A council when enabled). This is what makes a candidate link to a real final report — carrying a `final_report_version` and titled *LLM Council Analysis Draft* / *Internal Analysis Draft* — instead of the legacy deterministic **"Phase 9 Analysis Council Draft"**. Saved reports without a `final_report_version` are legacy drafts; the admin UI badges and labels the two distinctly. Generation failure degrades to linking the deterministic draft with a warning
  - Safety gate (`run_safety_gate`) — scans all section text for forbidden recommendation language; `blocks_approval=True` on any hit; exempt-field list prevents false positives from meta-documentation fields. Since Phase 27.1A the matching itself lives in `app/services/safety_terms.py` (see below) — this module no longer defines its own term list
  - **Phase 28A LLM council hook** — before validation, `_generate_and_save` calls `app.services.llm.council.maybe_run_council`. When `LLM_COUNCIL_ENABLED` and a provider resolves, it builds a bounded, cited evidence pack and runs the council; the council section is added to `report_content` so the existing safety gate scans it (a backstop on top of the council's own per-agent quarantine). When disabled/unavailable it returns `llm_used=false` and the deterministic content is unchanged

- LLM Analysis Council (Phase 28A, `apps/api/app/services/llm/`):
  - **Internal-only, citation-bound, safety-gated, OFF by default.** `get_llm_client` returns `None` (never raises) when the flag is off or credentials/deps are missing, so the report honestly reports `llm_used: false` and no fake output is produced
  - **Evidence pack** (`evidence_pack.py`) — bounded (`LLM_COUNCIL_MAX_EVIDENCE_ITEMS`), excerpts-only, stable ids `E1..En`. Records **`transport_tier` vs `content_tier`**: SEC EDGAR is a T2 transport, a filing pulled through it is T1 content (`provider_transport="SEC EDGAR / data.sec.gov"`); company press releases are `T1_primary_company_source`. Agents may cite ONLY evidence ids
  - **Eight agents** (`council.py`, run in order): Financial Analyst, Business/Moat, Catalyst, Risk/Governance, Valuation Guard (a guard — never a valuator), Source Quality Critic, Red Team, Committee Chair (labels limited to `internal_research_candidate`/`requires_more_evidence`/`insufficient_data`/`monitor_for_new_evidence`/`reject_for_now`). A single agent failing (timeout/malformed JSON/provider error) is isolated as `failed`; the report still saves
  - **Citation + safety enforcement** (`citation_checker.py`) — drops non-pack citation ids, moves un-cited material claims to `unsupported_claims`, and **quarantines** any agent output containing forbidden rating/valuation language (no forbidden term is ever echoed forward, not even into notes). The shared `safety_terms` scanner is the single source of truth
  - **Safe logging** — `llm_council_started`/`evidence_pack_built`/`llm_agent_completed`/`llm_agent_failed`/`llm_council_completed` carry ids/provider/model/status/counts/durations only; prompts, completions, evidence excerpts and credentials are never logged. Phase 32A TPM slice (ADR-020): agent events additionally carry prompt/completion/total token counts + a bounded `retry_after_seconds`, and every council emits a run-summary event (`llm_council_run_summary` / `discovery_council_completed` / `field_review_council_completed`) with token totals, 429 count, retries, paced wait, chair attempts and `chair_synthesis_basis`
  - **TPM-aware pacing (ADR-020)** — all three councils share ONE process-local sliding-window token pacer per Azure deployment (`app/services/llm/token_pacer.py`, enabled via `LLM_COUNCIL_TPM_CAPACITY`, advisory by design) with an explicit chair token reserve; the chair's prior-summary input can be deterministically compacted (`LLM_COUNCIL_CHAIR_PRIOR_SUMMARY_MAX_CHARS`). Every council result records `chair_synthesis_basis` (`llm_chair` vs `deterministic_fallback`) + `chair_error_type`, so a provider failure can never masquerade as an evidence-based `insufficient_data` judgement

- LLM Discovery Council (Phase 28B, `apps/api/app/services/llm/`):
  - **Run-level analog of the Phase 28A single-company council** — reviews a whole discovery run's candidate set and decides internal research **priority**. **Internal-only, citation-bound, safety-gated, OFF by default**, gated by **both** `LLM_COUNCIL_ENABLED` (the shared 28A client gate) and `LLM_DISCOVERY_COUNCIL_ENABLED` (new, default `false`). `get_discovery_llm_client` returns `None` (never raises) when either flag is off or no provider resolves, so no fake output is produced in production and the deterministic discovery result is unchanged. **Manual admin-triggered only** — never runs automatically after a discovery run
  - New files reuse the 28A `client.py`/`safety_terms`/structured logging: `discovery_schemas.py`, `discovery_evidence_pack.py` (`build_discovery_evidence_pack`), `discovery_prompts.py`, `discovery_citation_checker.py`, `discovery_council.py` (`get_discovery_llm_client`, `run_discovery_council`, `maybe_run_discovery_council`), `fake_discovery_client.py` (deterministic, offline — the ONLY provider used in tests)
  - **Run evidence pack** (`build_discovery_evidence_pack`) — bounded (`LLM_DISCOVERY_COUNCIL_MAX_CANDIDATES`, default 25), cited, no raw report bodies / no secrets: run-level facts get ids `R1, R2, …`; each candidate gets `C1, C2, …` (carrying `score_breakdown`, `data_coverage` `profile_source`/`fundamentals_source`/`sec_eligible`/`reason`/`requires_human_research`, `catalyst_summary`, `safety_valid`, `warnings`). Agents may cite ONLY those ids
  - **Eight agents** (`discovery_council.py`, run in order): `run_coordinator`, `candidate_prioritization`, `novelty_coverage`, `diversity_anti_convergence`, `evidence_sufficiency`, `risk_gatekeeper`, `run_red_team`, `discovery_chair` (chair runs last, over the prior agents' safety-scanned summaries). Only per-candidate internal actions: `research_next`/`monitor_for_evidence`/`insufficient_data`/`reject_for_now`; only `run_quality`: `strong`/`adequate`/`thin`/`failed`. One failing agent never fails the review
  - **Citation + safety enforcement** (`discovery_citation_checker.py`) — drops invalid citation ids, moves un-cited material claims to `unsupported_claims`, quarantines any forbidden-action agent output (`status=failed`, no forbidden term echoed forward — the quarantine note records tier names, not terms), coerces bad `internal_action`/`run_quality` to safe defaults; a final backstop re-scan runs before storing
  - **Service integration** — `market_discovery_service` adapts the ORM run + candidates into dicts, calls `maybe_run_discovery_council`, and stores the review under `config_json["discovery_council"]` (reassigned whole to trigger JSONB dirty-tracking — no migration). Two admin-only endpoints on the market-discovery router (`POST`/`GET .../runs/{run_id}/council-review`) surface it; `/admin/discovery` renders a "Discovery Council Review" panel
  - **Async execution (Phase 28B.2)** — `POST .../council-review` no longer blocks on all eight LLM calls. `start_discovery_council_review` writes a `pending` **status envelope** and returns immediately; a FastAPI `BackgroundTask` (`process_discovery_council_task` → `process_discovery_council_by_id`) opens its **own fresh DB session**, transitions the job `running`, runs the (sequential, per-agent-isolated) council, and persists a terminal envelope (`completed`/`completed_with_warnings`/`failed`) — every failure path stores a terminal status so a job never sticks in `running`. The envelope wraps the Phase 28B review (`status`/`started_at`/`completed_at`/`llm_used`/`agents_completed`/`agents_failed`/`safety_valid`/`error`/`review`); `get_council_envelope` normalises legacy raw reviews. `GET .../council-review` is the poll endpoint and a completed review survives the flags being turned off. `BackgroundTasks` are **process-local, not a durable queue** — same limitation as Phase 25.1 async discovery runs (future work: Azure Queue / Celery / a durable worker). Mirrors the Phase 25.1 `create_pending_run` + `process_discovery_run_task` pattern
  - **Safe logging** — `discovery_council_started`/`discovery_council_evidence_built`/`discovery_council_agent_completed`/`discovery_council_agent_failed`/`discovery_council_completed`/`discovery_council_disabled`/`discovery_council_failed` plus the 28B.2 job events `discovery_council_job_queued`/`discovery_council_job_started`/`discovery_council_job_completed`/`discovery_council_job_failed`/`discovery_council_job_duplicate` carry ids/status/provider/model/counts/durations only; prompts, completions, evidence and secrets are never logged

- Deep Field Review (Phase 32A Slice 6D, `apps/api/app/services/llm/` + `apps/api/app/services/field_review_*.py`):
  - **The THIRD council, and deliberately distinct from the other two.** The Discovery Council (28B) triages a run's **candidate list** *before* any full analysis exists; the Company Council (28A) analyses **one** company; the Deep Field Review runs *after* **2+ candidates from the SAME discovery run already have a COMPLETED full analysis** and compares those **already-persisted reports** to produce an internal **research-priority shortlist**. It never re-analyses, re-fetches, or recomputes anything. **Internal-only, citation-bound, safety-gated, OFF by default**, gated by **both** `LLM_COUNCIL_ENABLED` and `LLM_FIELD_REVIEW_COUNCIL_ENABLED`. **Manual admin-triggered only**
  - New files mirror the 28B trio's shape and reuse `client.py`/`safety_terms`/structured logging: `field_review_schemas.py`, `field_review_prompts.py`, `field_review_citation_checker.py`, `field_review_council.py` (`get_field_review_llm_client`, `run_field_review_council`, `maybe_run_field_review_council`), `fake_field_review_client.py` (deterministic, offline — the ONLY provider used in tests), plus `services/field_review_evidence_pack.py` (pack builder) and `services/field_review_service.py` (input resolution + async job)
  - **Input resolution** — candidates are resolved for ONE `discovery_run_id` via `discovery_candidates.analysis_report_id` **only**. There is deliberately **no** "latest report for this company_id" fallback: that would resurrect the Phase 32A from-company scoping bug and could silently substitute a report from a *different* run of the same company. Every non-comparable candidate is **recorded** with a closed-vocabulary reason (`no_analysis_run`/`report_deleted`/`draft_only`/`not_schema_valid`/`over_company_cap`), never dropped; mock/unknown-provenance companies are **included with a caveat**, never excluded. Below `FIELD_REVIEW_MIN_CANDIDATES` (default 2) the API returns **422** and the council never runs
  - **Comparative pack** (`build_company_summary` / `build_field_review_pack`) — bounded (`LLM_FIELD_REVIEW_COUNCIL_MAX_COMPANIES`, default 12), built from persisted data ONLY, reusing the existing `_extract_from_report_content` report parser and `primary_document_view_service`'s coverage summary. Run facts get ids `R1, R2, …` (including one per excluded candidate); each company gets `F1, F2, …`. Only the **qualitative** `valuation_readiness` label crosses over — never a valuation number. Every list-valued sub-field is capped
  - **Eight comparative agents** (run in order): `comparative_financial_quality`, `thematic_relevance_materiality`, `comparative_business_quality_moat`, `comparative_catalysts`, `comparative_risk`, `comparative_evidence_source_quality`, `field_red_team` (sees prior summaries), `field_chair` (last, sees everything). The chair returns **three internal research buckets only** — `strongest_candidates`/`second_tier`/`blocked_insufficient_evidence` — plus `field_uncertainties` and `field_quality` (`strong`/`adequate`/`thin`/`failed`). No rating or action vocabulary exists in the schema
  - **Citation + safety enforcement** (`field_review_citation_checker.py`) — an agent that trips the safety scanner or cites an id outside the pack is **quarantined** (`status=failed`), never sanitized-and-passed; ungrounded and duplicate chair placements are dropped; a defensive re-scan before persistence forces `safety_valid=false` rather than stripping
  - **Bounded retries** — an initial pass under a total wall-time deadline (`LLM_FIELD_REVIEW_COUNCIL_TOTAL_BUDGET_SECONDS`, default 600 — larger than the inline 28A budget because this is a background job), then a priority-ordered retry pass for **transient** failures only (429/5xx/timeout via `is_transient_llm_error`), with per-agent attempt caps, capped honored `retry-after`, capped jittered backoff, and a reserve protecting `field_red_team` + `field_chair`. `clock`/`sleeper`/`rng` are injectable for deterministic tests. No unbounded loop
  - **Async execution + persistence** — `start_field_review` (idempotent; returns an in-flight/completed job unless `force=true`) writes a `pending` row; a FastAPI `BackgroundTask` (`process_field_review_task` → `process_field_review_by_id`) opens its **own fresh DB session** and always persists a terminal row (`completed`/`completed_with_warnings`/`failed`/`insufficient_candidates`), so a job never sticks in `running`. Results live in `field_review_runs` + `field_review_candidate_summaries` (**migration `015`**). Two admin-only endpoints (`POST`/`GET /api/v1/discovery-runs/{run_id}/field-review`) surface it; `/admin/discovery` renders a clearly-distinct **"Deep Field Review"** panel below the Discovery Council panel
  - **Safe logging** — `field_review_job_queued`/`_started`/`_completed`/`_failed`/`_duplicate`, `field_review_pack_built`, `field_review_insufficient_candidates`, `field_review_council_started`/`_completed`/`_failed`, `field_review_agent_completed`/`_failed`/`_retry`/`_retry_skipped` carry ids/status/counts/durations/backoff only; prompts, completions, report bodies, pack text and secrets are never logged

- Source Registry + Connector Framework (Phase 29A, `apps/api/app/services/sources/`):
  - **Why:** the LLM councils (28A/28B) need richer evidence; this builds the unified source/provider framework **before** wiring many external sources one by one. Framework only — no live connector is wired into the report/discovery evidence flow in 29A (that is Phase 29B+); the councils still read the existing deterministic evidence packs
  - `taxonomy.py` — canonical **source tiers** (`SourceTier` T1–T6) with the **transport-vs-content** rule: `sec_tier_pair()` returns `(T2_regulator_or_gov, T1_primary_filing)` — SEC EDGAR is a regulator *transport*, a filing pulled through it is primary *content*. Also `ProviderType`/`CostModel`/`AccessMode`/`SourceStatus`/`ConnectorStatus` vocabularies. A test asserts these tier strings stay consistent with `integrations.financial_data_provider.SourceTier` and the `llm.schemas` tier constants
  - `evidence.py` — framework `EvidenceSource` + `EvidenceItem` models. Every item **requires** a valid `content_source_tier` (evidence with no provenance is rejected) and records `provider_transport_tier` separately; `url` is passed through `strip_url_secrets` (credential-bearing query params dropped) and `excerpt` is bounded. `to_council_item()` adapts an item into the council pack shape without the framework importing the LLM layer
  - `redaction.py` — reuses the tested `core.log_redaction` and adds `strip_url_secrets` (drops secret query params entirely for stored evidence) + `url_has_secret`
  - `connector_base.py` — the `SourceConnector` interface (`search_company` / `fetch_filings` / `fetch_events` / `fetch_macro_context` / `healthcheck`) with safe defaults. Unimplemented methods return an empty result; a planned/disabled connector returns a `SourceGap` (never `NotImplementedError`); `call_safe()` converts any exception into a warning + gap so **a connector failure never crashes a report or discovery run**. `ConnectorHealth`/`ConnectorResult` are safe, secret-free shapes
  - `registry.py` — the code-defined `SourceRegistry`: **35 enabled/live** sources (`sec_edgar`, `company_ir`, `gleif`, `eodhd`, `stooq`, `gdelt`, plus five regulated-disclosure **regulator reference** connectors — **Phase 29B.4A** `uk_fca_nsm` (UK FCA NSM/RNS), **Phase 29B.4B** `euronext_regulated_info` (Euronext Paris/Amsterdam regulated-information + AMF/AFM), and **Phase 29B.4C** `deutsche_boerse` (Bundesanzeiger / Deutsche Börse / BaFin), `nordic_disclosures` (Nasdaq Nordic / Finanstilsynet), and `six_swiss` (SIX Swiss Exchange / SIX Exchange Regulation) — each `T2_regulator_or_gov`, emitting a bounded venue reference + an honest content gap, no live content fetch yet, honest reliability note "T1 content not fetched at report time — Phase 29B.4 follow-up"; plus fifteen **Phase 29C** reference-only **macro / commodity-energy / policy-government** sources built from `ALL_MACRO_SOURCES` — **29C.1 macro:** `fred` / `imf` / `eurostat` / `national_stats_central_banks` (`macro_statistics`) and `world_bank_pink_sheet` (`commodity`), each `T2_regulator_or_gov`; **29C.2 commodity + energy:** `usgs` / `iea` / `irena` / `entsoe` (`commodity`, `T3_industry_specialist`) and `eia` (`commodity`, `T2_regulator_or_gov`); **29C.3 policy + government:** `ustr_taric` / `un_comtrade` (`trade_policy`, `T2_regulator_or_gov`; promoted from planned) + `nato` (`trade_policy`, `T2_regulator_or_gov`) / `sipri` (`trade_policy`, `T3_industry_specialist`) / `oecd` (`macro_statistics`, `T2_regulator_or_gov`) — each emitting a bounded `macro_report` SOURCE REFERENCE + an honest `data_not_sourced` gap, no live figures fetched, OFF by default behind `source_macro_enabled`; note `ProviderType` has no `government_data` member so the policy/government sources reuse `trade_policy`/`macro_statistics`; plus two **Phase 29D.1** reference-only **event-trigger** sources `eu_ted` (`ted.europa.eu`) + `usaspending` (`usaspending.gov`), both `procurement` / `T2_regulator_or_gov` and promoted from planned — each emitting a bounded event `SOURCE REFERENCE` (`source_type="government_data"`, no specific award/tender) + an honest `data_not_sourced` gap, no live figures fetched, OFF by default behind `source_event_enabled`; plus three **Phase 29D.2** reference-only **patent** event-trigger sources `google_patents` (`patents.google.com`, **T5** aggregator index) + `uspto` (`uspto.gov`, **T2**) + `epo_espacenet` (`worldwide.espacenet.com`, **T2**), all provider `patents` and promoted from planned — each emitting a bounded **T2/T5** patent `SOURCE REFERENCE` (`source_type="government_data"`, **purely thematic**, no specific patent number/title/inventor/assignee/claim/date, **NO legal/infringement/validity conclusion**) + an honest `data_not_sourced` gap, no live fetch, reusing `source_event_enabled`; plus three **Phase 29D.3** reference-only **permit / regulatory-event** sources `ferc` (`ferc.gov`, energy/grid/transmission/pipeline/LNG) + `us_nrc` (`nrc.gov`, nuclear licensing) + `us_epa` (`epa.gov`, environmental/emissions/industrial permitting), all provider `permits` (a NEW `ProviderType.permits`) / **T2** and **new (not promoted)** — each emitting a bounded **T2** `government_data` permit `SOURCE REFERENCE` (**purely thematic**, no specific docket/case/permit number/applicant/decision/outcome/date, **NO regulatory-outcome/approval/materiality conclusion**) + an honest `data_not_sourced` gap, no live fetch, reusing `source_event_enabled`; bare "industrial" deliberately excluded to avoid a GICS-Industrials collision with the 29D.1 defense path; plus one **Phase 30B** local-language business-press reference `local_language_business_press` (`news` / `T4_quality_media`, `PHASE_30B`, promoted from planned) — for a verified non-US FR/DE/IT/DA issuer it emits ONE bounded `news_article` SOURCE REFERENCE to a reputable local-language venue (Les Échos FR / Handelsblatt DE / Milano Finanza IT / Børsen DK, fixed public HTTPS landing page, no API key) carrying a GENUINE local-language descriptive excerpt (never a fabricated article/headline/quote/figure/date) marked `requires_translation` + `original_language` + honest content-not-fetched gap + `needs_human_review`, network-free, OFF-by-default behind `source_connector_enabled` and consumed by the Phase 30A translation layer), **2 scaffolded** filing/regulator connectors (Phase 29B — SEDAR+/ASX; class exists, honest gaps, no live fetch) + **1 planned placeholder** disabled by default (`openbb`). `summary()` reports `enabled`/`configured`/`scaffolded`/`planned`/`disabled`/`total`. `assert_registry_safe()` serialises the whole payload and fails on any secret-like token (a backstop; entries store no credentials). Health is deterministic/network-free (e.g. `eodhd` → `configured`/`not_configured` from settings, never revealing the key)
  - `connectors/` — `PlannedConnector` (planned placeholders), `WrappedProviderConnector` (migrated providers, health from config), `SecEdgarConnector` (Phase 29B: `fetch_filings` is exchange-aware — non-US → `source_not_eligible` gap — and maps an injected fetcher's output into T2-transport/T1-content evidence items + a `primary_filing_unavailable` full-text-pending gap; offline-testable), `CompanyIrConnector` (Phase 29B/**29B.1**: `search_company`→`company_ir_profile`, `fetch_filings`→`company_ir_annual_reports_index` + live-extracted `company_ir_annual_report` (`T1_primary_filing`), `fetch_events`→`company_ir_press_release_index` + replayed/live `company_ir_press_release`; for a verified issuer it emits `data_quality=metadata_only` T1 company-source evidence with **no network call**, URL secrets stripped, bounded, media URLs never cited), `UkFcaNsmConnector` (**Phase 29B.4A**: for a verified UK-regulated LSE issuer emits ONE bounded T2-transport `uk_fca_nsm_disclosure_reference` `EvidenceItem` citing the fixed FCA NSM/RNS venue URL — no query, no fabricated filing/headline/date/RNS number — plus a `primary_filing_unavailable` gap that T1 filing content is not fetched at report time; network-free, non-UK/non-verified → honest gap), `EuronextRegulatedConnector` (**Phase 29B.4B**: for a verified Euronext Paris FR (`MC.PA`/`RMS.PA`/`KER.PA`) or Amsterdam NL (`ASML.AS`) issuer emits ONE bounded T2-transport source reference to the Euronext regulated-information venue + country regulator (AMF/AFM) at the fixed public URL `https://www.euronext.com/en/regulated-information` — no query, no fabricated filing/headline/date/notice number — plus a `primary_filing_unavailable` content gap; French-jurisdiction issuers additionally carry a `requires_translation` (`GapType.translation_required`) marker; network-free, non-Euronext/non-verified → `source_not_eligible` gap), `DeutscheBoerseConnector` (**Phase 29B.4C**: for a verified German/Xetra issuer (`SAP.DE`) emits ONE bounded T2-transport source reference to the German regulated-information venue — Bundesanzeiger / Deutsche Börse / BaFin — at the fixed public URL `https://www.bundesanzeiger.de` (no query, no fabricated filing/headline/date/notice number) + a `primary_filing_unavailable` content gap + a German `requires_translation` (`GapType.translation_required`) marker; network-free), `NordicDisclosuresConnector` (**Phase 29B.4C**: for a verified Nordic issuer (`PNDORA.CO`, Nasdaq Copenhagen; generalizes to ST/HE/OL) emits ONE bounded T2-transport reference to the Nasdaq Nordic company-news venue `https://www.nasdaqomxnordic.com/news/companynews` + Finanstilsynet + a `primary_filing_unavailable` gap + a Danish `requires_translation` marker; network-free), `SixSwissConnector` (**Phase 29B.4C**, a NEW `six_swiss` source — no Swiss scaffold existed: for a verified SIX-listed issuer (`CFR.SW` Richemont, `UHR.SW` Swatch) emits ONE bounded T2-transport reference to the SIX Swiss Exchange / SIX Exchange Regulation official-notices venue `https://www.six-group.com/…/official-notices.html` + a `primary_filing_unavailable` gap; **NO `requires_translation` claim** — Switzerland is multilingual and major issuers publish English, so only a neutral DE/FR/IT multilingual note in warnings; network-free), `MacroReferenceConnector` (**Phase 29C.1 + 29C.2 + 29C.3**, generic — one instance per `ALL_MACRO_SOURCES` spec, i.e. the 29C.1 macro publishers plus the 29C.2 commodity / energy references USGS/EIA/IEA/IRENA/ENTSO-E plus the 29C.3 policy / government references USTR-TARIC/UN Comtrade/NATO/SIPRI/OECD: `fetch_macro_context` emits ONE bounded **T2/T3** `macro_report` SOURCE REFERENCE (fixed public landing URL + which datasets it covers) + an honest `data_not_sourced` gap for a relevant theme/region; **no figures / index levels / tonnage / capacity / production / reserves / release dates / forecasts**, network-free, no API key; `fetch_filings`/`fetch_events` return an honest not-eligible gap — a macro source is never a company filing), `EventReferenceConnector` (**Phase 29D.1 + 29D.2 + 29D.3**, `connectors/event_reference.py` — one instance per `ALL_EVENT_SOURCES` spec: 29D.1 procurement `eu_ted`/`usaspending` + 29D.2 patents `google_patents`/`uspto`/`epo_espacenet` (a per-kind `_EventFlavor` makes patent refs purely thematic) + 29D.3 permits `ferc`/`us_nrc`/`us_epa` (a per-kind `_PERMIT_FLAVOR` makes permit refs purely thematic, all provider `permits`, T2): `fetch_events` (previously a dead hook) emits ONE bounded **T2/T5** `government_data` event `SOURCE REFERENCE` (fixed public procurement/tender/patent/permit landing URL + which venue it covers) + an honest `data_not_sourced` gap for a relevant theme; **no specific tender / award / contractor / amount / contract-number / date — nor patent number / title / inventor / assignee / claim / filing-or-grant date — nor docket / case / permit number / applicant / decision / outcome / date**, network-free, no API key; labelled a **WEAK** research-priority signal carrying `needs_human_review` + `stale_after_days` freshness; not a materiality claim / candidate / catalyst / trade signal, and (patents) **no legal / infringement / validity / patentability / ownership conclusion** / (permits) **no regulatory-outcome / approval / materiality conclusion**; `fetch_filings`/`search_company`/`fetch_macro_context` return an honest not-eligible gap — an event source is never a company filing), `LocalLanguagePressConnector` (**Phase 30B**, `connectors/local_language_press.py` — for a verified non-US FR/DE/IT/DA issuer emits ONE bounded **T4_quality_media** `news_article` SOURCE REFERENCE (provider `ProviderType.news`) to a fixed reputable local-language business-press venue (`LOCAL_LANGUAGE_PRESS_SOURCES`: Les Échos FR / Handelsblatt DE / Milano Finanza IT / Børsen DK — fixed public HTTPS landing page, no query, no API key) carrying a GENUINE short local-language descriptive excerpt (what the venue covers + the full article content is NOT fetched here — never a fabricated article/headline/quote/figure/date), marked `requires_translation` + `original_language`, `low` confidence / `metadata_only`, + honest `translation_required` & content-not-fetched gaps + `needs_human_review` (deliberately LOWERS source quality; a WEAK research-priority signal, never a recommendation/catalyst/materiality/valuation); non-eligible / non-FR-DE-IT-DA / US issuer → honest `source_not_eligible` gap; network-free; the non-English excerpt is consumed by the Phase 30A translation layer when `source_translation_enabled`), `ScaffoldConnector` (Phase 29B: SEDAR+/ASX — every method returns an honest `connector_scaffolded` gap, never fake evidence)
  - `verified_issuer_sources.py` — **Phase 29B.1** a code-defined, maintained **allowlist** (`VerifiedIssuerSource` keyed by `(ticker, exchange)`) of each known non-US issuer's own IR / annual-reports / newsroom URLs + `allowed_domains` (Richemont/Swatch/LVMH/Hermès/Kering/Burberry/Pandora/Moncler + BAE/ASML/SAP/Nestlé). No secrets, no tokenized URLs; `validate_registry()` asserts HTTPS + host∈allowlist + unique `(ticker,exchange)` + required identity (test-enforced). This is a **bootstrap, not a crawler**
  - `safe_web_fetcher.py` — **Phase 29B.1** the bounded, SSRF-safe page fetcher used only by the live evidence-preview path: `is_safe_public_host` / `check_fetch_url` (HTTPS-only, host-allowlist-only, rejects localhost/private/IP-literal/internal hosts), pure HTML helpers (`parse_title`/`parse_meta_description`/`extract_links` — link secrets stripped, allowlist re-checked, capped), and `safe_fetch_page` (timeout / max-bytes / bounded same-allowlist redirects, never raises). **No arbitrary-URL surface**
  - `company_evidence.py` — **Phase 29B/29B.1** `collect_company_source_evidence(company, source_ids?, filings?, press_items?, filings_fetcher?, press_fetcher?, ir_page_fetcher?, cfg, registry)`: runs the SEC + company-IR connectors over already-fetched deterministic data (report path — no new network calls) or an injected live fetcher (preview path), resolves the verified-issuer source, runs the company-IR connector's three methods and merges/dedups/caps their evidence, and adds region-relevant scaffold + `translation_required` gaps for non-US issuers. **Phase 29B.4A** adds an explicit exchange/country→regulator mapping `regulator_connector_for()` (LSE/GB → `uk_fca_nsm`) so a verified UK issuer runs the dedicated `UkFcaNsmConnector` (bounded T2 FCA NSM/RNS reference + honest content gap), and tightens `_relevant_scaffold_ids` so a UK/LSE issuer maps to `uk_fca_nsm` **only** (no longer all four Europe scaffolds; DE/FR unchanged). **Phase 29B.4B/4C** extend `regulator_connector_for()` + `REGULATOR_REFERENCE_IDS` to also dispatch a verified issuer to its dedicated regulator connector by exchange/country — Euronext Paris PA/FR + Amsterdam AS/NL → `euronext_regulated_info`, Germany/Xetra/Frankfurt+Germany → `deutsche_boerse`, Nasdaq Copenhagen CO+Denmark → `nordic_disclosures`, SIX SW/VX+Switzerland → `six_swiss` — and further tighten `_relevant_scaffold_ids` down to the two remaining scaffolds (SEDAR+/ASX) for regions with no dedicated connector. **Phase 30B** adds a dedicated `LOCAL_LANGUAGE_REFERENCE_IDS` collection block that runs the `LocalLanguagePressConnector` for a verified non-US FR/DE/IT/DA issuer (the regulator mapping is untouched), emitting the bounded T4 local-language `news_article` reference the Phase 30A layer translates. Returns bounded `CompanySourceEvidence`. Adapters `sec_filings_from_catalyst` / `press_items_from_catalyst` re-express `catalyst_discovery` events as connector dicts
  - `live_fetchers.py` — **Phase 29B/29B.1** bounded live fetchers for the evidence-preview endpoint ONLY: `live_sec_filings_fetcher` (SEC EDGAR via `SecRecentFilingsProvider`, fixed host), `live_ir_press_fetcher` (curated verified-issuer feed only) and `live_ir_page_fetcher` (wraps `safe_fetch_page`, allowlisted issuer pages only). No caller-supplied URL is ever fetched (no SSRF)
  - `connectors/macro_reference.py` / `macro_evidence.py` — **Phase 29C.1 + 29C.2 + 29C.3** the reference-only MACRO / commodity-energy / policy-government layer. `ALL_MACRO_SOURCES` (`MACRO_SOURCES` — the 29C.1 macro publishers — combined with `COMMODITY_ENERGY_SOURCES` — the 29C.2 USGS/EIA/IEA/IRENA/ENTSO-E references — and `POLICY_GOVERNMENT_SOURCES` — the 29C.3 USTR-TARIC/UN Comtrade/NATO/SIPRI/OECD references) is the single immutable source of truth (`MacroSourceSpec`: source_id / display_name / fixed public URL / provider / **tier (T2/T3)** / theme keywords / indicator description / reliability note) from which the registry builds both its enabled macro/commodity rows AND its connectors. `collect_theme_macro_evidence(theme, region, cfg, *, max_items?, registry?)` → `ThemeMacroEvidence` (bounded `EvidenceItem` references + honest `SourceGap`s + `warnings`, `gap_messages()` helper): **dark by default** (empty when `source_macro_enabled` is False), never fetches, never fabricates a figure, capped at `source_macro_max_items`. The company path matches on the company's sector/industry theme; the discovery path on the run's parsed thesis theme/region
  - `connectors/event_reference.py` / `event_evidence.py` — **Phase 29D.1 + 29D.2 + 29D.3** the reference-only **event-trigger** layer (parallel to the 29C macro layer, INDEPENDENT flag). `EventReferenceConnector` is built per `ALL_EVENT_SOURCES` spec — `PROCUREMENT_SOURCES` (`eu_ted`/`usaspending`) + `PATENT_SOURCES` (`google_patents`/`uspto`/`epo_espacenet`, made **purely thematic** by a per-kind `_EventFlavor`) + `PERMIT_SOURCES` (`ferc`/`us_nrc`/`us_epa`, all provider `permits`/T2, made **purely thematic** by a per-kind `_PERMIT_FLAVOR`; bare "industrial" excluded to avoid a GICS-Industrials collision) — and implements `fetch_events` to emit ONE bounded **T2/T5** `government_data` event `SOURCE REFERENCE` (fixed public procurement/tender/patent/permit landing URL + which venue it covers) + an honest `data_not_sourced` gap for a relevant theme; **no specific tender/award/contractor/amount/contract-number/date — nor patent number/title/inventor/assignee/claim/filing-or-grant date — nor docket/case/permit number/applicant/decision/outcome/date, and NO legal/infringement/validity/patentability/ownership conclusion (patents) / NO regulatory-outcome/approval/materiality conclusion (permits)**, network-free, no API key; each reference is a **WEAK** research-priority signal carrying `needs_human_review` + `stale_after_days` freshness (never a materiality claim / candidate / catalyst / trade signal). `collect_theme_event_evidence(theme, region, cfg, *, max_items?, registry?)` → `ThemeEventEvidence` (bounded `EvidenceItem` references + honest `SourceGap`s + `warnings`): **dark by default** (empty when `source_event_enabled` is False), never fetches, never fabricates an award / patent / permit, capped at `source_event_max_items`. The company path matches on the company's sector/industry theme; the discovery path on the run's parsed thesis theme/region — same matching seam as the macro collector. **Phase 29D.3** also makes `discovery_council._event_discovery_facts` label each citeable `R#` run-fact **per `provider_type`** (via `_event_provider_type`): procurement byte-identical ("procurement / tender venue reference"), patents → "patent office / index venue reference", permits → "permit / regulatory-event venue reference"
  - `gaps.py` — normalized `SourceGap` (`gap_type` incl. `connector_scaffolded`/`source_not_eligible`/`primary_filing_unavailable`/`data_not_sourced`, `severity`/`suggested_followup_phase`/`blocks_research_complete`). Gap messages are recommendation-free and safety-gate-clean
  - `rate_limit.py`/`cache.py` — connector-politeness groundwork (`RateLimitPolicy`, `SlidingWindowLimiter`, `TTLCache`)
  - **Integration** — `evidence_pack.py` gains additive `connector_evidence` + `connector_gap_messages` params (connector primary filings are added first so they survive the cap); `council.py` calls `collect_company_source_evidence` when `SOURCE_CONNECTOR_ENABLED` and injects the result (a connector failure degrades to no connector evidence, never crashing the council). Both packs still carry `registry_gap_messages(build_registry())` so scaffold/planned gaps reach the source critic; `final_report_generator`'s `source_framework` block gains `connector_layer_enabled`
  - **API** — `GET /api/v1/sources/registry` + `GET /api/v1/sources/health` + **Phase 29B** `POST /api/v1/sources/evidence-preview` (read-only, secret-free, identity-only, admin/internal), all declared before `/{source_id}`. Frontend `/admin/sources` renders the registry read-only + a small evidence-preview form

- Forbidden-output vocabulary (Phase 27.1A, `apps/api/app/services/safety_terms.py`):
  - **Single source of truth.** Five gates delegate to it — `final_report_generator.run_safety_gate`, `scoring_engine._check_forbidden_terms`, `investment_committee_chair._check_forbidden_output`, `research_judge_service._scan_for_forbidden_terms`, `market_discovery_service.scan_forbidden_terms` — so they cannot drift apart as they had by Phase 27
  - Three tiers, because bare substring matching flagged real company names (`ENEOS Holdings` → HOLD, `Swatch Group` → WATCH) and a naive `\b` word boundary would still reject the legitimate phrase `watch industry`:
    - **Tier 1 rating tokens** — case-**sensitive**, word-bounded, ALL-CAPS (`BUY`/`SELL`/`HOLD`/`WATCH`/`REJECT`/`SHORTLIST`/`SHORTLIST_HIGH`/`OUTPERFORM`/…). Agents emit labels upper-case, so case-sensitivity is what lets ordinary English through
    - **Tier 2 rating context** — case-insensitive; a rating word within ~40 characters of a rating-intent word, either order, never crossing a sentence boundary (`Rating: Buy`, `we recommend a buy`, `assigned a watch rating`)
    - **Tier 3 phrases** — case-insensitive substring; multi-word valuation language (`price target`, `fair value`, `intrinsic value`, `upside of/to`, `buy signal`, …) plus the terms-of-art `undervalued`/`overvalued`
  - `recommendation` and `investment advice` are deliberately **not** forbidden standalone: the mandatory disclaimers read "this is not a recommendation" / "NOT INVESTMENT ADVICE", so banning them would fail every compliant report. Tier 2 covers the dangerous usage
  - `integrations/llm_provider.py` `_FORBIDDEN_RATING_PATTERN` is **intentionally separate and stricter** — it guards raw LLM text on the way in and out, not authored report content
  - LLM optional (offline by default) — enriches `executive_summary` via `packages/prompts/research/phase16_final_report_generator_v1.md`

### Database
- Local: PostgreSQL 16 via Docker Compose
- Production: Azure Database for PostgreSQL Flexible Server
- Status: **migrations 001–009 applied locally; migrations 001–009 applied on staging; migration 009 adds backtest_runs, backtest_results, thesis_tracking_events (Phase 22)**

### Vector Search
- Azure AI Search
- Document chunks from filings, news, industry reports
- Used for RAG in agent workflows
- Status: **not yet implemented — Phase 3+**

### File Storage
- Azure Blob Storage
- PDFs, downloaded documents, exported reports
- Status: **not yet implemented — Phase 3+**

### Background Jobs
- Azure Functions (scheduled weekly/monthly workflows)
- Azure Service Bus (job queue, later)
- Status: **not yet implemented — Phase 5+**

---

## Monorepo Structure

```
investingbuddy/
├── apps/
│   ├── api/        FastAPI backend
│   │   ├── app/
│   │   │   ├── main.py
│   │   │   ├── core/           config, security, logging
│   │   │   ├── api/
│   │   │   │   └── v1/
│   │   │   │       ├── health.py
│   │   │   │       ├── companies.py
│   │   │   │       ├── workflows.py
│   │   │   │       ├── sources.py      Phase 3
│   │   │   │       └── citations.py    Phase 3
│   │   │   ├── models/         SQLAlchemy ORM: Company, Report, AgentRun, AgentStep, Source, Citation
│   │   │   ├── schemas/        Pydantic: company, report, agent, source (incl. citations)
│   │   │   ├── integrations/   financial_data_provider.py (ABC + schemas + SourceRecordAttrs), financial_data_service.py, llm_provider.py (ResearchLLMClient ABC + MockClient + AzureClient + factory), providers/ (mock, eodhd, sec_edgar[live], stooq[live], gleif[live], openbb[placeholder])
│   │   │   ├── services/       company_service, report_service, agent_run_service, source_service, citation_service, report_validation_service
│   │   │   ├── agents/
│   │   │   │   ├── base.py     CompanyAnalysisState TypedDict
│   │   │   │   └── validation/
│   │   │   │       └── citation_validator.py   Phase 3 skeleton
│   │   │   ├── workflows/
│   │   │   │   ├── company_analysis.py   9-node Phase 7 workflow (+ optional LLM node)
│   │   │   │   └── snapshot_builder.py   pure transformation: snapshot + schema draft
│   │   │   └── db/             session, base
│   │   ├── alembic/
│   │   │   └── versions/
│   │   │       ├── 001_add_initial_tables.py
│   │   │       ├── 002_add_sources_and_citations.py   Phase 3
│   │   │       └── 003_add_citation_provenance_fields.py  Phase 6
│   │   ├── tests/
│   │   └── pyproject.toml
│   └── web/        Next.js frontend
│       └── src/app/
├── packages/
│   ├── shared-types/   TypeScript types shared between frontend and backend
│   ├── prompts/
│   │   └── research/
│   │       └── phase7_company_research_v1.md   Phase 7 LLM prompt (v1)
│   └── research-contracts/
│       └── real_asset_equity/
│           └── v1/     JSON Schema + source taxonomy + provider mapping + example (Phase 3.5)
├── infra/
│   ├── azure/          ARM / Bicep infrastructure definitions
│   ├── github-actions/ Reusable action fragments
│   └── terraform/      Terraform modules (later)
├── .github/
│   └── workflows/      GitHub Actions CI (api-ci.yml, web-ci.yml)
├── docker-compose.yml
├── .env.example
└── docs/
```

---

## API Versioning

All backend routes are versioned under `/api/v1/`.
The health endpoint lives at `/health` (unversioned, used by load balancers).

---

## Workflow Execution Pattern

```
API endpoint (POST /api/v1/workflows/company-analysis/run)
    ↓
run_company_analysis(db, company_id, provider_name, require_schema_valid)
    ↓
LangGraph StateGraph.ainvoke(initial_state)
    ↓
  load_company                → creates agent_run, resolves company from DB
  fetch_provider_data         → calls FinancialDataService (default: MockProvider)
  create_source_records       → build_source_record() + source_service.get_or_create_source()
  build_company_snapshot      → snapshot_builder.build_company_snapshot()
  generate_research_sections  → ResearchLLMClient.generate_research_sections() [optional; use_llm=False by default]
  create_citations            → CitationCreate with field_path, source_tier, data_quality
  validate_report_schema      → validate_real_asset_report() → ValidationResult stored
  save_draft_report           → ReportCreate with snapshot JSON + LLM sections + validation status
  log_agent_steps             → marks agent_run completed
    ↓
WorkflowRunResponse (agent_run_id, draft_report_id, status, summary,
                     provider_name, is_mock, schema_valid,
                     validation_errors, validation_warnings, missing_fields,
                     llm_provider, llm_used)
```

All errors are caught, logged to `agent_runs.error_message`, and returned as HTTP 422.
`require_schema_valid=true` in the request body forces `status=failed` when the schema draft is invalid.

---

## Canonical Evidence Inventory (product readiness, 2026-08-22)

`app/services/canonical_evidence.py` is the SINGLE definition of "what evidence
does this report actually have". Every final-report quality surface derives from
it. Adding a new surface means reading this inventory — never writing another
truth calculation.

```
company_snapshot + financial_data_summary + council primary_facts + catalyst summary
    ↓
normalize_financial_data_summary()   both key spellings, once (available_count /
                                     available_fields / missing_count / warnings_count)
resolve_fundamentals()               issuer document (T1) ▸ SEC XBRL (T2) ▸ EODHD (T5)
                                     strongest channel wins; every channel retained
resolve_price_provenance()           the PRICE FEED's own provider + tier
build_evidence_channels()            5 distinct channels reported separately
    ↓
executive_summary · data_availability_summary · evidence_channels ·
financial_snapshot · bull_case · bear_case · risk_analysis ·
valuation_readiness · source_quality_review · research_completeness ·
missing_information · research_memo · llm_council_analysis
```

**Source-priority rule.** For a decision-critical field a validated
regulator/issuer structured fact (T1/T2) supersedes a lower-tier estimate
(T5/T6). A weaker source never overwrites a stronger current fact; when both
exist BOTH are retained with their own provenance so a conflict is exposed
rather than silently resolved.

**Five distinct evidence channels.** These are different things and are never
conflated: (a) issuer primary document extracted, (b) regulator filing
metadata/events, (c) regulator XBRL structured financial facts, (d) issuer
press/newsroom, (e) persisted DB citations / council evidence. A company can
have complete regulator-backed statements with zero separately-extracted issuer
documents — the report says exactly that.

**Post-council refresh (issuer-document issuers).** For a non-US issuer there is
no SEC XBRL — its financials arrive only as issuer-document facts, which exist
ONLY after the council has run. The `data_availability_summary` and the
source-quality `missing_primary_sources` are therefore recomputed from the
post-council inventory, so a report cannot say "fundamentals_available: false"
beside a snapshot note reading "sourced from issuer_primary_document", nor keep
asserting "annual report required for financials" after that annual report has
been read. Caught on CFR/MC during the 2026-08-22 staging regression.

**Post-reconciliation rebuild.** The deterministic Bull / Bear / Risk /
Valuation-Readiness / Committee-Chair / Executive-Summary sections are recomputed
by the final-report generator AFTER ingestion + council + fresh source-quality
reconciliation, so the human-facing report carries one current narrative instead
of a pre-ingestion snapshot beside a post-council one. Only sections the workflow
produced are refreshed; a summary already carrying forbidden language is never
rebuilt (that would launder poisoned upstream state past the safety gate). The
original workflow draft is retained in full for audit. See ADR-019.

---

## One Final Reconciled Research State (Phase 32D2, 2026-08-24)

`app/services/final_research_state.py` owns the answer to "what evidence does
this report have AFTER ingestion". It is built ONCE, immediately after the
council returns, and every deterministic human-facing surface is rebuilt from
it. **No surface below that point re-derives evidence state from a
pre-ingestion input.**

```
council returns primary_facts
    ↓
build_final_research_state()
    ├─ resolve_fundamentals(..., primary_facts)      canonical inventory
    ├─ build_financial_evidence_state()              per-CATEGORY resolution:
    │                                                issuer doc (T1) ▸ SEC XBRL (T2)
    │                                                ▸ aggregator; strongest wins
    ├─ reconcile_financial_data_summary()            missing -> available for the
    │                                                categories actually validated
    └─ reconcile_research_completeness()             closes only the schema gaps a
                                                     validated fact truly satisfies
    ↓
data_availability_summary · missing_information · research_completeness_review ·
source_quality_review · evidence_quality · thin_evidence_state ·
evidence_channels · bull_case · bear_case · risk_analysis · valuation_readiness ·
committee_chair_summary · executive_summary · research_memo
```

**Why it exists.** Manual QA on the Pandora final report found one document
saying `fundamentals_source: issuer_primary_document / T1_primary_filing` and
`financials.revenue -> missing_fields`, "All 18 core financial fundamental
categories are missing", "fundamentals (not yet sourced)", "Source T1 primary
filings for revenue" and "All current data from T6 only" — all at once, all
about the same validated DKK 32.5bn revenue figure. Each earlier corrective
repaired one surface; nothing owned the answer.

**Two tiers, not one.** The IDENTITY/PRICE provider tier and the FINANCIAL
EVIDENCE tier are separate facts about a report and are reported separately. A
company can have T6 identity, T5 price and T1 financials simultaneously; a
single "source tier" cannot express that and produced "all current data is T6".

**Absence is preserved.** Reconciliation only moves a category that a validated,
high-confidence, group-scoped fact actually closes. Everything else stays
missing in every surface, with the same words. `STATEMENT_CATEGORIES` further
separates lines a filing can close from market/derived metrics it cannot, so the
report never asks a reviewer to "extract price to earnings from the annual
report".

**One issuer registry reaches every layer.** `verified_issuer_sources` (the
code-defined, safety-validated allowlist) is now consulted by company-source
discovery and by the discovery evidence pack, not only by the connector layer.
Previously a report could cite an issuer's own annual report while News &
Catalyst Discovery said no verified company source existed. The
`document_domains` CDN host stays a narrow fetch authority and is never promoted
to a news/publication source.

**Discovery states three issuer-source states, not two.** `unknown` /
`known_not_fetched` / `fetched`. A metadata-only discovery pass that has an
issuer's IR and annual-report URLs on record but does not fetch them must say
KNOWN-BUT-NOT-FETCHED — collapsing that into "unknown" told a council that eight
European issuers had no identifiable primary sources.

**Admin surfaces render the canonical shape.** The backend has emitted grouped
warnings (`DiscoveryRunRead.warning_groups`) since Phase C, but the admin
candidate queue kept rendering the RAW list because the TypeScript type never
declared the grouped field — 200 near-duplicate strings where the backend
already had 8 groups. Discovery/field-review agent summaries were rendered
inside a COLLAPSED `<details>`; the e2e assertion used `toContainText`, which
reads collapsed DOM text and passed while a reviewer saw an empty area. Both are
now rendered inline, and the e2e tests assert VISIBILITY rather than DOM text.

**Regeneration recovers state by explicit lineage.** A final report's markdown
stores rendered SECTIONS, not the workflow-state envelope, so regenerating from
one recovered nothing and rendered "Run company analysis workflow" beside the
report's own T1 facts. `generate_from_report` now falls back to the originating
Phase-9 draft of the same `created_by_agent_run_id` (never a ticker/name match),
and declares the degradation in a `regeneration_notice` when that is impossible.
See ADR-028.

**Evidence channels are seven, not five.** Issuer-primary FACTS (T1) are a
separate channel from regulator XBRL FACTS (T2) and from aggregator
fundamentals (T5); previously a Danish issuer's own PDF facts were rendered
under the heading "Regulator structured financial facts (SEC XBRL)".

---

## Phase History

| Phase | Status | What Changed |
|---|---|---|
| Phase 0 | ✅ Complete | Agentic dev infrastructure: skills, commands, docs scaffolding |
| Phase 1 | ✅ Complete | FastAPI skeleton, Next.js skeleton, Docker Compose, GitHub Actions CI |
| Phase 2 | ✅ Complete | DB foundation (Alembic + 4 tables), company endpoints, LangGraph workflow skeleton |
| Phase 3 | ✅ Complete | Source + Citation models, migration 002, source/citation services + API, CitationValidator skeleton, workflow creates placeholder source + citation |
| Phase 3.5 | ✅ Complete | Real-asset equity report schema contract, source taxonomy, EODHD provider mapping, offline schema validation utility, report validation tests, DATA_SOURCES.md |
| Phase 4 | ✅ Complete | Financial data provider abstraction, MockProvider, provider skeletons (SecEdgar/Stooq/OpenBB/Gleif/EODHD), FinancialDataService registry, smoke-test API endpoints |
| Phase 4.5 | ✅ Complete | Live free provider implementations: StooqProvider (OHLCV CSV), GleifProvider (LEI lookup), SecEdgarProvider (CIK submissions); SourceRecordAttrs helper; diagnostic API endpoints; fixture-based offline tests; integration test harness |
| Phase 6 | ✅ Complete | 8-node company_analysis workflow; FinancialDataService integrated; company snapshot; source + citation records (with field_path, source_tier, data_quality); schema validation gate; migration 003; 38 new tests; 306 total |
| Phase 7 | ✅ Complete | 9-node workflow; `ResearchLLMClient` abstraction (Mock + AzureOpenAI skeleton); optional `generate_research_sections` LLM node; `ResearchSectionsOutput` schema; safety gate; prompt template v1; `use_llm`/`llm_provider` API fields; 28 new offline tests; 334 total |
| Phase 8 | ✅ Complete | 13-node workflow v4.0.0; 4 deterministic Research Team agents (financial data, source quality, completeness, citation v2); 3 prompt templates; 9 new state fields; 5 API response fields; 52 new offline tests; 394 total |
| Phase 9 | ✅ Complete | 18-node workflow v5.0.0; 5 deterministic Analysis Council agents (bull, bear, risk, valuation guard, committee chair); 5 prompt templates; 9 new state fields; 9 API response fields; 64 new offline tests; 458 total |
| Phase 10 | ✅ Complete | Admin Review UI (`/admin`); 5 Next.js routes; `GET /api/v1/reports` + `GET /api/v1/reports/{id}`; typed API client; 13 new backend tests; 463 total; ruff + typecheck + lint + build clean |
| Phase 11 | ✅ Complete | Admin Review Workflow; 5 new admin endpoints; `ReportReviewEvent` model; migration 004; `ReviewPanel` client component; review event timeline; 30 new backend tests; 493 total; ruff + typecheck + lint + build clean |
| Phase 12 | ✅ Complete | Azure Staging Infrastructure; 5 Bicep modules; `main.bicep` with RBAC; activated `deploy-api-staging.yml` + `deploy-web-staging.yml` (OIDC); staging Basic Auth middleware; `gunicorn` deploy extra; docs updated |
| Phase 13 | ✅ Complete | EODHD real provider (`EodhdProvider`); `CompanyIdentifierResolver`; `company_financial_snapshots` table (migration 005, JSONB); workflow + snapshot_builder fundamentals enrichment; 4 EODHD diagnostic endpoints + `/resolve`; `WorkflowRunResponse` Phase 13 fields; 51 new offline tests; 552 total |
| Phase 14 | ✅ Complete | Company Discovery / Screener; `CompanyScreener`; `CompanyDiscoveryService`; 3 new tables (migration 006); 7 discovery API endpoints (universes + runs + candidates + promote); 6 themes; T5 source tier enforced for EODHD; fixture-based EODHD search; candidate promotion; 57 new offline tests; 601 total |
| Phase 15 | ✅ Complete | Scoring + Valuation Framework; `ScoringEngine` (10 dimensions; T6/mock ≤ 30, T5 ≤ 60, T1/T2 ≤ 100); `ValuationReadinessService`; `scorecards` table (migration 007); `ScoringService`; `score_research_attractiveness` node (Node 17); workflow v6.0.0 (19 nodes); 5 scoring API endpoints; 54 new offline tests; 675 total |
| Phase 16 | ✅ Complete | Final Report Generator; `FinalReportGeneratorService` (6 methods); safety gate (forbidden-term scan + exempt-field list); 19-section structured internal draft report; migration 008 (5 new reports columns); 5 API endpoints; LLM optional (offline by default); prompt template v1; 62 new offline tests; 737 total |
| Phase 17 | ✅ Complete | Admin Auth Proxy; Next.js server-side proxy (`/api/admin/proxy`); `BACKEND_BASIC_AUTH` server-only env var; path allowlist; credentials never exposed to browser |
| Phase 18 | ✅ Complete | Staging E2E reliability fix; research contracts bundled in API ZIP; schema path robustness; auth + Bicep hardening; 733 tests |
| Phase 19 | Superseded | Live EODHD smoke test — superseded by Phase 19.1 (EODHD /fundamentals requires paid plan) |
| Phase 19.1 | ✅ Released | Free Real Data Stack: `EodhdPriceOnlyProvider`, `SecEdgarFundamentalsProvider`, `TrendSignalEngine`, `FreeRealSnapshotComposer`, `NewsCatalystProvider` (8-K), composite `free_real` + `eodhd_free_real`; 64 new offline tests; 831 total. Staging: SEC EDGAR ✅; Stooq blocked from Azure; EODHD /eod partial. TrendSignalEngine not yet wired into main workflow. |
| Phase 19.1 safety fix | ✅ Complete | `investment_committee_chair` forces `human_review_required=True` when safety guard triggers |
| Phase 20 | ✅ Complete | Admin Final Report UI — final-report metadata rendering, generate/validate/regenerate actions in admin UI |
| Phase 21 | ✅ Complete | Playwright admin smoke tests (mock provider by default, staging E2E opt-in) |
| Phase 22 | ✅ Complete | Judge + Backtesting Framework; `BacktestingService`, `ResearchJudgeService`, `MockHistoricalOutcomeProvider`; migration 009 (3 tables); 8 admin-only API endpoints; 34 offline tests |
| Phase 22.1 | ✅ Complete | Admin Backtesting UI: `/admin/backtesting` list + detail; create/evaluate/refresh; 13 Playwright tests; typecheck + lint + build clean |
| Phase 22.1 maintenance | ✅ Complete | `/admin/reports` dynamic rendering fix; homepage platform phase text updated |
| Phase 19.2 | ✅ Released | Real Price + Trend Workflow: `TrendSignalEngine` wired into `company_analysis`; Stooq→EODHD non-blocking fallback; composite provider tracking; T5/T6 metadata in snapshot |
| Phase 19.2.1 | ✅ Complete | Staging deploy + provider observability hardening: SHA-verified `/health` deploy check, Oryx boot-fail detection, Stooq→EODHD fallback surfaced in provider warnings, `sector=None` scoring fix |
| Phase 19.3 | ✅ Delivered | SEC Fundamentals Normalization + Report Completeness: `sec_fundamentals_normalizer` maps us-gaap companyfacts → normalized income/cash-flow/balance-sheet metrics + derived margins/ROE/D-E/YoY; injected into `free_real` snapshot; `FinancialDataAgent` narrates fundamentals; `ValuationGuardAgent` reaches `partial`; EBITDA/market cap/EV never fabricated; 22 offline tests. Remaining identity/sector/market-metric enrichment → Phase 19.4 |
| Phase 19.4 | ✅ Delivered | Identity + Sector + Market-Metric Enrichment: `company_profile_enrichment` (sector from DB or inferred SEC SIC/T6, website/industry SEC/T2, LEI GLEIF/T2 name-guarded; LEI/ISIN/IPO never fabricated) + `market_metrics_enrichment` (latest close + 52-week range/T5, shares SEC DEI/T2, market cap/EV/P-E as DERIVED ESTIMATES/T6 when inputs exist; EBITDA/EV-EBITDA/beta never fabricated). Pruned from `missing_fields`; `FinancialDataAgent` recognises + narrates derived metrics; `ValuationGuardAgent` stays `partial`, conclusions still blocked; report gains identity/profile + Market Metrics sections; 24 offline tests |
| Phase 22.3 | ✅ Complete | UI Modernization + Markdown Report Preview (frontend/UI only): dark glassmorphism design system, animated aurora background (reduced-motion safe), `GlassCard`/`StatusPill`/`SafetyBanner`/`AppShell` primitives, safe `MarkdownReportPreview` (`react-markdown`+`remark-gfm`+`rehype-sanitize`, no `dangerouslySetInnerHTML`) with Preview/Raw toggle + mini TOC replacing the raw `<pre>`; all pages + homepage restyled; disclaimers preserved verbatim; no backend/report-semantics changes, no public publishing; 55 Playwright tests passing |
| Phase 22.3.1 | ✅ Complete | Web Deploy Cache Hardening (deploy/CI + frontend verification only): `/api/version` build-metadata endpoint, `x-ib-build-commit` `<meta>` tag, homepage `force-dynamic`, `deploy-web-staging.yml` bakes `NEXT_PUBLIC_*` build metadata + best-effort (optional `AZURE_CREDENTIALS`) restart + SHA-verified stale-homepage smoke check; prevents/detects the stale prerendered `/` under `WEBSITE_RUN_FROM_PACKAGE`; no backend/report-semantics changes, no secrets, no public publishing |
| Phase 25 | ✅ Complete | Real Market Candidate Discovery (internal-only): `discovery_runs` + `discovery_candidates` (migration 010); deterministic `discovery_scoring_service` (momentum/catalyst/fundamentals/source-quality/completeness − risk penalty, internal prioritization only); `discovery_signal_extractor` reusing the company-analysis workflow per ticker (injectable → offline CI); `market_discovery_service` orchestrator (universe validated against `DISCOVERY_MAX_UNIVERSE_SIZE`, non-blocking per-ticker failures, forbidden-term safety scan); 6 admin-only `/api/v1/market-discovery/*` endpoints; `/admin/discovery` dark-glass UI (start-run form + runs table + ranked candidate queue + inline detail + "Run Full Analysis"); 51 backend + 15 Playwright tests; no BUY/SELL/HOLD/WATCH, no price targets/fair value/upside, human review required, non-public |
| Phase 25.1 | ✅ Complete | Async Discovery Run Execution (UX/ops hardening, no migration): `POST /market-discovery/runs` returns a `pending` run immediately and processes tickers via FastAPI `BackgroundTasks` in a fresh DB session (`create_pending_run` + `process_run` + `process_discovery_run_by_id`), committing progress per ticker; run schema gains computed `progress_pct` + `is_async`/`message`; `/admin/discovery` polls `GET /runs/{run_id}` with a live progress bar; reprocess/double-run guards; removes multi-ticker `504` under the single B1 worker; `BackgroundTasks` process-local (durable queue deferred); 27 backend + 9 Playwright tests; safety unchanged |
| Phase 27 | ✅ Complete | Thesis-to-Universe Discovery (market-segment mode, migration 011): deterministic `market_thesis_parser` (NL thesis → structured themes/regions/…, vague → `needs_narrowing`) + `market_universe_builder` (bounded universe of REAL companies from a curated theme→company registry, region/exclusion-filtered, source/relevance-tagged) + `discovery_thesis_scoring` (`thesis_relevance_score` + `combined_internal_score`); `create_pending_thesis_run` rejects vague/no-match theses (422) before scheduling; `POST /market-discovery/thesis-runs` (+ `GET /thesis-runs/{id}`); run gains `mode/thesis_text/parsed_thesis_json/universe_json`, candidate gains `thesis_relevance_score/combined_internal_score/thesis_match_json`; `/admin/discovery` Manual/Thesis tabs + parsed-thesis/universe panel + relevance/combined columns; 29 backend + 10 Playwright tests; internal-only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, human review required, non-public, no publish route |
| Phase 27.1A | ✅ Complete | Exchange-Aware SEC Lookup + Safety-Matching Fix (no migration). **Safety:** new `app/services/safety_terms.py` is the single forbidden-vocabulary definition; five gates (`final_report_generator`, `scoring_engine`, `investment_committee_chair`, `research_judge_service`, `market_discovery_service`) delegate to a three-tier scanner — case-sensitive ALL-CAPS rating tokens, case-insensitive rating-context regex bounded by sentence, multi-word valuation phrases — so `ENEOS Holdings`/`Swatch Group AG`/`Watches & Jewelry`/`watch industry`/`buyback` pass while `Rating: Buy`/`we recommend a buy`/`price target`/`fair value`/`upside to` now fail (Tier 2 is new coverage no gate had). `recommendation`/`investment advice` deliberately not banned standalone (disclaimers require them); `llm_provider.py` intentionally left separate. **Exchange:** new `app/services/exchange_registry.py` (40 venues; `code/country/region/currency/is_us/sec_eligible`; absorbs the duplicated `_COUNTRY_TO_REGION` + `_EXCHANGE_TO_SUFFIX` tables) and `app/integrations/sec_issuer_registry.py` (explicit `(ticker, exchange)`→CIK map, **ships empty**, every entry requires `source_url` + `verified_on`); `resolve_cik(ticker, exchange)` consults the explicit map, then raises `SecExchangeNotSupportedError(ValueError)` **before any network call** for SEC-ineligible venues; the CIK cache is re-keyed `(ticker, exchange)`. Fixes `BA.LSE`→Boeing, `MC.PA`→Moelis, `EL.PA`→Estée Lauder, `CFR.SW`→Cullen/Frost. `SecRecentFilingsProvider` is exchange-aware too (no more foreign issuers inheriting US 8-K catalysts). **Degradation:** `FreeRealProvider`/`EodhdFreeRealProvider` return an honest `not_sourced` profile (`legal_name`=ticker, registry country, `D_weak_or_stale`) and empty fundamentals instead of raising, because `workflows/company_analysis` calls the profile unguarded; new `data_coverage` contract (`sec_eligible`/`reason`/`requires_human_research`) flows snapshot → signal → `raw_signal_json`, and `fundamentals.available=False` + `fundamentals_not_sourced_non_us_exchange` degrade completeness honestly. `is_sec_eligible(None) is True` preserves legacy AAPL/MSFT/NVDA flows. 315 new backend tests (1596 total); no migration; safety constraints unchanged |
| Phase 27.1A hotfix | ✅ Complete | Exchange Handoff Fix (no migration). Phase 27.1A's SEC gate was correct but never fired in the real pipeline: `extract_signal` invoked the workflow with `company_id` only and `node_load_company` echoed `ticker` back into state **without `exchange`**, so `state["exchange"]` stayed `None` all the way to the provider. `is_sec_eligible(None)` is `True` by design for legacy ticker-only flows, so the US-registrant index answered "BA" with **Boeing** — staging produced a BA/LSE candidate carrying `legal_name=BOEING CO`, `revenue_mln=89463.0` (evidence: `docs/evidence/`). Fix: `node_load_company` emits `exchange` (caller-stated venue wins, company row — `NOT NULL` — fills the gap); `extract_signal` passes `ticker`+`exchange` explicitly so the venue is stated at the seam; **fail-closed guard** in `node_fetch_provider_data` forces `sec_eligible=false` with `reason=exchange_missing_in_provider_state` when a company row yielded no venue, instead of falling through to the US default; `_clean_exchange()` rejects any non-string so only a real code can drive SEC eligibility. Warnings `exchange_recovered_from_company_record` / `exchange_missing_in_provider_state` surface the seam. LESSON: the original 315 tests all passed an exchange explicitly, proving the leaf function while never proving the pipeline supplied one — `tests/test_phase27_1a_exchange_handoff.py` drives the real workflow with the SEC network as a tripwire and **fails against `17e17f7`**. 17 tests (1613 total); safety/auth/publication unchanged |
| Phase 27.1B | ✅ Complete | Luxury/Watch Theme + Sector Taxonomy + Supported Themes (no migration). "European watch producers" previously failed with "Thesis did not match any known theme, sector, or industry". New `app/services/sector_taxonomy.py` is the canonical sector/industry vocabulary (`normalize_sector`/`normalize_industry`/`sector_matches`/`industry_matches`/`get_supported_sector_aliases`) — an unknown sector normalizes to `None` rather than being guessed, so a search never silently widens; `market_universe_builder._select_registry_entries` now matches through it, making `sector="Luxury Goods"` reach `Consumer Discretionary` issuers. New `luxury_goods` theme in `market_thesis_parser` (luxury/watch/timepiece/jewelry/personal-goods/leather-goods phrases → Consumer Discretionary + Luxury Goods/Watches & Jewelry/Personal Goods/Apparel & Accessories) plus a `_THEME_DISPLAY` label/example table and `get_supported_themes()`; a test asserts theme phrase sets stay pairwise disjoint so themes cannot silently union their sectors. New curated `luxury_goods` registry (11 real issuers: UHR.SW, CFR.SW, MC.PA, RMS.PA, KER.PA, MONC.MI, BRBY.LSE, PNDORA.CO, CPRI.US, TPR.US, 1913.HK) — mostly non-SEC venues, so fundamentals degrade honestly via the 27.1A `exchange_registry` gate (`MC.PA` is LVMH, never Moelis) and Europe-filtered theses record US/HK names in `excluded` rather than dropping them. New read-only `GET /market-discovery/supported-themes` (`SupportedThemesResponse`) derived from the parser table joined with the registry — a test asserts every advertised example builds a non-empty universe, so the UI can never offer a theme that yields nothing. **`company_name` backfill fix:** the scan's stub `Company(name=ticker)` was truthy and shadowed the curated registry name (candidates showed `UHR`, not `Swatch Group AG`); `ensure_company` now accepts a curated name hint (creating and upgrading bare-ticker stubs, never overwriting a real name) and `_build_candidate` no longer relies on truthiness — precedence live provider → curated registry → ticker, provenance recorded as `company_name_source`/`company_name_source_tier`, never attributed to SEC, `legal_name` untouched. **27.1B follow-up (`fix: preserve curated company-name provenance`):** the placeholder test alone was wrong in the real pipeline — `ensure_company` seeds the Company row with the curated name and the workflow echoes it back, so staging showed all eight European luxury candidates crediting `provider_profile` while their provider profile was explicitly `not_sourced`. Attribution now keys on `data_coverage.profile_source` (a `not_sourced` provider is never credited) plus value equality with the curated string; 4 regression tests model the real pipeline rather than an injected bare ticker. `/admin/discovery` fetches supported themes and renders clickable example chips under the thesis textarea and alongside (never instead of) the backend's verbatim 422 detail. 78 backend + 12 Playwright tests; safety/auth/publication unchanged — internal only, `human_review_required=true`, `is_public=false`, `publication_ready=false`, no publish route |
| Phase 27.1C | ✅ Complete | Prompt-Derived Autofill + Controlled Selectors + Strict Country Filtering (no migration). **Strict country (the source of truth):** `market_universe_builder` no longer accepts a candidate on `region OR country`; when any country filter is present it is the *sole* geographic filter (region cannot broaden a country-scoped search), so `"Swiss watch companies"` → `UHR`/`CFR` only and `"Danish jewelry companies"` → `PNDORA` only (previously all eight European luxury names). **Part C — prompt autofill:** `market_thesis_parser.parse_thesis` gains canonical single-value `region`/`country`/`sector`/`industry`/`theme`/`extraction_source` detected from the prompt text (US/USA short-forms added), with explicit-over-prompt precedence (explicit form value wins; a conflict keeps the explicit choice and emits a `"Prompt mentions X, but explicit Country=Y was selected."` warning surfaced on the run); new `POST /market-discovery/parse-thesis` previews detections **without creating a run** (DB-free). **Part D — controlled selectors:** new `app/services/discovery_filters.py` (canonical regions/countries derived from `exchange_registry`; canonical sectors from `sector_taxonomy`, aliases resolved internally, never surfaced as options) + `GET /market-discovery/supported-filters`; `create_pending_thesis_run` rejects a Region/Country/Sector outside the supported set (`422`, canonicalized case-insensitively). `/admin/discovery` replaces the free-text Region/Country/Sector inputs with searchable comboboxes (`SearchableSelect`, options from the backend, arbitrary text rejected on blur), debounce-calls `parse-thesis` to auto-fill non-manually-edited fields, shows a "Detected: …" preview + "Reset to detected" action + a prompt/selection conflict warning. 21 backend + 9 Playwright tests; safety/auth/publication unchanged — internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `is_public=false`, no publish route |
| Phase 27.1D | ✅ Complete | Safe Staging Telemetry / Logging Cleanup (no migration; API + docs + CI only, web unchanged). Makes staging validation **evidence-based** — previously `httpLogs.fileSystem` was disabled, `containerStream.log` was empty (INFO app logs dropped under gunicorn), and App Insights was unwired, so validation relied only on HTTP status codes + persisted run status. New `app/core/logging_config.py` guarantees a stdout handler at `LOG_LEVEL` (default INFO) so structured events reach the container log stream; `app/core/log_redaction.py` (tested) redacts sensitive header/mapping/URL-query values by key name; `app/core/structured_logging.py` emits `event key=value` lines (redacts sensitive field names, drops `None`, collapses newlines → one line per event); `app/core/request_logging.py` middleware logs one `http_request` line per request (`method`/`path`(path only, never query)/`status`/`duration_ms`/`request_id`(honours inbound `X-Request-ID`)/`route_family`) and NEVER logs headers/bodies/query strings/secrets, added **outermost** so it also captures Basic-Auth 401s. `market_discovery_service` emits `discovery_run_started`/`discovery_candidate`(ticker/exchange/company_name_source/profile_source/fundamentals_source/sec_eligible/reason/safety_valid/human_review_required — no raw payloads)/`discovery_run_completed`(status/processed/candidate/error/warning counts + duration_ms)/`discovery_run_failed`(run_id + exception_type + safe message). `final_report_generator.validate_final_report` emits `report_validation` (report_id + schema_valid/safety_valid/research_complete/publication_ready/human_review_required booleans + forbidden_terms_count + missing_required_sections_count — never report body). `/health` gains safe `app` + `build_time` metadata. New `LOG_LEVEL`/`REQUEST_LOGGING_ENABLED` settings. API deploy smoke echoes `environment`/`build_id` from `/health` (informational, never fragile). `docs/DEPLOYMENT.md` gains an Azure staging logging recipe (enable filesystem logs + retention, stream, download+grep by event, verify-no-secrets, reduce verbosity). 39 backend tests (1755 total); safety/auth/publication unchanged — no auth change, no `AUTH_TEST_MODE`, `human_review_required=true`, `publication_ready=false`, no publish route, no recommendation/target/fair-value/upside language. **Hotfix (`fix/phase-27-1d-suppress-thirdparty-url-logs`):** staging validation found that raising the root logger to INFO newly surfaced `httpx`'s INFO request-URL logs, leaking the EODHD `?api_token=<key>` (hidden before 27.1D because the unconfigured root dropped INFO); fixed by capping `httpx`/`httpcore`/`urllib3` at WARNING + a `RedactingFilter` on the stdout handler that scrubs token-bearing query params + Authorization/Cookie echoes from every record (`log_redaction.redact_text`); re-validated `api_token=` renders only `***REDACTED***` |
| Phase 28A | ✅ Complete | Single-Company LLM Analysis Council (no migration; OFF by default). New `app/services/llm/` package: `client.py` (provider-pluggable `LLMClient` ABC — JSON parse + one repair; factory `get_llm_client` returns `None` when disabled/unavailable so the deterministic path is preserved), `fake_client.py` (deterministic, offline — the ONLY provider in tests), `azure_openai_client.py` (thin langchain Azure + OpenAI clients, lazy import, `LLMUnavailableError` on missing creds), `prompts.py` (per-agent system prompts + prompt-injection guard + JSON contract — never logged), `schemas.py` (`EvidencePack`/`EvidenceItem` with **transport_tier vs content_tier**, `CouncilAgentOutput`/`CouncilResult`), `evidence_pack.py` (`build_evidence_pack` — bounded, cited, SEC EDGAR = T2 transport / T1 filing content), `citation_checker.py` (drops non-pack citation ids, flags un-cited material claims, quarantines any forbidden-language agent output, coerces committee labels), `council.py` (`run_council`/`maybe_run_council` — 8 agents in order, per-agent isolation on failure, safe structured logging). Integrated into `FinalReportGeneratorService._generate_and_save` before validation (council section is scanned by the existing safety gate); metadata persisted under `source_summary_json.llm_council`; `FinalReportResponse` gains additive `llm_used`/`llm_provider`/`llm_model`/`council_version`/`council_agents_*`/`evidence_pack_version`/`evidence_item_count`/`committee_label`. New config `LLM_COUNCIL_ENABLED`/`LLM_PROVIDER_COUNCIL`/`LLM_MODEL`/`LLM_TEMPERATURE`/`LLM_MAX_OUTPUT_TOKENS`/`LLM_REQUEST_TIMEOUT_SECONDS`/`LLM_COUNCIL_MAX_EVIDENCE_ITEMS`/`LLM_COUNCIL_VERSION`/`OPENAI_API_KEY`. `/admin/reports/[id]` gains an LLM-used pill + council metadata rows + an "LLM Council Analysis" section (bounded, safety-scanned, citation-bound output only — never raw prompts/evidence/secrets). Safe structured events `llm_council_started`/`evidence_pack_built`/`llm_agent_completed`/`llm_agent_failed`/`llm_council_completed` (ids/provider/model/status/counts/duration only). 33 backend + 3 Playwright tests; internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change, no `AUTH_TEST_MODE` |
| Phase 28B | ✅ Complete | Run-Level LLM Discovery Council (no migration; OFF by default) — the run-level analog of the 28A single-company council, reviewing a whole discovery run's candidate set to decide internal research **priority**. Gated by **both** `LLM_COUNCIL_ENABLED` and new `LLM_DISCOVERY_COUNCIL_ENABLED` (default `false`); when either is off or no provider resolves the council is disabled, no fake output is produced in production, and the deterministic result is unchanged. **Manual admin-triggered only.** New `app/services/llm/` files (reusing 28A `client.py`/`safety_terms`/logging): `discovery_schemas.py`, `discovery_evidence_pack.py` (`build_discovery_evidence_pack` — bounded run pack, run facts `R1..Rn` + per-candidate `C1..Cn` carrying `score_breakdown`/`data_coverage`/`catalyst_summary`/`safety_valid`/`warnings`; agents cite only those ids; bounded by `LLM_DISCOVERY_COUNCIL_MAX_CANDIDATES` default 25), `discovery_prompts.py`, `discovery_citation_checker.py` (drops invalid citations, moves un-cited material claims to `unsupported_claims`, quarantines forbidden-action output `status=failed` with no term echoed forward, coerces bad `internal_action`/`run_quality`; final backstop re-scan before storing), `discovery_council.py` (`get_discovery_llm_client`/`run_discovery_council`/`maybe_run_discovery_council` — 8 agents in order `run_coordinator`→`candidate_prioritization`→`novelty_coverage`→`diversity_anti_convergence`→`evidence_sufficiency`→`risk_gatekeeper`→`run_red_team`→`discovery_chair`, per-agent isolation, one failure never fails the review), `fake_discovery_client.py` (deterministic offline — ONLY provider in tests). `market_discovery_service.run_discovery_council_review` adapts ORM run+candidates to dicts, calls `maybe_run_discovery_council`, stores the `DiscoveryCouncilReviewResponse` under `discovery_runs.config_json["discovery_council"]` (reassigned whole for JSONB dirty-tracking). Two admin-only market-discovery endpoints (`POST`/`GET .../runs/{run_id}/council-review`; `409` disabled/no-provider, `422` no-candidates/not-terminal, `404` not-found). Only internal actions `research_next`/`monitor_for_evidence`/`insufficient_data`/`reject_for_now`; only `run_quality` `strong`/`adequate`/`thin`/`failed`. New config `LLM_DISCOVERY_COUNCIL_ENABLED`/`LLM_DISCOVERY_COUNCIL_MAX_CANDIDATES`/`LLM_DISCOVERY_COUNCIL_VERSION` (reuses 28A provider settings/credentials). `/admin/discovery` gains a "Discovery Council Review" panel (Run button + disabled state, run-quality/agent/safety pills, four candidate buckets, evidence gaps, next source tasks, red-team/agent summaries, inline internal-action pill per candidate row — no publish, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/downside). Safe events `discovery_council_started`/`discovery_council_evidence_built`/`discovery_council_agent_completed`/`discovery_council_agent_failed`/`discovery_council_completed`/`discovery_council_disabled`/`discovery_council_failed` (ids/provider/model/counts/durations only). 35 backend + 6 Playwright tests; internal only, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change, no `AUTH_TEST_MODE` |
| Phase 28B.2 | ✅ Complete | Async Discovery Council Execution (no migration; flags unchanged, still OFF by default). Makes the Phase 28B run-level council **asynchronous + pollable**, mirroring the Phase 25.1 async discovery-run pattern (fixes: a large candidate set under low Azure OpenAI quota could rate-limit agents or approach the gateway timeout — the European 8-candidate run completed only 1/8 agents synchronously). `start_discovery_council_review` writes a `pending` **status envelope** and returns immediately; a FastAPI `BackgroundTask` (`process_discovery_council_task` → `process_discovery_council_by_id`) runs the sequential, per-agent-isolated council on a **fresh DB session** and persists a terminal envelope (`completed`/`completed_with_warnings`/`failed`) — every failure path is caught and stored so a job never sticks in `running`. `POST .../council-review` returns immediately (no duplicate jobs: running → returns status; completed → returns review unless `force=true`); `GET .../council-review` polls status and a completed review stays readable after the flags are turned off. Envelope stored under `discovery_runs.config_json["discovery_council"]` (`status`/`started_at`/`completed_at`/`llm_used`/`agents_completed`/`agents_failed`/`safety_valid`/`error`/`review`); `get_council_envelope` normalises legacy raw reviews. `DiscoveryCouncilReviewResponse` gains `status`/`review_available`/`message`/`started_at`/`completed_at`/`error` + `from_envelope`/`disabled_response`. `/admin/discovery` polls `GET` every 3s and shows pending/running progress (agents ok/failed), a failed banner, or the completed review panel. Safe job events `discovery_council_job_queued`/`_started`/`_completed`/`_failed`/`_duplicate` (ids/status/counts/duration only). `BackgroundTasks` process-local, not durable (future: Azure Queue / Celery / durable worker). ~28 new backend + 4 new Playwright tests; internal only, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change, no `AUTH_TEST_MODE` |
| Phase 23 | ✅ Complete | Admin/Auth Hardening (no migration): `/admin/*` + `/api/admin/proxy/*` require an authenticated, allowlisted admin. Dependency-free HMAC-signed httpOnly session cookie (`src/lib/auth/*`, `AUTH_SECRET`); GitHub OAuth sign-in (`/api/auth/github` + callback; secret server-side only, access token discarded after reading the verified email); env allowlist (`ADMIN_ALLOWED_EMAILS`); Next 16 **Proxy** `src/proxy.ts` (→ `/login` / `/unauthorized`; 401/403 on proxy API); proxy route independently re-checks + adds advisory `X-IB-Admin-*` audit headers before Basic Auth; `/login` + `/unauthorized` pages; admin shell identity + Sign out; `AUTH_TEST_MODE` deterministic dev/CI sign-in. Backend Basic Auth retained (`install_staging_basic_auth`; identity headers never trusted without Basic Auth). 13 backend (1236 total) + 15 auth Playwright specs; no public publishing, no recommendation output, safety unchanged |
| Phase 29B.2 | ✅ Complete | Annual-Report Document Extraction + Primary-Fact Parsing (merged `793e0a7`, staging-validated — VALIDATED-WITH-ENVIRONMENTAL-NOTE: every live issuer report reached is scanned/index-only (no-OCR by design) → honest source-gaps, `primary_documents` present-but-empty, 0 fabricated facts; no migration; flags OFF by default). New `app/services/sources/` modules: `document_fetcher.py` (`safe_fetch_document` — bounded, SSRF-safe fetch of ONE allowlisted issuer document, reuses `check_fetch_url`, content-type-gated, cookie/auth-free, URL-secret-stripped, never raises, no arbitrary-URL surface), `document_text_extractor.py` (`extract_document_text` → `DocumentTextExtraction` — first-N-pages PDF via **pypdf**, NO OCR, bounded excerpts; scanned/empty/encrypted → honest gap; HTML strips scripts/styles/nav; full text never leaves the extractor), `primary_fact_parser.py` (`parse_primary_facts` → `PrimaryFact[]` — conservative regex on high-confidence primary facts only, each carrying value/unit/currency/scale/period/source_url/excerpt_id/page_number/confidence + `needs_human_review=true`; refuses ambiguous magnitudes; no inference, no currency conversion, no valuation metrics/price target/fair value/upside). New `app/services/llm/evidence_budget.py` (`apply_evidence_budget` — deterministic pack compression: de-dup, tier-rank, bound item/char counts, re-id `E1..En`, record `omitted_evidence_count`/`omitted_reason`, never drop all gaps) to keep larger packs under Azure OpenAI TPM quota; new `LLM_COUNCIL_EVIDENCE_MAX_ITEMS`(20)/`_MAX_CHARS`(24000)/`_MAX_CHARS_PER_ITEM`(1200). `CompanyIrConnector` accepts an injected `document_extractor` → tiered `company_ir_annual_report_excerpt`/`company_ir_business_description`/`company_ir_risk_excerpt` (T1_primary_filing) + `company_ir_financial_fact` (T1_primary_filing, data_quality B/C) evidence, prioritised ahead of metadata before the per-source cap; `collect_company_source_evidence` + `live_fetchers.live_document_extractor` wire fetch→extract→parse; `maybe_run_council` passes live fetchers when `SOURCE_DOCUMENT_EXTRACTION_ENABLED=true` and stores a text-free `primary_documents` summary under `source_summary_json.llm_council.primary_documents`. `POST /sources/evidence-preview` gains `include_document_text`/`max_items`/`max_excerpts` (still identity-only, no URL) + `document_extraction_performed`. `/admin/sources` gains an "Include annual-report text" checkbox + extracted-text/parsed-fact/document-extraction badges; report page gains a "Primary Documents (extracted)" card (excerpt/fact counts, domain, T1 tier, translation-pending flag, "not the full document / human review required"); T1/T2 checklist logic UNCHANGED. New flags default OFF; flag-off = exact Phase 29B.1 behaviour. New dependency `pypdf>=4.0,<6`. Limitations documented: no OCR, no translation (Phase 30), no PDF table extraction, regulator connectors still scaffolded, TPM-quota agent failures mitigated-not-eliminated, parsed facts unverified until human review. 48 new backend tests (2002 passed), ruff clean, no new mypy over baseline; web typecheck/lint/build clean, +2 e2e (194 passed); internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change |
| Phase 29B.3 | ✅ Complete | Primary-Fact Integration into Reports + Quality Gates (merged `29f4a84`, staging-validated — VALIDATED-WITH-ENVIRONMENTAL-NOTE; closure report `docs/development/closures/phase-29b3.md`; backend-only, no migration). New bounded `PrimaryFactRef` on `EvidenceItem` (`sources/evidence.py`) carries ONE parsed fact's structured fields only (field / value[≤160 chars] / numeric_value / unit / currency / scale / period / secret-stripped source_url / excerpt_id / page_number / confidence / needs_human_review) — no raw excerpt/document text; the `company_ir` connector attaches it to each `company_ir_financial_fact` (T1) item. Council `_primary_facts` surfaces HIGH-confidence facts only via `CouncilResult.primary_facts` → persisted **metadata-only** under `source_summary_json.llm_council.primary_facts` (`to_metadata_dict`); `to_report_dict` (safety-scanned body) is unchanged; the log gains a `primary_fact_count`. `final_report_generator` post-council inserts `T1_primary_filing` datapoints — `<field>_primary_filing` keys in the Financial Snapshot (existing T5 EODHD datapoints preserved), a `reporting_currency` override + `fiscal_year`/`employees` in Company Identity, each with its own source_url + page/excerpt provenance + `needs_human_review`; Source-Quality Review gains an `extracted_primary_facts` block (distinct from metadata-only IR index links); the T1/T2 checklist item is recomputed post-council to complete ONLY on a genuine high-confidence T1 fact (false with 0 facts / mock). `real_asset_report_completer` maps a genuine T1 fact to a properly-sourced schema datapoint (real source + T1 tier + as_of; never `not_sourced` / "internal analysis snapshot"), **refuses a non-USD revenue fact into the USD `revenue_ttm_usd_m` field (no currency conversion)**, and cuts placeholders only when a required field is genuinely filled — `publication_ready` stays false, `human_review_required` stays true, `research_complete` stays honest. A scoring + research-completeness credit for genuine T1 facts is a unit-tested **capability** (optional `t1_primary_fact_count` / `primary_facts` params) but is **NOT wired to a production caller** this phase (scoring runs pre-council) — documented deferral, no live numeric uplift. **Honest caveat:** on staging the Phase 29B.2 extraction reaches only scanned/index-only issuer PDFs (no-OCR) → **0 facts materialize**, so these paths are provable via unit fixtures but the live report shows them present-but-empty; facts flow once digital-text primary sources exist. Backend **2033 passing** (+31 Phase 29B.3, 12 skipped), ruff clean, no new mypy over baseline, security PASS; **no new flag, no migration** (still 011); internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change |
| Phase 29B.4A | ✅ Complete | UK FCA NSM/RNS Regulated-Disclosure Connector (backend-only; no migration; no new flag/host; PR #58 **merged + deployed + staging-validated at `5138725`**; branch `feature/phase-29b4a-uk-fca-rns-disclosures` @ `f80511c`). Promotes `uk_fca_nsm` from a generic `ScaffoldConnector` to a dedicated `UkFcaNsmConnector` (`connectors/uk_fca_nsm.py`): for a verified UK-regulated LSE issuer (`BRBY.LSE` Burberry / `BA.LSE` BAE Systems — resolved via `verified_issuer_sources`, never Boeing/SEC) it emits ONE bounded **T2 regulator-transport source reference** to the issuer's FCA National Storage Mechanism / RNS venue (fixed public NSM URL, no query, no fabricated filing/headline/date/RNS number) **plus an honest `primary_filing_unavailable` gap** that the T1 filing content is not fetched at report time (**network-free** this subphase). New `regulator_connector_for()` exchange/country→regulator mapping (LSE/GB → `uk_fca_nsm`) in `company_evidence.py` + tightened `_relevant_scaffold_ids` (UK/LSE → `uk_fca_nsm` only, not all four Europe scaffolds; DE/FR unchanged). `registry.py` moves `uk_fca_nsm` out of the scaffold table into an **enabled `regulator` / `T2_regulator_or_gov`** source (honest reliability note: T1 content not fetched at report time — Phase 29B.4 follow-up), so `/sources/registry` + `/sources/health` show it enabled (was scaffolded); scaffold set now **5** (SEDAR+/ASX/Euronext/Deutsche Börse/Nordic; since reduced to **4** by Phase 29B.4B, which promoted Euronext). **Deliberate deferral:** live FCA-NSM content fetch is NOT built this subphase (the NSM is a JS SPA → a bounded server-side fetch reads ~nothing while adding external-regulator fetch surface); the honest T2 reference + explicit content-gap is the chosen posture; live content fetch is a future 29B.4 follow-up (4C Swiss/Nordic/Germany upcoming; 4B Euronext promoted — see the 29B.4B row). Backend **2046 pass / 12 skip / 0 fail**, ruff clean, no new mypy over baseline, security PASS; company-IR path + CFR unchanged, reports stay schema/safety valid; internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change |
| Phase 29B.4B | ✅ Complete | Euronext Regulated-Disclosure Connector (backend-only; no migration, still DB head `011`; no new flag/host; PR #59 **merged + deployed + staging-validated at `1d97612`**). Promotes the `euronext_regulated_info` scaffold to a dedicated `EuronextRegulatedConnector` (`connectors/euronext_regulated_info.py`): for a verified Euronext issuer — Euronext Paris FR (`MC.PA` LVMH / `RMS.PA` Hermès / `KER.PA` Kering) or Euronext Amsterdam NL (`ASML.AS` ASML), resolved via `verified_issuer_sources` — it emits ONE bounded **T2 regulator-transport SOURCE REFERENCE** citing the Euronext regulated-information service + country regulator (AMF France / AFM Netherlands) at the fixed public venue URL `https://www.euronext.com/en/regulated-information` (no query, **no fabricated filing/headline/date/notice number**) **plus an honest `primary_filing_unavailable` content gap** that the T1 filing CONTENT is not fetched at report time (**network-free**). French-jurisdiction issuers (MC/RMS/KER) additionally carry a `requires_translation` flag (`GapType.translation_required`) — an honest "French docs not translated in this phase" marker, **not** a claim of official translation. New `company_evidence.py` exchange/country→regulator mapping (Euronext Paris PA/FR + Amsterdam AS/NL → `euronext_regulated_info`) + tightened `_relevant_scaffold_ids` (FR/NL Euronext issuer → `euronext_regulated_info` only; UK still `uk_fca_nsm`; DE unchanged); `registry.py` moves `euronext_regulated_info` out of the scaffold table into an **enabled `regulator` / `T2_regulator_or_gov`** source, so `/sources/registry` + `/sources/health` show it enabled and the summary becomes **8 enabled / 4 scaffolded** (remaining scaffolds: SEDAR+/ASX/Deutsche Börse/Nordic). Unresolvable / non-Euronext → honest `source_not_eligible` gap. **Deliberate deferral (carried from 29B.4A):** the live regulator-venue CONTENT fetch is NOT built this subphase (honest venue reference + explicit content gap is the chosen posture; 4C Swiss/Nordic/Germany still upcoming). Backend **2058 pass / 12 skip / 0 fail** (+`test_phase29b4b_euronext_disclosures.py`; one adjacent 29B.1 honesty test now allows the promoted regulator-reference source_ids), ruff clean, mypy `71` baseline (no new), security PASS; company-IR path + CFR unchanged, reports stay schema/safety valid; internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change |
| Phase 29B.4C | ✅ Complete | Swiss / Nordic / Germany Regulated-Disclosure Connectors (backend-only; no migration, still DB head `011`; no new flag/host/endpoint; PR #60 **merged + deployed + staging-validated at `de126ee`**). The **final** Phase 29B.4 subphase — completes the whole 29B.4 umbrella (EU/UK regulated-disclosure connectors); Phase 29C (macro/commodity/policy) is next. Adds three new dedicated regulator connectors following the same **T2 venue-reference + honest `primary_filing_unavailable` content-gap, network-free, verified-issuer-gated** pattern as 29B.4A/4B: `DeutscheBoerseConnector` (`connectors/deutsche_boerse.py`, promotes the `deutsche_boerse` scaffold) — `SAP.DE` (Germany/Xetra) → ONE bounded **T2 reference** to the German regulated-information venue (Bundesanzeiger / Deutsche Börse / BaFin) at the fixed public URL `https://www.bundesanzeiger.de` (no query, no fabricated filing/headline/date/notice number) + honest content gap + German `requires_translation` (`GapType.translation_required`); `NordicDisclosuresConnector` (`connectors/nordic_disclosures.py`, promotes the `nordic_disclosures` scaffold) — `PNDORA.CO` (Denmark/Nasdaq Copenhagen; generalizes to ST/HE/OL) → **T2 reference** (Nasdaq Nordic company news `https://www.nasdaqomxnordic.com/news/companynews` / Finanstilsynet) + honest content gap + Danish `requires_translation`; and `SixSwissConnector` (`connectors/six_swiss.py`, a **NEW `six_swiss` source built from scratch — no Swiss scaffold existed**) — `CFR.SW` Richemont + `UHR.SW` Swatch on SIX (SW/VX) → **T2 reference** (SIX Swiss Exchange / SIX Exchange Regulation official notices `https://www.six-group.com/…/official-notices.html`) + honest content gap, **NO `requires_translation` claim** (Switzerland is multilingual and major issuers publish English — only a neutral DE/FR/IT multilingual note in warnings, not a translation claim). `company_evidence.py` extends `regulator_connector_for()` (DE/Xetra/Frankfurt+Germany → `deutsche_boerse`; CO/Nasdaq-Copenhagen+Denmark → `nordic_disclosures`; SW/VX/SIX+Switzerland → `six_swiss`) + `REGULATOR_REFERENCE_IDS` + tightened `_relevant_scaffold_ids`; `registry.py` promotes `deutsche_boerse` + `nordic_disclosures` out of the scaffold table and registers `six_swiss` as a **new enabled `regulator` / `T2_regulator_or_gov`** source, so `/sources/registry` + `/sources/health` show all three enabled — registry now **11 enabled / 2 scaffolded** (remaining scaffolds: SEDAR+/ASX). Unresolvable / non-eligible issuers → honest `source_not_eligible` gap. **Deliberate deferral (carried from 29B.4A/4B):** the live regulator-venue CONTENT fetch is NOT built this subphase. Verified GREEN (pre-staging): backend **2071 pass / 12 skip / 0 fail**, ruff clean, mypy `71` baseline (no new), security scan PASS; company-IR path + CFR unchanged, reports stay schema/safety valid; internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change |
| Phase 29C.1 | ✅ Complete | Macro Reference Evidence Connectors (backend-only; no migration, still DB head `011`; no new host/endpoint; two new OFF-by-default flags `source_macro_enabled`/`source_macro_max_items`; PR #61 **merged + deployed + staging-validated at `a8ac580`** — OFF-state clean + ON-state VALIDATED-WITH-ENVIRONMENTAL-NOTE, `source_macro_enabled` flip kept ON on staging). The **first** Phase 29C subphase — the initial macro / thematic evidence layer, **reference-only + OFF by default**. A generic **`MacroReferenceConnector`** (`connectors/macro_reference.py`) over five official public sources — FRED (`fred.stlouisfed.org`), IMF, Eurostat, World Bank Commodity 'Pink Sheet', national statistics offices / central banks — implements `fetch_macro_context` (previously a dead hook) to emit, for a relevant theme/region, ONE bounded **T2 `macro_report` SOURCE REFERENCE** (fixed public token-free URL + which indicators the dataset covers) **plus an honest `data_not_sourced` gap** ("live figures not fetched at report time"); **no indicator value / index level / release date / forecast is ever emitted**, network-free, no API key. A theme collector **`collect_theme_macro_evidence(theme, region, cfg)`** (`macro_evidence.py`) is dark when `source_macro_enabled` is False and bounded by `source_macro_max_items` (default 3). `registry.py` promotes the five macro sources from PLANNED to **enabled** reference-only macro sources (built from `MACRO_SOURCES`), so `/sources/registry` + `/sources/health` show `fred`/`imf`/`eurostat`/`national_stats_central_banks` (`macro_statistics`) + `world_bank_pink_sheet` (`commodity`) as enabled `T2_regulator_or_gov` — registry now **16 enabled / 2 scaffolded** (only SEDAR+/ASX remain; total 32). Discovery council: `build_discovery_evidence_pack` threads macro references as citeable **`R#` run facts** (in `evidence_ids()`) + honest gaps so the council can cite theme-level macro context. Company report: council surfaces `CouncilResult.macro_context` via `to_metadata_dict` (empty `[]` when off, mirroring `primary_documents`) and `final_report_generator` renders an OPTIONAL **`industry_macro_context`** block (beside `industry_context_events`), each item labelled macro CONTEXT with an honest note that it is **NOT company-specific evidence and never a catalyst**, no figures. `schema_valid`/`safety_valid` stay true, `publication_ready` false, `human_review_required` true; **dark-by-default** — with the flag off the discovery pack + report body are **byte-identical** to Phase 29B. **Deliberate deferral (documented):** live macro-FIGURE fetch is DEFERRED (reference-only), mirroring the 29B.4 regulator content-fetch deferral — no API keys, no report-time network; a live official-data-API fetch (keyless World Bank/Eurostat/IMF etc.) is a documented 29C follow-up. All seven 29C.1 acceptance criteria satisfied (discovery cites theme macro; reports include industry-context evidence; evidence budget handles macro packs; no reco/valuation; no fake macro facts; source gaps explicit). Verified GREEN (pre-staging): backend **2092 pass / 12 skip / 0 fail** (+`test_phase29c1_macro_connectors.py`), ruff clean, mypy `71` baseline (no new), security scan PASS; internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change. **29C.2 (commodity + energy) shipped — see the 29C.2 row; 29C.3 (policy + government) still upcoming.** |
| Phase 29C.2 | ✅ Complete | Commodity + Energy Reference Connectors (backend-only; no migration, still DB head `011`; no new host/endpoint; **no new flag** — reuses the existing OFF-by-default `source_macro_enabled`; PR #62 **merged + deployed + staging-validated at `80c8454`** — VALIDATED-WITH-ENVIRONMENTAL-NOTE). The **second** Phase 29C subphase — extends the 29C.1 reference-only macro layer to **commodity + energy** with **zero new wiring**. Adds a `COMMODITY_ENERGY_SOURCES` table (+ combined `ALL_MACRO_SOURCES`) to the SAME generic `MacroReferenceConnector` over five official public agencies — **USGS** (`usgs.gov`, T3; copper/lithium/rare-earths/critical minerals/cobalt/nickel/mining/uranium), **US EIA** (`eia.gov`, T2, **no API key**; uranium/nuclear/oil/gas/energy/electricity), **IEA** (`iea.org`, T3; energy/power-grid/nuclear/renewables/energy-transition), **IRENA** (`irena.org`, T3; renewables/solar/wind/hydrogen), **ENTSO-E** (`transparency.entsoe.eu`, T3; power-grid/electricity/grid/transmission). Each emits ONE bounded **T2/T3 `macro_report` SOURCE REFERENCE** (fixed official public URL + which datasets it covers) **plus an honest `data_not_sourced` gap** — **no tonnage/price/capacity/production/reserve figure or release date is ever emitted**, network-free, no API key. Reuses the SAME collector (`collect_theme_macro_evidence` now iterates `ALL_MACRO_SOURCES`), the discovery-council `R#` citation path, the `industry_macro_context` report block and the existing `source_macro_enabled` flag — a **copper company report** surfaces up to two macro refs (World Bank Pink Sheet + USGS) when the flag is on. `registry.py` promotes the five commodity/energy rows PLANNED→**enabled** (built from `ALL_MACRO_SOURCES`), so `/sources/registry` + `/sources/health` show `usgs`/`iea`/`irena`/`entsoe` (`commodity`, T3) + `eia` (`commodity`, T2) as enabled — registry now **21 enabled / 2 scaffolded** (only SEDAR+/ASX remain; **9 planned**; total 32). **Deliberate deferral (carried from 29C.1):** live commodity/energy FIGURE fetch remains DEFERRED (reference-only) — no API keys, no report-time network, evidence-first, honest gaps. `schema_valid`/`safety_valid` stay true, `publication_ready` false, `human_review_required` true; **dark-by-default** (flag off → discovery pack + report body byte-identical to 29C.1). Verified GREEN (pre-staging): backend **2119 pass / 12 skip / 0 fail** (+`test_phase29c2_commodity_energy_connectors.py`), ruff clean, mypy `71` baseline (no new), security scan PASS; internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change. **29C.3 (policy + government) — now PR-open; see the 29C.3 row below.** |
| Phase 29C.3 | ✅ Complete | Policy + Government Reference Connectors (backend-only; no migration, still DB head `011`; no new host/endpoint; **no new flag** — reuses the existing OFF-by-default `source_macro_enabled`; PR #63 **merged + deployed + staging-validated at `ad6dde5`** — completes the whole Phase 29C umbrella). The **third and LAST** Phase 29C subphase — completes the reference-only macro layer by extending it to **policy + government** context with **zero new wiring**. Adds a `POLICY_GOVERNMENT_SOURCES` table (+ combined `ALL_MACRO_SOURCES`) served by the SAME generic `MacroReferenceConnector`: **USTR / EU TARIC** (`ustr_taric`, `trade_policy`, T2; tariffs/trade/customs) + **UN Comtrade** (`un_comtrade`, `trade_policy`, T2; tariffs/trade/customs), both **promoted PLANNED→enabled**; plus three **new** sources **NATO defence expenditure** (`nato`, `trade_policy`, T2, `nato.int`; defense/military-spending/procurement/arms), **SIPRI military expenditure** (`sipri`, `trade_policy`, T3, `sipri.org`; defense/military-spending/procurement/arms) and **OECD** (`oecd`, `macro_statistics`, T2, `oecd.org`; subsidies/industrial-policy/state-aid/energy-transition/grid-investment). Each emits ONE bounded **T2/T3 `macro_report` SOURCE REFERENCE** (fixed official public URL + which datasets it covers) + honest `data_not_sourced` gap — **no budget/spending-%/tariff-rate/subsidy figure or date, network-free, no API key**. Reuses the SAME collector (`collect_theme_macro_evidence` iterates `ALL_MACRO_SOURCES`), the discovery-council `R#` path, the `industry_macro_context` report block and the existing `source_macro_enabled` flag — NO new flag. *Note: `ProviderType` has no `government_data` member, so the government sources use the existing `trade_policy` / `macro_statistics` members.* `registry.py` promotes `ustr_taric` + `un_comtrade` PLANNED→**enabled** and adds `nato` / `sipri` / `oecd` (built from `ALL_MACRO_SOURCES`), so `/sources/registry` + `/sources/health` show all five — registry now **26 enabled / 2 scaffolded / 7 planned** (only SEDAR+/ASX scaffolds; USAspending/EU TED/OpenBB + patent rows stay planned → 29D; total **35**). **Deliberate deferral (carried from 29C.1/29C.2):** live policy/government FIGURE fetch remains DEFERRED (reference-only). `schema_valid`/`safety_valid` stay true, `publication_ready` false, `human_review_required` true; **dark-by-default** (flag off → discovery pack + report body byte-identical to 29C.2). Verified GREEN (pre-staging): backend **2150 pass / 12 skip / 0 fail**, ruff clean, mypy `71` baseline (no new), security scan PASS; internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change. **LAST 29C subphase — on merge + staging validation the whole Phase 29C umbrella (macro + commodity/energy + policy/government) is complete; Phase 29D (event-trigger: procurement/tenders/patents/permits) is next.** |
| Phase 29D.1 | ✅ Complete | Procurement / Tender Event-Trigger Reference Connectors (backend-only; no migration, still DB head `011`; no new host/endpoint/secret; **two new OFF-by-default flags `source_event_enabled`/`source_event_max_items`, INDEPENDENT of `source_macro_enabled`**; PR #64 **merged + deployed + staging-validated at `a671e97`** — OFF-state clean + ON-state VALIDATED-WITH-ENVIRONMENTAL-NOTE, `source_event_enabled` flip kept ON on staging). The **first** Phase 29D subphase — a NEW reference-only **event-trigger** evidence category (parallel to the 29C macro layer). A new `EventReferenceConnector` (`connectors/event_reference.py`) over two official public procurement/tender venues — **EU TED** (`ted.europa.eu`) + **USAspending.gov** (`usaspending.gov`) — implements the previously theme-dead `fetch_events` hook to emit, per relevant theme, ONE bounded **T2 SOURCE REFERENCE** (`source_type="government_data"` — deliberately NOT `"government_contract"`; `ProviderType` `procurement`; fixed official public URL, no API key, **no specific tender / award / contractor / amount / contract-number / date**) + honest `data_not_sourced` gap ("live tenders/awards not fetched at report time"). Each item is a **WEAK** internal research-priority signal + `needs_human_review`, records freshness via `stale_after_days`, and is **NOT a materiality claim / candidate / catalyst / trade signal**; network-free. `collect_theme_event_evidence(theme, region, cfg)` (`event_evidence.py`) is DARK when `source_event_enabled` False, bounded by `source_event_max_items` (3). `registry.py` promotes `eu_ted` + `usaspending` PLANNED→**enabled** (`procurement`, `T2_regulator_or_gov`), so `/sources/registry` + `/sources/health` show both enabled — registry now **28 enabled / 2 scaffolded / 5 planned / 35 total** (SEDAR+/ASX scaffolds; google_patents/uspto/epo_espacenet + openbb + local-language press stay planned → 29D.2/later). Discovery council threads event refs as citeable `R#` run facts (in `evidence_ids()`) + honest gaps; company report gains an OPTIONAL `industry_event_context` block (beside `industry_macro_context`; WEAK event CONTEXT — not company-specific, not a catalyst/materiality/trade signal, no figures), rendered only when `source_event_enabled` + a reference exists; `CouncilResult.event_context` via `to_metadata_dict` (empty `[]` off). `schema_valid`/`safety_valid` stay true, `publication_ready` false, `human_review_required` true; **dark-by-default** — flag off → discovery pack + report body + macro layer byte-identical. **Deliberate deferral:** live EU TED / USAspending tender/award FETCH is DEFERRED (reference-only) — a Phase 29D follow-up; full candidate-generation-from-events also deferred (candidates come from the curated registry — AC1 satisfied at the context level). AC coverage: discovery cites event-trigger context (AC1, context level), source-tiered + citeable (AC2), freshness via `stale_after_days` (AC3), weak/needs-review labeling (AC4), `human_review_required=true` (AC5), no recommendation (AC6). Verified GREEN (pre-staging): backend **2184 pass / 12 skip / 0 fail**, ruff clean, mypy `71` baseline (no new), security scan PASS; 18 files (incl. tests); internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change. **First 29D subphase — closed; 29D.2 (patents) now PR-open (see the 29D.2 row); 29D.3 (permits/regulatory-event metadata) still upcoming.** |
| Phase 29D.2 | ✅ Complete | Patent Event-Trigger Reference Connectors (backend-only; no migration, still DB head `011`; no new host/endpoint/secret; **NO new flag — reuses the existing OFF-by-default `source_event_enabled`**; PR #65 **merged + deployed + staging-validated at `1c6b1c9`** — VALIDATED-WITH-ENVIRONMENTAL-NOTE, `source_event_enabled` already ON, kept ON). The **second** Phase 29D subphase — extends the 29D.1 reference-only **event-trigger** layer to **patents** with **zero new wiring**. A new `PATENT_SOURCES` table (added into the combined `ALL_EVENT_SOURCES`) is served by the SAME generic `EventReferenceConnector` over three official public patent venues — **Google Patents** (`patents.google.com`, **T5** aggregator index, provider `patents`), **USPTO** (`uspto.gov`, **T2**, provider `patents`) + **EPO Espacenet** (`worldwide.espacenet.com`, **T2**, provider `patents`). A per-kind `_EventFlavor` makes patent refs **purely thematic** (innovation / R&D / patent / IP / technology / semiconductor / pharma / battery / EV / materials), `source_type="government_data"`. Each emits ONE bounded **T2/T5 SOURCE REFERENCE** (fixed official public URL, no API key, **no specific patent number / title / inventor / assignee / claim / filing-or-grant date**) + an honest `data_not_sourced` gap, and carries an explicit disclaimer that **NO legal / infringement / validity / patentability / ownership / competitive-strength conclusion is drawn** — patents are **WEAK internal research-priority CONTEXT only**, `needs_human_review`, `stale_after_days` freshness; network-free. Reuses the SAME collector (`collect_theme_event_evidence` now iterates `ALL_EVENT_SOURCES`), the discovery-council `R#` citation path, the `industry_event_context` report block and the existing `source_event_enabled` flag — **NO new flag/wiring**. `registry.py` promotes `google_patents` + `uspto` + `epo_espacenet` PLANNED→**enabled** (all provider `patents`), so `/sources/registry` + `/sources/health` show all three — registry now **31 enabled / 2 scaffolded / 2 planned / 35 total** (only `openbb` + `local_language_business_press` remain planned; SEDAR+/ASX remain the two scaffolds). **Known reuse-consequence (recorded):** the `industry_event_context` narration is procurement-flavored, so a patent surfaced there is described generically ("venue reference … not a candidate / catalyst / trade signal") — the patent-specific "no legal conclusion" disclaimer lives in the item excerpt / gap. **Deliberate deferral (carried from 29D.1):** live patent-filing FETCH remains DEFERRED (reference-only) — a Phase 29D follow-up. `schema_valid`/`safety_valid` stay true, `publication_ready` false, `human_review_required` true; **dark-by-default** — flag off → discovery pack + report body + macro/event layers byte-identical. Verified GREEN (pre-staging): backend **2210 pass / 12 skip / 0 fail**, ruff clean, mypy `71` baseline (no new), security scan PASS; 12 files (incl. tests); internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change. **Second 29D subphase — closed; 29D.3 (permits / regulatory-event venues) is the LAST 29D subphase — now PR-open (see the 29D.3 row below).** |
| Phase 29D.3 | ✅ Complete | Permit / Regulatory-Event Reference Connectors (backend-only; no migration, still DB head `011`; no new host/endpoint/secret; **NO new flag — reuses the existing OFF-by-default `source_event_enabled`**; PR #66 **merged + deployed + staging-validated at `d567019`** — completes the whole Phase 29D umbrella and ALL OF PHASE 29). The **third and LAST** Phase 29D subphase — completes the reference-only **event-trigger** layer by extending it to **permits / regulatory-event venues** with **zero new wiring**. A new `PERMIT_SOURCES` table (into the combined `ALL_EVENT_SOURCES` = procurement + patents + permits) is served by the SAME generic `EventReferenceConnector` over three official public regulatory venues — **FERC** (`ferc.gov`, energy / grid / transmission / pipeline / LNG dockets & permits), **US NRC** (`nrc.gov`, nuclear reactor licensing / permits) and **US EPA** (`epa.gov`, environmental / emissions / industrial permitting) — all provider **`permits`** (a NEW `ProviderType.permits`), **T2**, `source_type="government_data"`, fixed official public URLs (no API key). A per-kind `_PERMIT_FLAVOR` makes permit refs **purely thematic** (energy / grid / transmission / pipeline / nuclear / environmental / emissions / mining / lng / power-plant / permit / licensing — deliberately **excluding bare "industrial"** to avoid a GICS-Industrials substring collision with the 29D.1 defense path). Each emits ONE bounded **T2 SOURCE REFERENCE** (fixed official public URL, no API key, **no specific docket / case / permit number / applicant / decision / outcome / date**) + an honest `data_not_sourced` gap, and carries an explicit disclaimer that **NO regulatory-outcome / approval / denial / materiality conclusion is drawn** — permits are **WEAK internal research-priority CONTEXT only**, `needs_human_review`, `stale_after_days` freshness; network-free. Reuses the SAME collector (`collect_theme_event_evidence` iterates `ALL_EVENT_SOURCES`), the discovery-council `R#` citation path, the `industry_event_context` report block and the existing `source_event_enabled` flag — **NO new flag/wiring**. **Folded-in 29D.2 tidy:** `_event_discovery_facts` (`discovery_council.py`) now labels each run-fact **per `provider_type`** (procurement byte-identical; patents → "patent office / index venue reference"; permits → "permit / regulatory-event venue reference"), resolving the 29D.2 cosmetic-mislabel follow-up. `registry.py` adds `ferc` + `us_nrc` + `us_epa` as **NEW enabled** event-reference sources (all provider `permits`, T2), so `/sources/registry` + `/sources/health` show all three — registry now **34 enabled / 2 scaffolded / 2 planned / 38 total** (only `openbb` + `local_language_business_press` remain planned; SEDAR+/ASX remain the two scaffolds). **Deliberate deferral (carried from 29D.1/29D.2):** live permit / docket FETCH remains DEFERRED (reference-only) — the keyed FERC eLibrary / EPA / NRC ADAMS APIs are NOT used. `schema_valid`/`safety_valid` stay true, `publication_ready` false, `human_review_required` true; **dark-by-default** — flag off → discovery pack + report body + macro/event layers byte-identical. Verified GREEN (pre-staging): backend **2238 pass / 12 skip / 0 fail**, ruff clean, mypy `71` baseline (no new), security scan PASS; 13 files (incl. tests); internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change. **LAST 29D subphase — closed; the whole Phase 29D umbrella (procurement + patents + permits) and ALL OF PHASE 29 are complete; Phase 30A (language detection + translation foundation) is now PR-open (see the 30A row below).** |
| Phase 30A | ✅ Complete | Language Detection + Translation Foundation (backend-only; no migration, still DB head `011`; no new host/endpoint/secret; four OFF-by-default flags `source_translation_enabled`/`source_translation_max_chars`(400)/`source_translation_max_excerpts`(3)/`translation_provider`(`fake`); **merged + deployed + OFF-state staging-validated at `fa3632a` (PR #67)**; closure `docs/development/closures/phase-30a.md`). A **foundation-only** language + translation layer — OFF by default, with the LLM-backed provider deliberately **not** the default. A new `app/services/sources/language.py` exposes `detect_language(text, hint)` for **en/fr/de/it/da** (generalizes the document-extractor stopword heuristic, adds Danish/Nordic) + `LANGUAGE_NAMES`/`language_name()`; `document_text_extractor._detect_language` now delegates (behaviour preserved). A new `app/services/sources/translation.py` defines `TranslationResult` + a `TranslationProvider` ABC with **`FakeTranslationProvider`** (the DEFAULT — an honest, clearly-marked placeholder that **NEVER** fabricates fluent English) and **`LLMTranslationProvider`** (composes the existing `get_llm_client()` — no new host/model/secret; bounded input AND output; logs ONLY counts + language codes, **NEVER** the prompt/original/translated text), chosen by `get_translation_provider(cfg, *, client)`. Every `TranslationResult` carries a `MACHINE_TRANSLATION_WARNING` ("machine-assisted, NOT an official translation; human review required") + `needs_human_review=True`, is **bounded per-excerpt (never whole-document)**, and always preserves the **original text + secret-stripped `source_url`**. The single-company **council** (`llm/council.py::_collect_translated_excerpts`, gated by `source_translation_enabled`) detects non-English evidence excerpts (`requires_translation` OR a detected non-`en` language) and produces BOUNDED machine-assisted translations, surfaced via `CouncilResult.translated_excerpts` → `to_metadata_dict` (empty `[]` when off, mirroring `macro_context`/`event_context`/`primary_documents`) — each entry keeps the original + `source_url` as the **citation of record**. The **report** gains an OPTIONAL `report_content["translated_evidence"]` block (rendered only when translated excerpts exist) with the machine-assisted / NOT-official / human-review disclaimer, scanned by the safety gate **before** validation. **Interpretation call (documented):** translated excerpts are exposed as council METADATA + a report block (each retaining the original `source_url`), **not** injected into the single-company council's evidence pack — mirroring the macro/event precedent; original evidence untouched. Existing infra is reused (`EvidenceItem.language`/`original_language`/`requires_translation`, `GapType.translation_required`, and the 29B.4 euronext-FR/deutsche_boerse-DE/nordic-DK `requires_translation` markers). `schema_valid`/`safety_valid` stay true, `publication_ready` false, `human_review_required` true; **dark-by-default** — flag off → council pack + report body byte-identical. **Deliberate:** LLM-backed translation OFF by default (default provider `fake`); this is a FOUNDATION — **Phase 30B** will add local-language evidence sources that consume it. Verified GREEN (pre-staging): backend **2264 pass / 12 skip / 0 fail** (+`test_phase30a_translation.py`), ruff clean, mypy `71` baseline (no new), security scan PASS; 9 files (incl. tests); internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change. **Phase 30A merged + OFF-state staging-validated (`SOURCE_TRANSLATION_ENABLED` KEPT OFF — no non-English excerpts on staging to translate until 30B; the happy path is unit-fixture-proven, not staging-demonstrable); the Phase 30 umbrella stays open — Phase 30B (local-language evidence sources) is now PR-open (see the 30B row), then Phase 31 (source-aware research memo).** |
| Phase 30B | ✅ Complete | Local-Language Business-Press Evidence Sources (backend-only; no migration, still DB head `011`; no new host/endpoint/secret; **NO new flag** — reuses the OFF-by-default `source_connector_enabled` (collection) + `source_translation_enabled` (the Phase 30A layer); **merged + deployed + staging-validated at `e1d2d8d` (PR #68)**; closure `docs/development/closures/phase-30b.md`). The **second** Phase 30 subphase — adds the local-language evidence SOURCES that produce the non-English excerpts the 30A layer translates. A new `LocalLanguagePressConnector` (`connectors/local_language_press.py`) + a `LOCAL_LANGUAGE_PRESS_SOURCES` allowlist of reputable local-language business-press venues — **Les Échos** (FR), **Handelsblatt** (DE), **Milano Finanza** (IT), **Børsen** (DK), each a fixed public HTTPS landing page, no API key — emits, for a **verified non-US FR/DE/IT/DA issuer**, ONE bounded **T4_QUALITY_MEDIA `news_article` SOURCE REFERENCE** (provider `ProviderType.news`) carrying a **GENUINE short local-language descriptive excerpt** (what the venue covers + that the full article content is NOT fetched here — **never a fabricated article / headline / quote / figure / date**), marked `requires_translation=True` + `original_language`, `low` confidence / `metadata_only`, with honest `translation_required` + content-not-fetched gaps + `needs_human_review` — it deliberately **LOWERS** source quality (a WEAK research-priority signal, never a recommendation / catalyst / materiality / valuation); a non-eligible company → an honest `source_not_eligible` gap, never a reference; **network-free**. `company_evidence.py` gains a dedicated `LOCAL_LANGUAGE_REFERENCE_IDS` collection block in `collect_company_source_evidence` that runs the connector for verified non-US FR/DE/IT/DA issuers (regulator mapping untouched). **Consumed by Phase 30A:** with `source_translation_enabled` ON the 30A layer detects the non-English excerpt and produces a bounded machine-assisted `translated_excerpt` (original preserved + `needs_human_review` + NOT-official warning); dark when off. `registry.py` promotes `local_language_business_press` PLANNED→**enabled** (`news` / `T4_quality_media`, `PHASE_30B` label), so `/sources/registry` + `/sources/health` show it enabled — registry now **35 enabled / 2 scaffolded / 1 planned / 38 total** (only `openbb` remains planned; SEDAR+/ASX remain the two scaffolds). AC coverage: non-English evidence appears in evidence-preview (the reference item carries `language`/`original_language`/`requires_translation`); translation state visible; original + translated both bounded (400 chars); source quality reflects the translation / human-review need (T4 / low / needs-review); the LLM council cites the translated evidence safely via 30A (the original `source_url` stays the citation of record). `schema_valid`/`safety_valid` stay true, `publication_ready` false, `human_review_required` true; **dark-by-default** — with the collection flag off the pack + report body are byte-identical to Phase 30A. Verified GREEN (pre-staging): backend **2278 pass / 12 skip / 0 fail** (+14 over 30A; +`test_phase30b_local_language_sources.py`), ruff clean, mypy `71` baseline (no new), security scan PASS; 15 files (incl. tests); internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change. **Phase 30B merged + staging-validated (`e1d2d8d`) → the Phase 30 umbrella (30A foundation + 30B local-language sources) is COMPLETE; Phase 31 (source-aware internal research memo builder) is the final phase.** |
| Phase 31 | 🟡 PR open / pre-staging | Source-Aware Internal Research Memo Builder (full-stack; **7 files incl. tests**; **no migration**, still DB head `011`; **no new host/endpoint/secret**; ONE new OFF-by-default flag `source_research_memo_enabled` / `SOURCE_RESEARCH_MEMO_ENABLED`; branch `feature/phase-31-research-memo-builder` @ `618344d`, **PR about to open — NOT merged/deployed/validated**). **The FINAL campaign phase** — on merge + staging validation it closes Phase 31 and completes the entire roadmap through Phase 31. A source-aware **INTERNAL research MEMO builder**, OFF by default. Backend `_build_research_memo(report_content, council_result)` (`services/final_report_generator.py`) is a **DETERMINISTIC synthesis** of the already-assembled `report_content` sections + `CouncilResult` metadata + gaps — **no external call / no LLM / no ORM / no recompute** — emitting `report_content["research_memo"]` (attached BEFORE the safety gate): `header` (internal-only / not-advice), `company_identity`, `why_surfaced`, `what_is_sourced`, **`what_is_missing` (PROMINENT)**, `primary_evidence_summary` (primary_documents + primary_facts, cited with token-stripped `source_urls`), `catalyst_event_evidence`, `financial_facts_summary` (T5 EODHD + T1 `*_primary_filing` datapoints; **no derived valuation**), `business_risk_summary`, `council_disagreement_red_team` (`red_team` dissent + `unsupported_claims` + committee label), `research_next_steps`, `human_review_checklist` (references the existing checklist, no recompute), `source_appendix`, a `disallowed_outputs` notice, `note` + `disclaimer` + `human_review_required=True`. **Citation-bound** (each claim ties to an existing provenance / source / citation). The forbidden BUY/SELL/HOLD/WATCH + price-target/fair-value/upside/downside literals appear ONLY in the scanner-exempt `disallowed_outputs` field (its key is in `_EXEMPT_FIELD_NAMES`); every other field is safety-clean. **Honest degradation** on thin evidence (`what_is_missing` prominent, `provenance=missing_data`, honest-empty sections, never fabricates). NOT in `_REQUIRED_SECTIONS` (so `schema_valid` is unaffected); `publication_ready` False; `human_review_required` True; **dark-by-default byte-identical when off** — and the memo happy-path IS staging-demonstrable (flip the flag → a real report includes the memo, since it derives from existing report data). Frontend: `research_memo` added to `SECTION_ORDER` + an "Internal Research Memo" label (`components/reports/finalReportContent.ts`); a bespoke `ResearchMemoSection` (`components/reports/FinalReportRenderer.tsx`) renders the memo's sub-blocks readably in the **Readable** tab (Raw JSON stays the hidden-by-default developer tab; legacy reports without the memo render unchanged); `disallowed_outputs` renders as a plain NOTICE (no rating / BUY-SELL UI). Verified GREEN (pre-staging): backend **2289 pass / 12 skip / 0 fail**, ruff clean, mypy `71` baseline (no new); web typecheck / lint / build pass; e2e **196/196**; security scan PASS; 7 files (incl. tests). Internal only, no recommendation / valuation outside the exempt notice, `human_review_required=true` / `publication_ready=false`, no publish route, `/admin/*` OAuth-gated, no auth change. **Phase 31 is PR-open — STOP at the merge gate; on merge + staging validation it CLOSES Phase 31 and, as the FINAL phase, completes the entire roadmap through Phase 31.** |
| Phase 32A Slice 5 | 🟡 Slices 5A/5B.1/5B.2/5B.3 merged + staging-validated (each individually) — **Phase 32A as a whole is NOT fully closed** | Bounded Primary-Document Ingestion (backend-only; **migration `013`** — `013_add_extracted_documents.py`, reversible / additive / backfill-free, creates `extracted_documents` + `extracted_facts`; this was the **foundation of the LAST Phase 32A slice**, which then continued through Slices 5B.1 (migration `014`, document reachability + secure fetching), 5B.2 (real Azure OCR adapter, no migration), and 5B.3 (admin API + web visibility, no migration) — full detail per sub-slice is tracked in `docs/development/PHASE_LEDGER.md`, rows `32A.5A`/`32A.5B.1`/`32A.5B.2`/`32A.5B.3`). Deepens the Phase 29B.2 document-extraction capability so the single-company council can eventually cite an issuer's OWN primary documents with precise provenance. **New service layer:** `sources/primary_document_extractor.py` (pdfplumber native-PDF text + table extraction with page/table location + stdlib-`HTMLParser` deepened HTML tables/sections/boilerplate-removal; raw-bytes `content_hash`), `sources/extracted_fact_validator.py` (stricter validation for table/OCR-derived facts — label/value/unit/period + column alignment + cross-field arithmetic + cross-method agreement; non-validated → `excerpt_only`, never a fact), `sources/ocr_provider.py` (a `TranslationProvider`-style **NoOp seam only** — the sole provider returns an empty `ocr_unavailable` result, never fabricated text; a real Azure Document Intelligence adapter is DEFERRED, needs resource provisioning + admin sign-off — see `docs/DECISIONS.md` ADR-014), `services/extracted_document_service.py` (persist + TTL-bounded reuse), + `models/extracted_document.py` (`ExtractedDocument` / `ExtractedFact`). **Ingestion hierarchy (least-cost first):** structured SEC/XBRL (unchanged) → deepened HTML → native PDF → OCR fallback (NoOp), run in the source-connector phase inside `maybe_run_council` BEFORE the council under an AGGREGATE wall-budget so ingestion + the ~150s council stay under the ~230s inline gateway; extraction persists (migration `013`) + is reused across regeneration. **Evidence pack** gains a `primary_document` floor+cap (ON only with the master flag) WITHOUT weakening the Slice-2 `financial_floor=3` / news caps; citations carry page/section/table location; no citation from failed / metadata-only extraction; a deep item's runtime-only `document_content_hash` (`exclude=True`) keys one canonical `Source` per document. **Security:** allowlist-gated hardened fetch with an opt-in resolved-IP / DNS-rebinding guard (before & after redirects), `%PDF` magic-byte check, decompression-bomb + page/byte caps, Pillow image-pixel cap, external text treated as untrusted inert data; NO user-supplied-URL surface, no new public endpoint, no JS/browser/paywall/auth bypass. **Gated by a default-OFF master flag `PRIMARY_DOCUMENT_INGESTION_ENABLED` (+ `PRIMARY_DOCUMENT_OCR_ENABLED` + 15 knobs; reuse/persistence additionally gated on `REPORT_CITATION_PERSISTENCE_ENABLED`); flag OFF ⇒ byte-for-byte identical to Phase 29B.2 / Slice 4, tables stay empty.** New dependency `pdfplumber>=0.11,<0.12` (pure-Python; + pdfminer.six + Pillow transitively; NO tesseract/pdf2image/pymupdf). **Honest limitation (at the time this slice's foundation was written):** scanned / JS-gated issuer PDFs (e.g. some Richemont reports) still degraded honestly to metadata-only / gaps; no fabricated facts. Verified GREEN (pre-staging): 7 new `test_phase32a_slice5_*.py` files + `tests/helpers/pdf_fixtures.py`. Internal only, no BUY/SELL/HOLD/WATCH/target/fair-value/upside/recommendation, `human_review_required=true`, `publication_ready=false`, no publish route, no auth change. **What happened next (tracked in `docs/development/PHASE_LEDGER.md`, not re-detailed here):** Slice 5A itself closed staging-validated as a foundation with an efficacy caveat (0/7 successful native extractions at the time); Slice 5B.1 added real SEC filing-body fetch + bounded non-browser document discovery + durable ingestion-attempt persistence (migration `014`) and closed staging-validated with real extractions proven live; Slice 5B.2 added a real Azure Document Intelligence OCR adapter behind the same `OcrProvider` seam and closed as a foundation with an explicit efficacy caveat (the real OCR API call itself was never observed firing on staging, for structural reasons); **Slice 5B.3 — the LAST slice of Slice 5 — added `GET /api/v1/reports/{report_id}/primary-documents` (admin-scoped provenance API) and a "Primary Documents" admin UI tab, and closed staging-validated with no caveats of its own** (see `docs/development/closures/phase-32a-slice5b3.md`). **Phase 32A as a whole is NOT fully closed** — one exact, structural (non-defect) blocker remains: a real, live Azure Document Intelligence OCR API call has never been observed executing on staging. See `docs/development/closures/phase-32a-final-status.md` for the authoritative overall verdict. |

---

## What Is Not Yet Implemented

- **TrendSignalEngine in main workflow** — `TrendSignalEngine` exists but is not yet wired into `company_analysis`; Phase 19.2
- **Price data visible in T5 source-tier summary** — EODHD /eod price not visibly confirmed in staging smoke test; Phase 19.2
- **Stooq fallback on Azure** — Stooq appears blocked from Azure outbound; `free_real` provider needs non-blocking fallback to EODHD price-only; Phase 19.2
- **Composite provider_name tracking** — workflow metadata does not yet preserve composite provider names (e.g. `"free_real: stooq+sec_edgar"`); Phase 19.2
- **Admin auth hardening** — ✅ **Delivered (Phase 23):** `/admin/*` and `/api/admin/proxy/*` require an authenticated, allowlisted admin — GitHub OAuth sign-in → dependency-free HMAC-signed httpOnly session cookie (`AUTH_SECRET`) + env allowlist (`ADMIN_ALLOWED_EMAILS`), enforced by the Next 16 Proxy (`src/proxy.ts`) plus an independent re-check in the admin proxy route. Backend Basic Auth retained as server-to-server defense. Microsoft Entra ID and a persisted per-action audit trail remain future enhancements
- **News + catalyst discovery workflow** — ✅ **Delivered (Phase 24):** `catalyst_discovery_agent` wired into `company_analysis` (SEC recent filings T2 + company press releases T1 + optional news T5; deterministic classifier; report sections + council context; final-report `news_catalyst_discovery`). Optional paid/high-quality news provider + LLM-assisted event summarization remain future enhancements
- **Real market candidate discovery** — ✅ **Delivered (Phase 25):** bounded internal-only `market_discovery_service` + `discovery_scoring_service` (momentum + fundamentals + catalyst + source-quality + completeness, internal prioritization only) over a curated seed / manual universe; `discovery_runs` + `discovery_candidates` (migration 010); 6 admin-only `/api/v1/market-discovery/*` endpoints + `/admin/discovery` UI. Full-market crawl and scheduling remain out of scope
- **Public report publishing** — all reports are internal only; public approved-report pages are Phase 26
- **User accounts** — no signup, dashboard, watchlists, or preferences yet; Phase 27
- **Paid plans / Stripe** — not built; Phase 28
- **Personalized research reports** — not built; Phase 29
- **Monitoring + alerts** — thesis tracking table exists (Phase 22); automated monitoring workflow is Phase 30
- Azure OpenAI in production (real keys) — optional, configure `LLM_PROVIDER=azure_openai` + env vars
- Live EODHD calls require `EODHD_API_KEY` — set in env or Azure Key Vault; tests run offline
- Azure AI Search (embeddings, RAG) — future
- Azure Blob Storage (PDF documents) — future
- OpenBB integration (evaluation pending) — future
- Scheduled background jobs (Azure Functions / Service Bus) — Phase 30+
